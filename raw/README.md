# Raw Data

Raw provider captures, shadow snapshots, vendor files, and bulky research inputs
are kept outside git. This directory may contain routing notes (`AGENTS.md`),
but not production raw data.

See `raw/AGENTS.md` for the file registry and rules.
