id: mid-02
title: A1.5 PAYLOAD_BIND — SIGNED_ORDER_PERSISTED event + signer interception (schema cols MERGED INTO up-04 per Mid R2L2 A1)
merged_into: up-04  # Mid R2L2 A1 ALTER MERGE — up-04 absorbs all schema columns including mid-02's 3 signing cols (signed_order_hash, payload_hash, signed_at_utc); mid-02 retains EVENT-payload + writer-interception work only.
authority_basis:
  - Apr26 review §3 F-001 (line 303, payload-binding residual after INV-30 row-state)
  - Apr26 review §3 F-003 (line 353, exchange-proven idempotency / signed-order-hash)
  - Apr26 review §3 F-008 (line 462, YES/NO outcome-token identity at command level)
  - Apr26 §8.3 transition_SIGNED_ORDER_PERSISTED + transition_COMMAND_PERSISTED
  - INV-29 closed-grammar amendment authority (implementation_plan.md:226)
  - Judge ledger §29 X1 verdict: signer.sign(order) interception seam approved CONDITIONALLY (Region-Down's collapsed D-phase shape MUST preserve this seam)
file_line:
  current_state:
    - src/state/venue_command_repo.py:194-242  # insert_command schema (NO signed_order_hash, NO condition_id, NO outcome_index columns; only market_id + token_id)
    - src/execution/executor.py:917  # client.place_limit_order — SIGNING happens INSIDE SDK call, hash not pre-persisted
    - src/execution/command_bus.py:64-77  # CommandEventType enum; SIGNED_ORDER_PERSISTED + COMMAND_PERSISTED missing
    - src/execution/command_bus.py:164-218  # IdempotencyKey.from_inputs (LOCAL hash, not signed-order hash; F-003 residual)
    - src/contracts/execution_intent.py  # ExecutionIntent currently has token_id but not condition_id, outcome_index, no_token_id
  schema_migration_seam: src/state/db.py init_schema() — venue_commands ALTER TABLE
  signing_interception_seam: py-clob-client unified-surface signer.sign(order) — verified via X1 conditional verdict (R2/R3 file:line MUST cite the actual signer hook)
schema_changes:
  # POST Mid R2L2 A1 ALTER MERGE (2026-04-26): up-04 absorbs ALL schema columns.
  # mid-02 carries NO ALTER TABLE work; it reads up-04 columns at runtime and writes events.
  # See: docs/operations/task_2026-04-26_ultimate_plan/evidence/mid/converged_R2L2.md §A1
  pointer: up-04 owns 15-column ALTER (12 original + signed_order_hash + payload_hash + signed_at_utc absorbed from mid-02).
  mid_02_runtime_only:
    # mid-02's runtime work after up-04's ALTER lands:
    - "Compute signed_order_hash = keccak(signed.signature) at polymarket_client.py:195 (post-create_order, pre-post_order)"
    - "Compute payload_hash = SHA-256(canonical OrderArgs json) at polymarket_client.py:194 (pre-create_order)"
    - "UPDATE venue_commands SET signed_order_hash=?, payload_hash=?, signed_at_utc=? via append_event(SIGNED_ORDER_PERSISTED, payload={...})"
    - "invariant_hash already in up-04; mid-02 may consume but does not write/compute it (source columns also up-04 owned)"
new_event_types:
  - CommandEventType.COMMAND_PERSISTED  # row exists in INTENT_CREATED before signing (already implicit; surfacing as explicit event for §8.3 audit-table compat)
  - CommandEventType.SIGNED_ORDER_PERSISTED  # populated at signer.sign(order) seam, BEFORE place_limit_order POST
new_states: []  # no CommandState additions; SIGNED_ORDER_PERSISTED transitions ACKED→ACKED (idempotent within ACKED) OR is recorded against SUBMITTING before the post
write_path:
  current_chain: insert_command → SUBMIT_REQUESTED → place_limit_order(SDK) → SUBMIT_ACKED
  new_chain: insert_command → COMMAND_PERSISTED (event) → SUBMIT_REQUESTED → signer.sign(order) → SIGNED_ORDER_PERSISTED (event with signed_order_hash payload + UPDATE venue_commands.signed_order_hash) → place_limit_order(POST signed) → SUBMIT_ACKED
  signer_seam_commitment: mid-02 implementation MUST intercept at py-clob-client unified surface signer.sign(order); R2 file:line cite required
antibody_test:
  new_file_or_extend: extend tests/test_executor_command_split.py + new tests/test_payload_binding.py
  assertions:
    - test_signed_order_hash_persisted_before_sdk_post  # payload-binding analog of INV-30 row-state; signed_order_hash column NON-NULL before place_limit_order returns
    - test_invariant_hash_detects_mid_cycle_market_drift  # if condition_id changes between command create and place, invariant_hash mismatch raises
    - test_outcome_index_yes_no_round_trip  # YES vs NO direction-to-outcome_index mapping is bijective and matches token_id resolution
    - test_clob_token_ids_raw_provenance  # raw Gamma response stored verbatim; reconstructable forensically
    - test_signed_order_hash_unique_per_idempotency_key  # idempotency replay returns same signed hash (deterministic signing across retries)
    - test_command_persisted_event_emitted  # explicit COMMAND_PERSISTED event in venue_command_events table at INTENT_CREATED step
  extends_existing:
    - tests/test_command_bus_types.py:268 (test_inflight_states_match_repo_unresolved_filter) — must remain green after enum extension
    - tests/test_command_bus_types.py (new test) — round-trip closed-grammar including SIGNED_ORDER_PERSISTED + COMMAND_PERSISTED
  closes_invariants: [INV-30 (extended to payload-binding), F-001 payload-residual, F-003, F-008, NC-19 (extension to durable-payload idempotency)]
depends_on:
  - up-04  # SINGLE coordinated venue_commands ALTER TABLE per X-UM-1 cross-region 2026-04-26
  - X1 (judge verdict §29) — signer.sign(order) seam stability from Region-Down's collapsed D-phase
  - INV-29 amendment (implicit in mid-03 if any new CommandState is needed — currently none for mid-02; mid-02 only adds CommandEventType members which is a smaller amendment scope)
estimated_h: 12-18  # schema migration + write-path rewiring + 6 antibody tests + signer-seam audit
risk:
  - "X1-CONDITIONAL: judge X1 verdict §29 ties mid-02 to signer.sign(order) seam. If Region-Down's D-phase collapse REMOVES that seam (e.g. SDK switches to monolithic post that signs internally without external hook), mid-02 cannot intercept and F-001 payload-binding STAYS OPEN. Failure mode: mid-02 lands but signed_order_hash column is always NULL or populated only post-hoc from venue response (post-side-effect = reverses INV-30). Critic MUST verify the seam exists."
  - "X-UM-1 COORDINATION: if up-04 and mid-02 both attempt ALTER TABLE for condition_id, second migration fails. If up-04 owns it (current decision), mid-02 implementation must coordinate test-fixture schema state."
  - "DETERMINISTIC SIGNING: test_signed_order_hash_unique_per_idempotency_key relies on EIP-712 signing being deterministic across retries given identical inputs. py-clob-client behavior here must be verified at R2/R3 cite-time."
  - "EIP-712 DETERMINISM EVIDENCE GATE (multi-review critic §3 + verifier §ANTIBODY_RUNNABILITY 2026-04-26 ELEVATED TO BLOCKING): mid-02 implementation MUST land evidence/mid/eip712_signing_determinism_2026-04-26.md BEFORE contract lock. Evidence must show: (a) py-clob-client (V1+V2 unified) SDK source path of `signer.sign(order)`, (b) demonstration test signing the SAME OrderArgs twice and asserting identical hash, (c) absence of fresh-nonce / random-salt in EIP-712 typed-data construction. If determinism is FALSE, F-003 closure must be redesigned: idempotency_key + signed_order_hash both stored, dedup by idempotency_key (already in NC-19), signed_order_hash as forensic-only audit column (NOT a uniqueness key). Critic-opus verdict gates contract lock."
  - "PROVENANCE-BY-CODE: clob_token_ids_raw stores raw Gamma response. Per Memory L20+L21 antibody discipline, this column gives forensic reconstruction; antibody test_clob_token_ids_raw_provenance LOCKS the contract."
critic_gate:
  - signer.sign(order) seam grep-verified to exist in py-clob-client at R2/R3 cite-time
  - All 6 antibody tests pass on HEAD with mid-02 patch applied
  - up-04 dependency resolved (either landed first or X-UM-1 sequencing committed)
  - INV-29 amendment scope (CommandEventType extensions only) does NOT touch CommandState; mid-03 is the slice that touches CommandState if needed
  - test_command_state_strings_match_repo (per src/execution/command_bus.py:50-51 docstring assertion) remains green
notes:
  - This card REPLACES my Turn-5 mid-02 + mid-03 schema-migration ownership (now resolved via X-UM-1 routing to up-04).
  - mid-01 (A1) depends_on mid-02 because RED→cancel commands need signed_order_hash to be a no-op-extension on the SAME schema once mid-02 lands. CANCEL events emitted by mid-01 do NOT themselves need signed_order_hash (cancel is order-id-keyed, not order-payload-keyed), but the ROW they cancel MUST have signed_order_hash for forensic traceability of what was cancelled.
  - K6 settlement (REDEEM_*) explicitly OUT-OF-SCOPE per opponent-mid Strike-3 + judge §74 K=4 mid-active.
