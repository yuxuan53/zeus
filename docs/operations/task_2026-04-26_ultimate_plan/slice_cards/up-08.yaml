id: up-08
title: FROZEN_REPLAY_HARNESS — bit-identical replay of probability chain (P_raw → Size) pre/post Wave A+B (calibration-schema-drift antibody)
authority_basis:
  - AGENTS.md "THE PROBABILITY CHAIN" — full chain `51 ENS members → daily max → MC → P_raw → Platt → P_cal → α-fusion → P_posterior → Edge & DBS-CI → Kelly → Size`
  - 'evidence/multi_review/trading_correctness_report.md PROBABILITY_CHAIN_RISKS §1 (schema risk: up-04 15-col ALTER + INV-29 amendment touch venue_commands; "no antibody asserts calibration tooling reads only schema-versioned columns")'
  - evidence/multi_review/trading_correctness_report.md VERDICT §2 (PROBABILITY_CHAIN_FROZEN_REPLAY — bit-identical replay before/after Wave A+B)
  - architecture/invariants.yaml INV-23 (degraded loader must NOT export VERIFIED — same authority direction applies to replay determinism)
  - Fitz Constraint #2 (translation-loss is thermodynamic — encode integrity into structure)
file_line:
  current_state:
    - src/state/venue_command_repo.py:152-238 (insert_command — pre-Wave-A baseline; up-04 ALTER will mutate this surface)
    - src/state/db.py:813 (init_schema — venue_commands DDL)
    - src/contracts/execution_intent.py (input contract for P_posterior → Edge → Kelly → Size pipeline)
    - tests/fixtures/ — must contain at least 3 frozen portfolio states (e.g., 2026-04-15 weather-Day0, 2026-04-20 settlement-day, 2026-04-25 mid-cycle-degraded)
  new_emission_seam:
    - tests/integration/test_frozen_replay_harness.py (NEW)
    - tests/fixtures/frozen_portfolios/ (NEW — 3 captured snapshots)
    - scripts/capture_frozen_portfolio.py (NEW — pre-Wave-A snapshot tool)
antibody_test:
  - |
    tests/integration/test_frozen_replay_harness.py::test_p_posterior_bit_identical_pre_post_wave_a
    asserts: replay of fixture portfolio 2026-04-15 through P_raw → P_cal → P_posterior pipeline yields IDENTICAL bytes (sha256 hash match) after up-04 ALTER lands; if schema change leaks into P_posterior, hash mismatches and test fails
  - |
    tests/integration/test_frozen_replay_harness.py::test_position_size_bit_identical_pre_post_wave_b
    asserts: replay of fixture portfolio through Edge & DBS-CI → Kelly → Size yields IDENTICAL position_size float after mid-01..06 land; isolates calibration-output drift from execution-output drift
  - |
    tests/integration/test_frozen_replay_harness.py::test_replay_uses_only_versioned_schema_columns
    asserts: calibration tooling reads only columns present at pre-Wave-A schema version (validates `pragma user_version` gating); fails fast if calibration accidentally consumes a new up-04 column
  - |
    tests/integration/test_frozen_replay_harness.py::test_red_path_cancel_emission_does_not_alter_p_posterior
    asserts: mid-01's new RED→durable-cmd authority surface does NOT mutate calibration inputs (pure execution-side change); P_posterior is identical with/without mid-01 wired
  - architecture/test_topology.yaml registry entry added
depends_on:
  - up-04  # 15-col ALTER must land — replay validates ALTER doesn't leak into calibration
  - mid-02  # signed_order_hash binding must land — replay validates payload-binding is execution-only, not calibration-side
  - mid-04  # PARTIAL_FILL payload must land — replay validates partial-fill state extension doesn't perturb position-size attribution
  - mid-01  # RED→durable-cmd must land — replay validates RED path doesn't alter calibration
estimated_h: 10
risk: medium
critic_gate: critic-opus
notes:
  - 'SLICE PURPOSE: prove the probability chain (forecast → Size) is structurally INTACT post-plan. Plan touches the tail of the chain (Size → Order). This slice antibodies any silent leak from execution-side changes back into calibration-side outputs.'
  - Frozen-replay is the canonical "translation-loss antibody" per Fitz Constraint #2. Without it, plan integrity is preserved by omission, not by proof.
  - 'Snapshots captured BEFORE Wave A starts via scripts/capture_frozen_portfolio.py. Critical: snapshot tool must capture ALL upstream inputs (ENS members, daily max bins, source role assignments, settlement targets) AT pre-Wave-A schema version.'
  - Lands LATE in Wave B (after mid-01..06 settle) because replay validates the FULL Wave A+B execution-side deltas. Cannot land before up-04 (ALTER) or before mid-04 (PARTIAL state extensions are testable).
  - Post-deploy: re-run on every PR touching src/state/db.py / src/state/venue_command_repo.py / src/execution/* / src/state/lifecycle_manager.py as a regression harness.
  - 'Memory `feedback_grep_gate_before_contract_lock` applies: line cites in this card must be re-grepped within 10min of contract lock.'
