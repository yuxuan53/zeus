id: down-01
title: D1 unified-SDK drop-in swap (preserve signer.sign two-step seam)
authority_basis:
  - docs/operations/task_2026-04-26_polymarket_clob_v2_migration/v2_system_impact_report.md (REWRITE-PENDING per Phase 0.F gate; reference under marketing-label disclaimer)
  - docs/operations/task_2026-04-26_polymarket_clob_v2_migration/zeus_touchpoint_inventory.md §1-2 (chokepoint module + 12 consumer sites)
  - docs/operations/task_2026-04-26_ultimate_plan/evidence/down/_context_boot_opponent.md §3.1 §6 (V2 SDK is unified V1+V2 client; `_resolve_version()` per-token caches; `_is_v2_order()` discriminates per-order)
  - docs/operations/task_2026-04-26_ultimate_plan/evidence/down/converged_R1L1.md (joint R1L1 verdict)
  - judge_ledger §X1/X-MD-1 routing decision (signer.sign seam preservation iff Down's collapsed D-phase preserves the two-step pattern)
  - architecture/invariants.yaml:196 INV-25 (V2 endpoint preflight blocks placement; already shipped on main)
file_line:
  current_state:
    - src/data/polymarket_client.py:17  # CLOB_BASE = "https://clob.polymarket.com"  -- V1 host hardcoded
    - src/data/polymarket_client.py:60-69  # V1 import + ClobClient construction (chain_id=137, signature_type=2 GNOSIS_SAFE)
    - src/data/polymarket_client.py:75-100  # get_orderbook mixed SDK + direct httpx
    - src/data/polymarket_client.py:116-128  # get_fee_rate direct httpx GET /fee-rate (line 153 actual)
    - src/data/polymarket_client.py:148-149  # V1 import OrderArgs + side constants
    - src/data/polymarket_client.py:155  # OrderArgs construction (price, size, side, token_id)
    - 'src/data/polymarket_client.py:195-196  # SEAM: signed = self._clob_client.create_order(args); result = self._clob_client.post_order(signed)  -- THIS IS THE INTERCEPTION POINT MID-02 ANCHORS ON'
    - src/data/polymarket_client.py:167  # cancel_order → self._clob_client.cancel(order_id)
    - src/data/polymarket_client.py:200  # OpenOrderParams import
    - src/data/polymarket_client.py:268  # AssetType, BalanceAllowanceParams import (collateral asset; pUSD-rewire is down-07's scope)
    - requirements.txt:14  # py-clob-client>=0.25
  zero-emission_evidence:
    - "grep 'py_clob_client_v2|create_and_post_order|_resolve_version|_is_v2_order' src/ = 0 hits across HEAD 874e00c"
  new_emission_seam:
    - 'polymarket_client.py:60 import swap: from py_clob_client.client import ClobClient → from py_clob_client_v2.client import ClobClient'
    - polymarket_client.py:148-149 V1 OrderArgs import unchanged (V2 SDK exports OrderArgsV1 too); V2 path uses OrderArgsV2 only when token resolves to v2
    - polymarket_client.py:153 (get_fee_rate body) replace direct httpx /fee-rate with self._clob_client.get_fee_rate_bps(token_id)
    - 'polymarket_client.py:195-196 SEAM-PRESERVED: KEEP two-step `signed = create_order(args); result = post_order(signed)`. DO NOT migrate to V2''s `create_and_post_order` convenience wrapper. This preserves Mid''s mid-02 interception point.'
    - requirements.txt:14 add py-clob-client-v2>=1.0.0 alongside V1; V1 dual-pin removed in down-07/cleanup phase post-monitoring window
antibody_test:
  new_file_or_extend: tests/test_v2_sdk_drop_in_swap.py (NEW)
  assertions:
    - test_polymarket_client_imports_unified_sdk  # `from py_clob_client_v2.client import ClobClient` succeeds
    - test_polymarket_client_preserves_two_step_create_post_pattern  # source-grep / AST assert that polymarket_client.py:195-196 still uses create_order(args) followed by post_order(signed); FAILS if anyone migrates to create_and_post_order
    - test_get_fee_rate_uses_sdk_method_not_direct_httpx  # asserts get_fee_rate body calls self._clob_client.get_fee_rate_bps; FAILS if direct httpx /fee-rate reintroduced
    - test_v1_token_routing_preserved  # mock SDK.__resolve_version returns "v1" for known V1-shape token, assert OrderArgsV1 path used and signature passes V1 EIP-712 domain
    - test_v2_token_routing_works  # mock SDK.__resolve_version returns "v2", assert OrderArgsV2 path used (when V2 markets exist)
  closes_invariants: [INV-25 (already-shipped V2 preflight), F-009 partial (tick/min/negRisk discipline at SDK boundary; full closure in down-03 antibody)]
depends_on:
  - down-02  # D0 question-trio must answer Q1-zeus-egress before swap lands; Q-HB optional (gates down-06)
  - down-03  # unified-client antibody must be GREEN against py-clob-client-v2 v1.0.0 before swap proceeds (tripwire pre-condition)
  - up-04  # X-UM-1 ALTER TABLE adds raw_signed_order/raw_post_order_response columns BEFORE down-01 ships, so D1 wires the new column-fills at the SDK boundary
estimated_h: 4-6
risk:
  - "AUTHORITY-DIRECTION: SDK-source-vs-impact-report: 6 plan premises BUSTED-HIGH per opponent-down boot (unified-client; mandated 10s heartbeat unsourced; delayed-status unsourced; fee_rate_bps removed partial-truth; V1-release-date transcription error; pUSD authority earlier judge-misruled overturned by Q-NEW-1 RPC). All material to D1 scope are absorbed; impact_report REWRITE is critic gate (Phase 0.F)."
  - "SEAM-PRESERVATION CRITICAL: migrating to `create_and_post_order` saves 1 LOC but DESTROYS Mid's mid-02 interception. X1 ruling is conditional on this seam. NO LOC-saving refactor here. Antibody test_polymarket_client_preserves_two_step_create_post_pattern is the immune-system tripwire."
  - "DUAL-PIN DEPENDENCY CONFLICT (low-prob): py-clob-client>=0.25 and py-clob-client-v2>=1.0.0 may share transitive dep ranges; resolve via `pip install -r requirements.txt` clean test in down-01 acceptance + slice 4.A V1-pin-removal in cleanup phase."
  - "GET_BALANCE PATH (out of scope, delegated to down-07): balanceOf rewire to pUSD ERC-20 contract `0xC011a7…2DFB` is down-07's scope per Q-NEW-1 resolution. down-01 keeps existing get_balance call shape; down-07 follow-up swaps the asset_type wiring."
critic_gate:
  - INV-25 V2 preflight test (architecture/invariants.yaml:202-204) MUST stay green after import swap
  - tests/test_neg_risk_passthrough.py:66-83 MUST stay green OR be migrated alongside down-03 antibody addition (V2-SDK still exposes get_neg_risk; V1 antibody pattern survives)
  - test_polymarket_client_preserves_two_step_create_post_pattern MUST FAIL when create_and_post_order is substituted for the two-step pattern (negative-spec test)
  - critic verifies down-03 antibody passes BEFORE down-01 ships (tripwire ordering)
  - dual `pip install -r requirements.txt` (V1 + V2) succeeds without dependency-conflict warnings; if conflict, slice does NOT ship until 1.G dual-pin reconciliation lands
notes:
  - 'X1 verdict: A1 + mid-02 + UNKNOWN_STATUS parallelizable IFF signer.sign seam preserved. down-01 commits to that seam at polymarket_client.py:195-196. mid-02 anchors on the same seam.'
  - '**Down R2L2 (2026-04-26)**: seam line cite updated from rotted `:160-161` (R1L1 boot evidence) to `:195-196` (verified at HEAD 874e00c via Mid R2L2 + R3L3 grep). All 6 occurrences in this yaml replaced.'
  - 'X4 verdict: F-011 raw-payload capture is Up''s clob_market_snapshot + executor.py instrumentation; down-01 fills the columns Up''s up-04 ALTER TABLE adds (post-merge of up-04).'
  - Q-NEW-1 resolved 2026-04-26 11:12 (pUSD distinct ERC-20); collateral asset rewire is down-07's scope, NOT down-01.
  - Sub-routing: down-01 ships AFTER down-02 (D0 questions answered) AND down-03 (antibody green) AND up-04 (column scaffold landed). Three explicit gates.
