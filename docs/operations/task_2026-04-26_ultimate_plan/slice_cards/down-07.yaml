id: down-07
title: pUSD collateral branch — balanceOf() rewire + redemption path activation (ACTIVATED post-Q-NEW-1)
authority_basis:
  - On-chain Q-NEW-1 RESOLVED-DIVERGENT 2026-04-26 11:12 via opponent-down direct Polygon RPC eth_call against polygon.drpc.org for `0xC011a7E12a19f7B1f670d46F03B03f3342E82DFB`:
    - symbol() ABI-decoded "pUSD" (4 bytes 0x70 0x55 0x53 0x44)
    - name() ABI-decoded "Polymarket USD" (14 bytes 0x506f6c796d61726b657420555344)
  - Earlier judge ruling ("0xC011a7…2DFB is bridged USDC marketing label, not separate ERC-20") OVERTURNED by on-chain ground truth (judge ledger Q-NEW-1 line 81 amended)
  - team-lead directive 2026-04-26 11:13 (re-mint down-07 with REAL acceptance criteria; promote from D2-gated tracker to ACTIVE)
  - py_clob_client_v2/config.py Polygon mainnet collateral=0xC011a7…2DFB (SDK still references the historically-USDC.e address; on-chain identity at that address is now pUSD)
  - 'docs.polymarket.com/trading/quickstart "Set Up Your Client": "pUSD (for buying outcome tokens) and POL (for gas, if using EOA type 0)"'
  - zeus_touchpoint_inventory.md §1 line 34 (USDC balance + redemption helpers; pUSD swap point)
file_line:
  current_state:
    - src/data/polymarket_client.py:266-275 (USDC balance + redemption helpers; calls balanceOf() / approve() against the collateral asset)
    - src/data/polymarket_client.py:268 from py_clob_client.clob_types import AssetType, BalanceAllowanceParams
    - 'src/main.py:379 logger.info("Startup wallet check: $%.2f USDC available", balance)'
    - src/execution/harvester.py:1244-1264 T2-G redemption path with comment "USDC still claimable later"
    - tests/ (no test_pusd_collateral_boundary.py exists on HEAD 874e00c)
  zero-emission_evidence:
    - "grep 'pUSD|Polymarket USD' src/ = 0 hits across HEAD 874e00c"
  new_emission_seam:
    - polymarket_client.py:266-275 — balanceOf() ABI calls now target the on-chain pUSD contract at 0xC011a7…2DFB. The address is the SAME as before (config.py unchanged in V2 SDK), but the contract IDENTITY is now pUSD per Q-NEW-1; behavior is preserved by the address but symbol/name labels in code/logs change.
    - polymarket_client.py:268 import path swaps to py_clob_client_v2.clob_types when down-01 ships; AssetType.COLLATERAL still maps to the same on-chain address; SDK's BalanceAllowanceParams structure unchanged.
    - 'main.py:379 wallet-check log copy: "$%.2f USDC available" → "$%.2f pUSD available" (matches on-chain runtime reality)'
    - 'harvester.py:1244-1264 redemption path comment updated; if the redemption ABI for pUSD redemption differs from USDC.e (operator MUST confirm via Phase 0.B SDK source check), branch the redemption call. PRELIMINARY: V2 SDK uses same SDK-internal redemption helpers; no ABI difference at the Zeus call-site level. Confirm in critic gate.'
    - tests/test_pusd_collateral_boundary.py NEW (~80 LOC); see antibody_test below.
antibody_test:
  new_file_or_extend: tests/test_pusd_collateral_boundary.py (NEW)
  assertions:
    - test_balanceof_targets_pusd_address — assert polymarket_client.get_balance() resolves to the pUSD contract at 0xC011a7E12a19f7B1f670d46F03B03f3342E82DFB; mocked Web3 returns balance with the pUSD asset_type marker
    - test_pusd_symbol_assertion — eth_call mock returning ABI-encoded "pUSD" passes; mock returning "USDC" or "USDC.e" fails the test (catches future contract migration)
    - test_redemption_path_calls_pusd_collateral_assettype — harvester redemption uses AssetType.COLLATERAL bound to pUSD address
    - test_wallet_check_log_says_pUSD — main.py:379 startup log emits "pUSD" not "USDC"
    - test_v1_legacy_position_redemption_still_works — Zeus may hold USDC.e from V1-era settled-but-unredeemed positions; the redemption path must NOT mis-label them as pUSD. (Phase 3 cutover prerequisite per plan §6.2 slice 3.A.)
  closes_invariants: [Phase 2.C (pUSD redemption swap); A3 antibody (pUSD ↔ USDC.e boundary); F1 FX classification re-opens as Q-FX-1 — Q-FX-1 is operator decision, NOT engineering, so this slice does NOT close it]
depends_on:
  - down-01  # SDK swap must land first; pUSD ABI rewire piggybacks on the new SDK import path
  - down-02  # Q-NEW-1 evidence file in evidence/q-new-1_*.txt must be on disk citing the on-chain probe results before this slice hardens its asserts
estimated_h: 6
risk: medium
critic_gate: critic-opus (collateral semantics are S0-grade per team-lead directive)

# Down R2L2 (2026-04-26) — A1 SHIP-BLOCK GATE + A3 7-row pUSD propagation matrix:
ship_block_gate:
  rule: "down-07 BLOCKS until Q-FX-1 operator-decision file exists on disk"
  required_artifact: "docs/operations/task_2026-04-26_polymarket_clob_v2_migration/evidence/q-fx-1_classification_decision_2026-04-26.md"
  required_content: "operator signoff specifying ONE of {fx_line_item, trading_pnl_inflow, carry_cost} for USDC.e ↔ pUSD conversion in PnL accounting"
  runtime_gate: "ZEUS_PUSD_REDEMPTION_ENABLED env-flag (defaults False)"
  fail_closed: "FXClassificationPending exception raised in down-07 implementation when env-flag absent or False"
  test: "tests/test_pusd_collateral_boundary.py::test_pusd_redemption_blocked_until_fx_classification — patch env to False, assert clob.redeem() raises FXClassificationPending"
  type_gate_supplement: "Multi-review architect §GAPS:3 + scientist VERDICT 2026-04-26: env-flag is process gate, not type gate. Add typed FXClassification enum at src/contracts/fx_classification.py (NEW) with members {TRADING_PNL_INFLOW, FX_LINE_ITEM, CARRY_COST}. PnL accounting paths (src/observability/status_summary.py + src/state/portfolio_loader.py + src/observability/finance_export.py if exists) IMPORT and consume FXClassification, making misclassification a TypeError at call site, not a string mismatch. Operator decision document specifies which enum value Q-FX-1 selected; module reads the value at startup and stores in FX_CLASSIFICATION constant."
  type_gate_antibody: "tests/test_fx_classification_enum.py::test_misclassified_pusd_redemption_raises_typeerror — calling redemption-path with str literal 'usdc_pusd_swap' (not enum) raises TypeError; only FXClassification enum members accepted"

pusd_propagation_matrix:  # 8-row matrix (4 modify + 4 negative-test) per Down R2L2 A3 turn-3 grep-corrections
  active_modify_sites:
    - row: 1
      site: "src/data/polymarket_client.py:344 get_balance()"
      change: "returns pUSD balance (separate from USDC.e legacy positions)"
    - row: 2
      site: "src/main.py:379 startup wallet check"
      change: 'log copy "USDC" → "pUSD"'
    - row: 3
      site: "src/execution/harvester.py:1244-1264 redemption path"
      change: "branch on collateral_token CHECK constraint (per up-04 col addition)"
    - row: 4
      site: "src/observability/status_summary.py:308 cycle_summary.get('wallet_balance_usd')"
      change: "USD-denominated balance surface needs pUSD-aware: rename to wallet_balance_pusd OR keep 'USD' as union of pUSD + legacy USDC.e (operator-facing decision; recommend explicit rename for clarity)"
  negative_test_sites:  # antibodies — grep-empty at HEAD 874e00c (must STAY clean)
    - row: 5
      site: "src/state/db.py"
      grep: "grep -in 'USDC|pUSD|collateral' src/state/db.py = 0 hits at HEAD 874e00c"
      verdict: "VERIFIED-CLEAN — antibody: `grep -l 'USDC|pUSD' src/state/db.py` returns empty; no schema-level FX awareness"
    - row: 6
      site: "src/riskguard/riskguard.py"
      grep: "grep -in 'USDC|pUSD|collateral|balance' src/riskguard/riskguard.py = 0 hits at HEAD 874e00c"
      verdict: "VERIFIED-CLEAN — riskguard uses $-unit/collateral-agnostic sizing; INVARIANT: riskguard MUST stay collateral-agnostic; antibody asserts `grep -l 'USDC|pUSD' src/riskguard/` empty"
    - row: 7
      site: "src/state/chain_reconciliation.py"
      grep: "grep -in 'USDC|pUSD|collateral|currency' src/state/chain_reconciliation.py = 0 hits at HEAD 874e00c (verified Down R2L2 T3)"
      verdict: "VERIFIED-CLEAN — chain reconciliation operates on shares (token_id) not collateral asset; antibody asserts grep-empty"
    - row: 8
      site: "src/calibration/*"
      grep: "grep -irn 'USDC|pUSD|currency' src/calibration/ = 0 hits at HEAD 874e00c"
      verdict: "VERIFIED-CLEAN — calibration training is currency-agnostic; antibody asserts grep-empty"
  matrix_summary: "4 modify + 4 negative-test (8 rows). Negative-test rows are antibodies preventing collateral-leak into modules that should stay agnostic. Down R2L2 closure does not require code touches in rows 5-8; absence-via-grep is the antibody."

q_new_1_re_verification:
  re_verified_at: "2026-04-26 (Down R2L2 turn-2; fresh on-chain eth_call this run)"
  rpc_endpoint: "https://polygon.drpc.org"
  contract: "0xC011a7E12a19f7B1f670d46F03B03f3342E82DFB"
  symbol_response: "0x...0470555344... (ABI-decoded: 'pUSD', 4 chars)"
  name_response: "0x...0e506f6c796d61726b657420555344... (ABI-decoded: 'Polymarket USD', 14 chars)"
  conclusion: "Q-NEW-1 STABLE; pUSD distinct ERC-20 confirmed at HEAD 874e00c session time. No proxy upgrade since 2026-04-26 11:12. Per memory feedback_on_chain_eth_call_for_token_identity."
notes:
  - PROMOTED from D2-gated tracker (zero LOC) to ACTIVE slice (6h) per Q-NEW-1 RESOLVED-DIVERGENT result.
  - 'The contract address `0xC011a7E12a19f7B1f670d46F03B03f3342E82DFB` is HISTORICALLY associated with USDC.e on Polygon. The on-chain identity TODAY (verified 2026-04-26 11:12 via direct eth_call) is `Polymarket USD` symbol `pUSD`. Either (a) USDC.e was upgraded to pUSD via proxy contract upgrade, or (b) the historical USDC.e address was always Polymarket-deployed and Zeus''s prior assumption was wrong, or (c) some combination. Behavior preservation: balanceOf()/approve() at this address still works the same way; only the asset''s identity label changes.'
  - 'F1 (FX classification) re-opens as Q-FX-1 per converged_R1L1.md "Open" section: "How does Zeus classify USDC.e ↔ pUSD conversion in PnL accounting?" This slice does NOT decide Q-FX-1; operator decides. This slice ensures the CODE handles whatever Q-FX-1 returns.'
  - Wallet check log copy at main.py:379 transitions "USDC" → "pUSD". Operator-visible string change. Phase 3 cutover runbook (down-07's sibling slice down-07-cutover) must brief operators on the rename.
  - 'Why this is S0-grade: collateral semantics determine PnL correctness, redemption-path correctness, and operator trust. Misrouting balanceOf() to a stale USDC.e contract while pUSD is the active collateral leaks capital silently.'
  - 'Authority status: Q-NEW-1 result is dispositive for the immediate work. Q-FX-1 result is required before Phase 3 cutover (per down-07-cutover follow-up; not this slice''s scope).'
