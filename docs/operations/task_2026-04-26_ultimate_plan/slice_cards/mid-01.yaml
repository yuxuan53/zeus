id: mid-01
title: A1 K4 RED → durable-cmd (new authority surface)
authority_basis:
  - docs/operations/task_2026-04-26_execution_state_truth_p1_command_bus/work_log.md:505-510 (PR18 P2 deferred A1)
  - docs/architecture (forthcoming) INV-29 closed-grammar amendment via mid-03
  - Apr26 review §3 F-010 (RED authority drift)
file_line:
  current_state:
    - src/riskguard/riskguard.py:826  # writes force_exit_review=1 to risk_state on RED
    - src/riskguard/riskguard.py:1077-1094  # get_force_exit_review() reader
    - src/engine/cycle_runner.py:359-373  # 2-cycle latency: read flag → mark pos.exit_reason → next cycle exit_lifecycle
    - src/execution/executor.py:325-690  # execute_exit_order is the durable terminal path TODAY
  zero-emission_evidence:
    - "grep insert_command|venue_command_repo|append_event|command_bus across src/riskguard/*.py = 0 hits"
    - "grep insert_command across src/engine/cycle_runner.py = 0 hits (R1L1 turn-3 verified)"
  new_emission_seam: src/engine/cycle_runner.py (cycle_runner-as-proxy — LOCKED per Mid R2L2 A3) → src/state/venue_command_repo.insert_command(intent_kind=IntentKind.CANCEL or IntentKind.DERISK)
  proxy_insertion_anchor:
    description: "mid-01 inserts the proxy emission BLOCK adjacent to the existing force_exit_review block at cycle_runner.py:359-373; new lines will land in the range cycle_runner.py:~365-385 within the same conditional `if force_exit:` scope"
    rationale: "co-locating with the force_exit sweep keeps RED-handling semantics in ONE block; cycle_runner is canonical RED→action authority surface (not riskguard.py)"
    line_anchor_warning: "exact line number will drift on every commit to cycle_runner.py — antibodies cite by SYMBOL (function name + sentinel comment) per memory feedback_grep_gate_before_contract_lock + multi-review architect §STRENGTHS:3"
antibody_test:
  new_file_or_extend: tests/test_riskguard_red_durable_cmd.py (NEW)
  assertions:
    - test_red_emits_cancel_command_within_same_cycle  # RED detection → venue_commands row state=SUBMITTING in same process; no 2-cycle latency
    - test_red_emit_grammar_bound_to_cancel_or_derisk_only  # IntentKind ∈ {CANCEL, DERISK}; ENTRY/EXIT raise ValueError
    - test_red_emit_satisfies_inv_30_persist_before_sdk  # row exists in SUBMITTING before any SDK call
    - test_red_emit_satisfies_nc_19_idempotency_lookup  # idempotency-key check pre-insert
    - test_red_emit_passes_through_command_recovery  # if SDK fails, row resolved by INV-31 recovery loop next cycle
    - test_red_emit_sole_caller_is_cycle_runner_force_exit_block  # function-scope antibody for NC-NEW-D — assert the SOLE caller of insert_command(IntentKind.CANCEL, reason='red_force_exit_proxy') is the new force-exit block in cycle_runner._execute_force_exit_sweep (not just file-scope)
  closes_invariants: [INV-30, NC-17 (grammar-bounded authority surface), F-010]
depends_on:
  - mid-02  # A1.5 PAYLOAD_BIND must land first so CANCEL events carry signed_order_hash provenance; without mid-02, A1 emits CANCELs with no signed-payload binding (F-010 closure incomplete on payload side)
  - mid-03  # mid-03 STATE_GRAMMAR_AMEND adds CLOSED_MARKET_UNKNOWN event-type used by RED→cancel when discovery has invalidated the market mid-cycle
estimated_h: 6-10
risk:
  - "AUTHORITY-DIRECTION: NC-17 holds because new emission is grammar-bounded (CANCEL/DERISK only, never SUBMIT_REQUESTED). If grammar-bound check is bypassed, decorative-capability returns."
  - "OWNERSHIP LOCKED 2026-04-26 Mid R2L2 A3 (post multi-review correction): cycle_runner-as-proxy. riskguard.py REMAINS observability-only (sets force_exit_review flag in risk_state). cycle_runner reads flag + emits CommandBus CANCEL events. NC-NEW-D allowlist scope: cycle_runner._execute_force_exit_sweep ONLY (function-scope, not file-scope per multi-review architect §GAPS:1)."
  - "LATENCY: today RED→cancel is 2-cycle through pos.exit_reason. New direct path is same-cycle. Verify same-cycle in antibody."
  - "FUNCTION-SCOPE ANTIBODY: NC-NEW-D semgrep+runtime test must enforce that insert_command(IntentKind.CANCEL, ...) within cycle_runner.py is reachable ONLY from _execute_force_exit_sweep. Adding a second call site in cycle_runner.py is a re-introduction of decorative authority — guard against per multi-review architect."
critic_gate:
  - INV-30 must hold for new emission path (persist before SDK)
  - NC-19 idempotency lookup must occur pre-insert
  - test_red_emit_grammar_bound_to_cancel_or_derisk_only must FAIL when CANCEL_REQUESTED is replaced with SUBMIT_REQUESTED (negative-grammar test)
  - critic verifies cycle_runner-as-proxy is the SOLE authority surface (NC-NEW-D function-scope antibody catches imposter callers within the same file)
  - tests/test_riskguard_red_durable_cmd.py::test_red_emit_sole_caller_is_cycle_runner_force_exit_block MUST be runnable + green
notes:
  - X1 verdict (judge ledger §29): A1 + mid-02 + D2.A parallelizable if all code against py-clob-client unified surface; mid-02 commits to signer.sign(order) seam.
  - K6 settlement (REDEEM_REQUESTED + REDEEMED) is OUT-OF-SCOPE — Down region (K6 → D2 pUSD redemption flow per routing yaml lines 319-337).
  - mid-01 ↔ NC-19 antibody: tests/test_command_bus_types.py:268 (test_inflight_states_match_repo_unresolved_filter) MUST stay green after CANCEL emission path lands.
