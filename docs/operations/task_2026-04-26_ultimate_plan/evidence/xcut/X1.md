# X1 — Signing-Surface Seam Parallelizability (X-MD-1 fold)

Created: 2026-04-26
HEAD anchor: 874e00cc0244135f49708682cab434b4d151d25d (main)
Status: CONVERGED — pending judge accept

Signed-by:
- proponent @ 2026-04-26
- opponent @ 2026-04-26

## Question

Are A1 (cycle_runner mid-01 RED-cancel emission) + mid-02 (signed_order_hash interception) + Down's D2.A (V2 SDK swap) parallelizable, given the X-MD-1 cross-region risk that V2 SDK might collapse the create_order/post_order seam?

## Verdict

**PARALLELIZABLE** — guarded by triple-belt antibody.

Implementation contract: interception happens BETWEEN `create_order()` return and `post_order()` call, regardless of line numbers. Line :195-196 is the current cite, NOT the contract.

## Triple-belt enforcement

| Layer | Antibody | What it pins |
|---|---|---|
| Semgrep (lint) | NC-NEW-A `zeus-create-order-via-order-semantics-only` (Up R3L3 mint) | Single-emit gate: only `polymarket_client.py + order_semantics.py + tests/` may call `create_order` |
| Mid runtime (single-source) | `tests/test_payload_binding.py::test_create_order_single_seam_polymarket_client_only` | File-set assertion: `git grep -l 'create_order' src/` returns exactly `{polymarket_client.py}` |
| Down runtime (AST shape) | `tests/test_v2_sdk_drop_in_swap.py::test_polymarket_client_preserves_two_step_create_post_pattern` | AST: `create_order(args)` immediately followed by `post_order(signed)` (NOT `create_and_post_order`) |
| Down SDK contract | `tests/test_v2_sdk_contract.py::test_v2_sdk_two_step_seam_intact` | SDK exposes both methods (V1+V2 unified surface) |

## What X1 verdict permits

- mid-02 implementation can land BEFORE down-01 V2 swap, as long as antibodies hold.
- down-01 V2 swap cannot collapse the two-step pattern (would break the antibody).
- A1 (cycle_runner emission) is independent of mid-02's seam, but inherits NC-NEW-A discipline transitively (if mid-01 emits CANCEL via venue_command_repo, the post-cancel SDK call still goes through polymarket_client).

## What X1 verdict forbids

- Migration from `signed = create_order(args); post_order(signed)` → `result = create_and_post_order(args)` (would break interception)
- New call sites of `create_order` outside the NC-NEW-A allowlist
- SDK helper-extraction that abstracts the 2-call pattern into a single combined function

## Memory cites

- `feedback_zeus_plan_citations_rot_fast` — line numbers rot; AST shape is durable
- Down R1L1 §X-MD-1 → X1 routing decision
- Mid R3L3 NC-NEW-D + NC-NEW-A allowlist locks

## L4 deferred

- mid-02 implementation packet may capture exact line numbers for human readability, but contract-binding is AST shape, not line numbers.
- Future create_order helper-extraction may move the call BUT must preserve {file=polymarket_client.py, AST=2-step}.

## Closure

X-MD-1 folded into X1; verdict locked; 4 antibodies covering 3 enforcement layers.
