# Side-channel fixes

Small bugs (≤30 LOC, no invariant impact) discovered during debate and fixed by teammate-dispatched executor. Index only — fixes are NOT plan slices.

Format:

```
## YYYY-MM-DD — <commit-sha> — <region-author> — <one-line title>
- file: <path:line>
- discovered_in: round <N> of region <region>
- fix: <one-line>
```

(empty)
