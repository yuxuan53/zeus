# R3 M3 Pre-close Review — 2026-04-27

Phase: M3 — User-channel WS ingest + REST fallback
Status: PRE-CLOSE PASS

## Critic: Pascal the 2nd

Initial verdict: BLOCK

Blockers found:
1. Stale WS summary/assert paths could block submit without materializing `m5_reconcile_required=True`.
2. `MARKET_MISMATCH` was initially scoped so cycle summary could allow unrelated submits.
3. Maker-order trade facts could be appended with a foreign taker `venue_order_id` when Zeus was the maker.

Remediation:
- `src/control/ws_gap_guard.py` now materializes stale states via `record_gap("stale_last_message")` in summary/assert guard paths.
- Any `m5_reconcile_required=True` status now globally blocks new submit until future M5 recovery evidence.
- `src/ingest/polymarket_user_channel.py` now writes the matched command `venue_order_id` for maker-order trade facts.
- Regressions added in `tests/test_user_channel_ingest.py`:
  - `test_stale_guard_path_sets_m5_reconcile_required_without_manual_check`
  - `test_market_subscription_mismatch_blocks_all_new_submit_until_m5`
  - `test_market_subscription_mismatch_stays_global_block_after_later_valid_message`
  - `test_maker_order_trade_fact_uses_matched_zeus_order_id`

Final verdict: APPROVE

Evidence quoted from Pascal the 2nd final review:
- Stale guard materializes `m5_reconcile_required=True`: `src/control/ws_gap_guard.py:113-125`, `200-206`.
- Any `m5_reconcile_required=True` status blocks all submits before affected-market scoping: `src/control/ws_gap_guard.py:64-73`.
- Valid later messages preserve `m5_reconcile_required`: `src/control/ws_gap_guard.py:129-155`.
- Maker-order trade facts use matched Zeus `command["venue_order_id"]`: `src/ingest/polymarket_user_channel.py:366-378`.
- Regressions cover all fixes: `tests/test_user_channel_ingest.py:304-360`.

## Verifier: Descartes the 2nd

Verdict: PASS

Evidence:
- `pytest -q -p no:cacheprovider tests/test_user_channel_ingest.py` -> 15 passed, warnings only.
- `pytest -q -p no:cacheprovider tests/test_user_channel_ingest.py tests/test_digest_profile_matching.py tests/test_executor.py tests/test_executor_command_split.py tests/test_live_execution.py tests/test_executor_db_target.py tests/test_executor_typed_boundary.py` -> 80 passed, 2 skipped, warnings only.
- `python3 scripts/topology_doctor.py closeout ... --receipt-path docs/operations/task_2026-04-26_ultimate_plan/receipt.json ... --json` -> `ok: true`, `blocking_issues: []`.
- Code inspection confirmed stale materialization, global `m5_reconcile_required` blocking, and maker-order `venue_order_id` linkage.

## Nonblocking warnings

- Code Review Graph freshness/coverage warnings remain nonblocking and derived-only.
- Existing context-budget warnings remain for `docs/operations/current_state.md` and `architecture/module_manifest.yaml`.
- No live endpoint smoke was run; live WS activation remains operator-gated and outside M3 closeout.
