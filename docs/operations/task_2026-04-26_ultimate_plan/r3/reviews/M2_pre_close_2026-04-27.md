# M2 pre-close review — 2026-04-27

Phase: M2 `SUBMIT_UNKNOWN_SIDE_EFFECT semantics`
Branch: `plan-pre5`
Status: PASS after critic blocker remediation.

## Critic review — Fermat

Final verdict: **APPROVE**.

Blockers found and remediated before close:
1. Economic-intent duplicate lookup compared exact Decimal spellings, allowing float representation drift (`0.3` vs `0.1 + 0.2`). Fixed by matching M1 idempotency precision (4 decimals) in `find_unknown_command_by_economic_intent()` and adding regression coverage.
2. Generic pre-submit/preflight failures could leave commands in `SUBMITTING` or be misclassified. Fixed in executor entry/exit paths and PolymarketClient wrapper typed rejection handling.
3. Lazy `PolymarketClient` adapter/preflight failures inside `place_limit_order()` were still raising into executor unknown-side-effect handling. Fixed by returning typed `V2_PREFLIGHT_EXCEPTION` rejection before `submit_limit_order()`.
4. `PolymarketV2Adapter.submit_limit_order()` still had pre-POST snapshot/signing/local seams that could raise before SDK post. Fixed by returning typed `V2_PRE_SUBMIT_EXCEPTION` rejection for preflight/client/snapshot/signing/local unsupported-submit failures while intentionally preserving one-step `create_and_post_order()` and two-step `post_order()` exceptions as possible unknown side effects.

Final critic evidence:
- Target pytest: `169 passed, 1 skipped, 1 warning`.
- R3 drift: `GREEN=10 YELLOW=0 RED=0`.
- No `SAFE_REPLAY_PERMITTED`, `RESTING`, `MATCHED`, `MINED`, or `CONFIRMED` enum/event added by M2.

## Verifier review — Curie

Final verdict: **PASS** under the intended receipt-scoped closeout.

Verifier evidence:
- Receipt-scoped planning-lock: ok.
- Receipt-scoped map-maintenance closeout: ok.
- Receipt-scoped topology closeout: `ok: true`, `blocking_issues: []`.
- Nonblocking warnings only: stale/partial Code Review Graph coverage, source downstream drift for `src/state/venue_command_repo.py`, and context-budget warnings for `docs/operations/current_state.md` / `architecture/module_manifest.yaml`.

## Close decision

M2 may be marked `COMPLETE` because both pre-close critic and verifier review passed after remediation. Per project directive, M3 remains frozen until the post-close third-party critic + verifier pass also completes.
