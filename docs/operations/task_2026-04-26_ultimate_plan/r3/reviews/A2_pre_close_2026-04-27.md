# A2 pre-close review — 2026-04-27

Phase: A2 — RiskAllocator + PortfolioGovernor + kill switch
Status: PRE-CLOSE PASS; post-close third-party critic+verifier pending
Timestamp: 2026-04-27T22:42:40Z

## Implementation evidence

```text
python3 scripts/topology_doctor.py --navigation ...: navigation ok True, profile r3 risk allocator governor implementation
python3 -m py_compile src/contracts/execution_intent.py src/risk_allocator/governor.py src/risk_allocator/__init__.py src/execution/executor.py src/data/polymarket_client.py src/engine/cycle_runner.py src/engine/cycle_runtime.py tests/test_risk_allocator.py tests/test_executor.py: PASS
pytest -q -p no:cacheprovider tests/test_risk_allocator.py: 24 passed
pytest -q -p no:cacheprovider tests/test_risk_allocator.py tests/test_executor.py tests/test_heartbeat_supervisor.py tests/test_k2_slice_e.py tests/test_executor_command_split.py tests/test_live_execution.py tests/test_executor_db_target.py: 82 passed, 6 skipped
pytest -q -p no:cacheprovider tests/test_digest_profile_matching.py::test_r3_a2_risk_allocator_routes_to_a2_profile_not_heartbeat: 1 passed
python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase A2: GREEN=12 YELLOW=0 RED=0
python3 scripts/topology_doctor.py closeout ... --summary-only: closeout ok, changed_files=36
```

Closeout warnings were nonblocking repo-wide Code Review Graph / docs / source / tests / scripts global-health findings outside the scoped A2 closeout.

## Critic result

Critic: Euclid the 2nd
Result: APPROVE

Evidence summarized by critic:

- Allocator selection is concrete and behavior-changing: `RiskAllocator.maker_or_taker()` selects `MAKER` / `TAKER` / `NO_TRADE`, `select_global_order_type()` maps maker to `GTC`, taker to `FOK`, and no-trade to `AllocationDenied`, and the public package exports the selector.
- Executor entry and exit both use the selected order type for heartbeat gating, pre-submit envelope persistence, `SUBMIT_REQUESTED`, `client.place_limit_order()`, and `SUBMIT_ACKED` evidence.
- `PolymarketClient.place_limit_order()` accepts `order_type` and forwards it to the V2 adapter; adapter submit uses the envelope order type.
- Regression coverage proves degraded heartbeat selects `FOK/FAK`, lost heartbeat becomes no-trade, healthy/deep book can select maker, shallow-book entry submits/persists `FOK`, degraded-heartbeat exit submits/persists `FOK`, and the client forwards `FOK` to the V2 adapter.
- Original A2 guarantees remain: typed `ExecutionIntent` metadata, production construction/runtime candidate metadata population, cycle-start governor refresh, and read-only/governance-only allocator behavior.
- No CLOB cutover, live activation, production DB mutation, or live venue submit/cancel/redeem authorization was found in A2 surfaces.

## Verifier result

Verifier: Ampere the 2nd
Result: PASS

Evidence summarized by verifier:

- Fresh verification passed: py_compile, focused A2 tests (`24 passed`), combined executor/heartbeat/client suites (`82 passed, 6 skipped`), digest profile (`1 passed`), A2 drift (`GREEN=12 YELLOW=0 RED=0`), and scoped closeout (`closeout ok`, changed_files=36).
- Runtime-behavior proof exists in code: allocator maker/taker selection maps to concrete order types, executor selects before persistence, heartbeat-gates and persists/submits the selected type, and `PolymarketClient` forwards it to the V2 adapter.
- Regression tests cover shallow-book entry `FOK`, degraded-heartbeat exit `FOK`, and client `FOK` forwarding.
- The work record/frozen interface/receipt artifacts exist at the R3 package paths. Verifier noted the work-record header was stale at review time; it was updated after the pass to record pre-close completion.

## Freeze decision

Decision: A2 may be marked complete after this pre-close review. Per the standing R3 loop directive, G1 remains frozen until the required A2 post-close third-party critic + verifier pass completes. No live venue submit/cancel/redeem, production DB mutation, credentialed live-shadow activation, live strategy promotion, CLOB cutover, or live deployment is authorized.
