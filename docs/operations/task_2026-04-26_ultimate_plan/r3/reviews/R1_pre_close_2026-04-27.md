# R3 R1 Pre-close Review — 2026-04-27

Phase: R1 — Settlement / redeem command ledger
Status: PRE-CLOSE PASS; post-close third-party review pending

## Critic: Mencius the 2nd

Verdict: APPROVE

No blocking issues found. Evidence summarized by critic:

- Durable command state machine exists in `src/execution/settlement_commands.py`; schema constrains settlement states and payout assets, mirrored in `src/state/db.py`.
- pUSD request creation fails closed on Q-FX-1 before command insert; unset environment leaves zero command rows.
- pUSD submit fails closed on Q-FX-1 before `adapter.redeem()`; adapter-call tripwire verifies no contact.
- `REDEEM_TX_HASHED` is implemented as the crash-recovery anchor; chain receipt reconciliation moves tx-hashed commands to confirmed or failed.
- No runtime live redeem path was introduced: runtime harvester code creates command intents via `request_redeem()`, and `submit_redeem()` is not invoked by runtime code.
- Harvester integration is idempotent through active-command de-duplication and creates intents only.
- Redeem failure does not settle or otherwise mutate `position_current` lifecycle phase.
- Legacy `USDC_E` remains distinct and routes to `REDEEM_REVIEW_REQUIRED`, not pUSD accounting.
- R1 routing/profile evidence points to `r3 settlement redeem command ledger implementation`, not heartbeat.
- Status surfaces were not prematurely closed before review.

Remediation: none required.

## Verifier: Hume the 2nd

Verdict: PASS

Evidence summarized by verifier:

- Work record and frozen interface correctly showed implementation complete with pre-close review still pending before this artifact.
- `settlement_commands` and `settlement_command_events` durable tables exist, with R1 settlement states documented in code.
- Q-FX-1 gating is enforced before command creation and before adapter contact, with focused regression coverage.
- Tx-hash recovery is implemented and exercised by tests.
- Redeem failure leaves position lifecycle state unchanged.
- Legacy `USDC_E` review classification and payout asset constraints are covered.
- Harvester does not directly redeem via adapter; it records an R1 command intent.
- Digest/profile routing is covered by `tests/test_digest_profile_matching.py::test_r3_r1_settlement_redeem_routes_to_r1_profile_not_heartbeat`.
- Receipt and closeout evidence were consistent, with closeout `ok=true` and no blocking issues.

Verifier rerun evidence:

```text
pytest -q -p no:cacheprovider tests/test_settlement_commands.py tests/test_digest_profile_matching.py::test_r3_r1_settlement_redeem_routes_to_r1_profile_not_heartbeat tests/test_collateral_ledger.py::test_polymarket_client_redeem_fails_closed_before_adapter_when_q_fx_open tests/test_collateral_ledger.py::test_polymarket_client_redeem_defers_to_r1_without_sdk_side_effect tests/test_collateral_ledger.py::test_v2_adapter_redeem_deferred_without_sdk_contact: 10 passed, 2 warnings
python3 scripts/topology_doctor.py closeout ... --json: ok=true, blocking_issues=[]
```

## Existing implementation verification evidence

```text
python3 -m py_compile src/execution/settlement_commands.py src/execution/harvester.py src/state/db.py tests/test_settlement_commands.py tests/test_digest_profile_matching.py: PASS
pytest -q -p no:cacheprovider tests/test_settlement_commands.py tests/test_digest_profile_matching.py::test_r3_r1_settlement_redeem_routes_to_r1_profile_not_heartbeat: 7 passed
pytest -q -p no:cacheprovider tests/test_settlement_commands.py tests/test_collateral_ledger.py tests/test_v2_adapter.py tests/test_exchange_reconcile.py tests/test_digest_profile_matching.py::test_r3_r1_settlement_redeem_routes_to_r1_profile_not_heartbeat: 76 passed, 4 known deprecation warnings
pytest -q -p no:cacheprovider tests/test_settlement_commands.py tests/test_digest_profile_matching.py::test_r3_r1_settlement_redeem_routes_to_r1_profile_not_heartbeat tests/test_collateral_ledger.py tests/test_v2_adapter.py tests/test_exchange_reconcile.py tests/test_venue_command_repo.py tests/test_exit_safety.py tests/test_user_channel_ingest.py: 151 passed, 22 known deprecation warnings
python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase R1: GREEN=7 YELLOW=0 RED=0
python3 scripts/topology_doctor.py --navigation ...: navigation ok True, profile r3 settlement redeem command ledger implementation
```

## Nonblocking warnings / non-goals

- Live redeem side effects were not exercised; live venue submission/cancel/redeem remains unauthorized.
- Production DB mutation and CLOB cutover remain unauthorized.
- Q-FX-1 operator classification remains required for pUSD redemption/accounting; unset gate blocks pUSD request/submit.
- The real adapter still defers redemption to the R1 ledger surface; tests use fake adapters for ledger semantics and chain receipt simulation.
- Code Review Graph/source/context warnings remain nonblocking derived-context warnings from closeout.
- `tests/test_harvester_dr33_live_enablement.py` still carries a pre-existing stale `physical_quantity` expectation unrelated to R1 and is not R1 evidence.

Pre-close conclusion:

- R1 may be marked COMPLETE for pre-close purposes.
- T1 remains frozen until the required additional post-close third-party critic + verifier pass completes.
- Live venue submit/cancel/redeem, production DB mutation, credentialed live activation, and CLOB cutover remain outside authorization.
