# M1 pre-close review record — 2026-04-27

Phase: M1 `Lifecycle grammar + cycle_runner-as-proxy`
Branch: `plan-pre5`
Close status: `COMPLETE_AT_GATE` only; `INV-29 amendment` remains OPEN.

## Pre-close critic/verifier

- Critic: Parfit — PASS for COMPLETE_AT_GATE.
  - Confirmed command grammar remains command-side only; no `RESTING`/U2 fact states in `CommandState`.
  - Confirmed U2 order/trade facts remain separate.
  - Confirmed transitions are grammar-checked, unresolved lookup derives from `IN_FLIGHT_STATES`, and RED proxy emission is scoped to `_execute_force_exit_sweep` without SDK place/cancel side effects.
  - Confirmed RiskGuard does not call `insert_command`.
  - Confirmed full COMPLETE and M2 unfreeze are not acceptable while `INV-29 amendment` remains OPEN.
- Verifier: Singer — PASS for COMPLETE_AT_GATE.
  - Fresh verifier run: `python3 -m pytest -q tests/test_command_grammar_amendment.py tests/test_riskguard_red_durable_cmd.py tests/test_command_bus_types.py tests/test_venue_command_repo.py` -> `106 passed`.
  - Confirmed gate-edge artifact, receipt, command grammar, RED proxy tests, and M2 freeze state.

## Leader remediation after review

Critic noted a non-blocking stale docstring in `src/execution/command_recovery.py` saying SUBMITTING without `venue_order_id` gets `EXPIRED`. The implementation and tests use `REVIEW_REQUIRED`; the docstring now matches the code. Literal escaped unicode markers in nearby recovery comments/docstrings were also normalized without changing runtime behavior.

## Evidence

- `pytest -q -p no:cacheprovider tests/test_command_grammar_amendment.py tests/test_riskguard_red_durable_cmd.py` -> `14 passed`.
- `pytest -q -p no:cacheprovider tests/test_command_bus_types.py tests/test_venue_command_repo.py tests/test_command_recovery.py tests/test_executor_command_split.py` -> `123 passed`.
- Combined M1 focused suite + digest/neg-risk/dual-track antibody -> `142 passed`.
- Executor/U1/Z2 regression suite -> `80 passed, 2 skipped`.
- `pytest -q -p no:cacheprovider tests/test_runtime_guards.py -k force_exit` -> `1 passed, 118 deselected`.
- `python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase M1` -> GREEN.
- `python3 -m py_compile src/execution/command_bus.py src/state/venue_command_repo.py src/execution/command_recovery.py src/engine/cycle_runner.py` -> ok.
- `git diff --check` -> clean.
- Post-review rerun: `pytest -q -p no:cacheprovider tests/test_command_grammar_amendment.py tests/test_riskguard_red_durable_cmd.py tests/test_command_bus_types.py tests/test_venue_command_repo.py` -> `106 passed`.
- Post-review rerun: `python3 -m py_compile src/execution/command_recovery.py`, `r3_drift_check.py --phase M1`, and `git diff --check` -> ok/GREEN/clean.
- Post-review rerun: `topology_doctor` map-maintenance, planning-lock, and closeout -> ok.

## Decision

M1 may be recorded as `COMPLETE_AT_GATE`. It must not be marked full `COMPLETE`, and M2 must not be unfrozen, until `INV-29 amendment` governance commit + planning-lock receipt exists and is cited.
