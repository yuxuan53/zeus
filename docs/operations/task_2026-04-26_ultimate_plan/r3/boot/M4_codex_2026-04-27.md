# Boot — M4

Author: codex
Started: 2026-04-27T18:55:00Z
HEAD: 874e00cc0244135f49708682cab434b4d151d25d
Branch: plan-pre5

## Reads completed

- `AGENTS.md` / scoped `src/execution/AGENTS.md`, `tests/AGENTS.md`, `architecture/AGENTS.md`, `docs/operations/AGENTS.md`
- `r3/R3_README.md`, `IMPLEMENTATION_PROTOCOL.md`, `CONFUSION_CHECKPOINTS.md`, `SELF_LEARNING_PROTOCOL.md`, `SKILLS_MATRIX.md`
- `r3/slice_cards/M4.yaml`, `r3/INVARIANTS_LEDGER.md`, `r3/operator_decisions/INDEX.md`
- R2 cards/evidence: `slice_cards/mid-03.yaml`, `slice_cards/mid-04.yaml`, `evidence/mid/converged_R3L3.md`
- Module references/manifests for execution/state/topology and current M3/M4 status surfaces

## Drift status

- `python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase M4` → GREEN=1 YELLOW=0 RED=0, skipped=10.
- Initial topology navigation misrouted to heartbeat because no M4 profile existed; fixed with `r3 cancel replace exit safety implementation` profile and regression `test_r3_m4_cancel_replace_routes_to_m4_profile_not_heartbeat`.

## Working hypothesis

M4 should not add command grammar. The safe implementation is a DB-only `exit_safety` surface that owns per-position/token mutexes, typed cancel outcome parsing, and replacement-sell gating, then integrates at the existing real actuation seams: `execute_exit_order`, `exit_lifecycle` stale-cancel retry, and `venue_command_repo` terminal release. `CANCEL_UNKNOWN` is represented as a typed semantic outcome plus `CANCEL_REPLACE_BLOCKED` payload requiring M5, not as a new `CommandState` or `CommandEventType`.

## Operator gates blocking

- None specific to M4. Live venue submission/CLOB cutover gates remain closed; tests use fakes/monkeypatches only.

## Cross-phase touchpoints

- Consumes: Z4 `CollateralLedger` CTF sell preflight/reservation; U2 order facts; M1 command grammar; M2 unknown-side-effect semantics; M3 submit gap guard.
- Produces: `src/execution/exit_safety.py` surface and `exit_mutex_holdings` schema for M5/T1/G1 consumers.
