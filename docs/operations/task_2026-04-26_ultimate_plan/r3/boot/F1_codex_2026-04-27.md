# F1 boot note — Forecast source registry

Date: 2026-04-27
Agent: codex
Branch: plan-pre5
Phase: F1 — Forecast source registry

## Goal

Wire forecast-source plumbing so Zeus can identify, gate, and provenance-stamp forecast sources before later F2/F3 work adds calibration retrain controls or TIGGE fetch behavior.

## Scope in

- Add a K2 data-layer forecast ingest protocol and source registry.
- Keep existing forecast table sources enabled by default.
- Make experimental TIGGE source dormant behind both operator artifact and env flag.
- Add additive legacy-safe `forecasts` columns: `source_id`, `raw_payload_hash`, `captured_at`, `authority_tier`.
- Update live forecast appender and live ensemble client to stamp registry provenance.
- Keep coverage scanner source list aligned with the registry.

## Scope out / stop conditions

- No calibration retrain trigger or Platt refit (F2).
- No TIGGE network/GRIB fetch implementation (F3).
- No settlement-source routing change.
- No live venue/order side effects.
- No EnsembleSignal Monte Carlo/math changes.

## Semantic boot answers

- Current source/data surfaces were read: `current_source_validity.md` and `current_data_state.md` are current enough for planning (audited 2026-04-21/2026-04-23, max staleness 14 days).
- F1 registry is forecast-source plumbing only; it does not prove settlement, Day0, or hourly truth.
- Existing Open-Meteo forecast-history sources remain forecast/training rows, not settlement-adjacent truth.
- Experimental TIGGE remains inert until operator evidence + `ZEUS_TIGGE_INGEST_ENABLED=1` are both present.

## Routing evidence

`topology_doctor --navigation` for the F1 task now resolves to profile `r3 forecast source registry implementation` and admits the F1 source/schema/test/doc files after registry maintenance.
