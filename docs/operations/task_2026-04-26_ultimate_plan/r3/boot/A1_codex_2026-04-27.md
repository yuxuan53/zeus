# A1 boot — StrategyBenchmarkSuite

Date: 2026-04-27
Phase: A1 — StrategyBenchmarkSuite + promotion gate
Author: codex
Status: IN_PROGRESS

## Topology / semantic boot

```text
python3 scripts/topology_doctor.py --navigation --task "R3 A1 StrategyBenchmarkSuite alpha execution metrics replay paper live shadow promotion gate strategy_benchmark_runs INV-NEW-Q" --files ...
initial result: profile misrouted to heartbeat and A1 files unclassified
remediation: added `r3 strategy benchmark suite implementation` topology profile + digest regression
rerun result: navigation ok True, profile r3 strategy benchmark suite implementation
python3 scripts/topology_doctor.py --task-boot-profiles: topology check ok
```

## Proof questions

1. Current law: no live strategy promotion without replay + paper + shadow benchmark evidence (`INV-NEW-Q`).
2. Active now: A1 benchmark/promotion-gate implementation only; A2/G1 remain dependency-gated.
3. Derived context: R3 plan/slice card/topology outputs guide scope but do not authorize live side effects.
4. History location: R3 work records/reviews under this packet; invariant ledger stays PENDING until CI/merge.

## Boundaries

- No live venue submit/cancel/redeem.
- No credentialed live-shadow activation.
- No production DB/state artifact mutation.
- No CLOB cutover or live strategy promotion.
- Candidate modules are non-executable stubs only.
