# M1 Boot — Codex — 2026-04-27

Read set: root `AGENTS.md`, scoped `src/engine/AGENTS.md`, `src/execution/AGENTS.md`, `src/state/AGENTS.md`, `architecture/self_check/zero_context_entry.md`, `architecture/self_check/authority_index.md`, `architecture/kernel_manifest.yaml`, `architecture/invariants.yaml`, `architecture/zones.yaml`, `architecture/negative_constraints.yaml`, `docs/authority/zeus_current_architecture.md`, `docs/authority/zeus_current_delivery.md`, `docs/operations/task_2026-04-26_ultimate_plan/r3/slice_cards/M1.yaml`, `SKILLS_MATRIX.md`, `CONFUSION_CHECKPOINTS.md`, `IMPLEMENTATION_PROTOCOL.md`, `operator_decisions/INDEX.md`, and targeted module books for engine/execution/state.

Topology: initial M1 navigation misrouted to the Z3 heartbeat profile because broad R3 docs + `cycle_runner.py` path evidence dominated. Added `r3 lifecycle grammar implementation` profile plus digest regression `test_r3_m1_lifecycle_grammar_routes_to_m1_profile_not_heartbeat`; navigation now resolves to M1.

Drift/gate status: M1 is unblocked by U2 post-close review, but `operator_decisions/INDEX.md` still marks `INV-29 amendment` as OPEN. Engineering may proceed to the edge of the gate, but M1 must not be represented as mergeable without the governance amendment commit + planning-lock receipt cite.

Proof answers:
- Authoritative truth surface: `CommandState`/`CommandEventType` in `src/execution/command_bus.py`, transition enforcement in `src/state/venue_command_repo.py`, RED actuation sequencing in `src/engine/cycle_runner.py`, and current law in `architecture/invariants.yaml` + `docs/authority/zeus_current_architecture.md`.
- Zone: K2 runtime across execution/state/engine; planning-lock applies because this is cross-zone runtime/command grammar work.
- Invariants in scope: INV-05, INV-19, INV-29, INV-30, INV-31, INV-32, NC-18, NC-19, NC-NEW-D, NC-NEW-E.
- Forbidden: no `RESTING` CommandState; no order/trade/finality collapse from U2 into command grammar; RiskGuard must not call `insert_command`; no M2 unknown-side-effect runtime resolution; no M3 websocket; no M5 exchange sweep; no live venue side effects.
- Evidence required: M1 grammar tests, RED durable command antibodies, existing command bus/repo/recovery regressions, executor split regressions, digest/profile route test, neg-risk passthrough, drift check, map-maintenance, planning-lock, closeout receipt, critic+verifier. If operator gate remains OPEN, close as `COMPLETE_AT_GATE` with gate-edge artifact rather than COMPLETE.

Started at: 2026-04-27T09:47:01Z
