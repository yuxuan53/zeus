# M2 semantic boot — 2026-04-27

Phase: M2 `SUBMIT_UNKNOWN_SIDE_EFFECT semantics`
Branch: `plan-pre5`

## Topology / boot evidence

- `python3 scripts/topology_doctor.py --task-boot-profiles` -> ok.
- Initial navigation before profile registration misrouted M2 into `r3 heartbeat supervisor implementation`; implementation was not started until a narrow M2 topology profile and digest regression were added.
- `python3 scripts/topology_doctor.py --navigation --task "R3 M2 SUBMIT_UNKNOWN_SIDE_EFFECT unknown-side-effect semantics unknown_side_effect never rejected post-POST timeout idempotency economic-intent fingerprint safe replay via SUBMIT_REJECTED payload" ...` -> profile `r3 unknown side effect implementation`, navigation ok.
- Scoped routers read: root AGENTS, `src/execution/AGENTS.md`, `src/state/AGENTS.md`, `src/contracts/AGENTS.md`, `src/data/AGENTS.md`, `src/venue/AGENTS.md`, high-risk overlay `architecture/self_check/zero_context_entry.md`, and `architecture/self_check/authority_index.md`.
- Authority spine read: `architecture/kernel_manifest.yaml`, `architecture/invariants.yaml`, `architecture/zones.yaml`, `architecture/negative_constraints.yaml`, `docs/authority/zeus_current_architecture.md`, `docs/authority/zeus_current_delivery.md` excerpts for command/lifecycle/runtime truth.
- Target module books read/updated: `docs/reference/modules/execution.md`, `docs/reference/modules/state.md`; `docs/reference/modules/data.md` read for the `PolymarketClient` compatibility seam; `docs/reference/modules/venue.md` read/updated for the V2 adapter pre/post side-effect boundary.
- Sidecar architect Hume reviewed the M2 design; key constraint accepted: do **not** add a new `SAFE_REPLAY_PERMITTED` enum/event without a new INV-29 amendment. Safe replay is therefore encoded as a terminal `SUBMIT_REJECTED` event payload.

## Proof questions answered

1. What is authoritative here?
   - The canonical trades DB `venue_commands` / `venue_command_events` journal written through `src/state/venue_command_repo.py`, plus `CommandState`/`CommandEventType` grammar from `src/execution/command_bus.py`. Derived docs/artifacts are evidence only.
2. Which zone is touched?
   - K2 live execution (`src/execution/executor.py`, `src/execution/command_recovery.py`) and K1 state journal seam (`src/state/venue_command_repo.py`). No lifecycle phase grammar or production DB artifact is touched.
3. Which invariants are in scope?
   - INV-28/30/31/32, INV-29 amendment surface, NC-19, and R3 INV-NEW-G: post-POST network/timeout exceptions must create `SUBMIT_UNKNOWN_SIDE_EFFECT`, never semantic rejection.
4. What files are forbidden?
   - Production DB artifacts, authority docs, graph caches, parallel worktrees, OMX runtime state, RESTING/MATCHED/MINED/CONFIRMED `CommandState`, M3/M4/M5 runtime surfaces, production DB mutation, and live venue submission/cutover.
5. Why no `SAFE_REPLAY_PERMITTED` event?
   - INV-29 currently asserts the exact incorporated command event set. Adding a new event would require a separate planning-lock amendment; M2 can record safe replay permission in the `SUBMIT_REJECTED` payload after recovery proof instead.
6. How is cautious improvisation bounded?
   - M2 adds economic-intent duplicate blocking and venue_order_id persistence because they are necessary to make the unknown-side-effect invariant effective, but it does not invent exchange sweeps, websocket ingest, live cutover, or new command/order/trade finality states.
