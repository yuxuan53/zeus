# U1 boot — codex — 2026-04-27

Read set: root `AGENTS.md`, `R3_README.md`, `IMPLEMENTATION_PROTOCOL.md`,
`CONFUSION_CHECKPOINTS.md`, `SELF_LEARNING_PROTOCOL.md`,
`INVARIANTS_LEDGER.md`, `slice_cards/U1.yaml`, scoped AGENTS for
`src/contracts`, `src/state`, `src/execution`, and `tests`. Topology
navigation is green under the `r3 executable market snapshot v2 implementation`
profile after registering the new contract/repo/test surfaces.

Drift check: `python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase U1`
returned `GREEN=14 YELLOW=0 RED=0` and refreshed the 2026-04-27 drift report.

Working hypothesis: U1's job is not live cutover and not U2 raw provenance.
It creates immutable CLOB executable-market evidence and makes
`venue_command_repo.insert_command()` the single Python freshness/tradability
gate. Every new venue command must cite a fresh `snapshot_id` before command
persistence, collateral reservation, preflight, signing, or SDK submit.

Resolved edge cases:
- Existing command-journal tests must seed fresh executable snapshots; command
  grammar tests are not exempt from INV-NEW-E.
- Executor entry and exit intents carry executable snapshot citation and the
  CLOB facts the command gate must compare (`min_tick_size`, `min_order_size`,
  `neg_risk`).
- Compatibility entry/exit paths that lack a snapshot now fail closed before
  SDK contact; idempotency pre-lookup remains safe because it only returns an
  existing command and performs no venue side effect.
- Legacy DBs get a nullable `snapshot_id` ALTER path, while fresh U1 DBs make
  the column `NOT NULL`; new inserts are Python-enforced in both cases.

Operator gates: Q1/cutover and Q-FX-1 remain open. U1 does not authorize live
placement, redemption, cancel/replace, exchange reconciliation, or raw U2
projection tables.

Pre-close evidence target: U1 snapshot tests, command journal suite, focused
executor/collateral suite, `py_compile`, U1 drift GREEN, topology navigation,
map-maintenance, planning-lock, pre-close critic+verifier, then post-close
third-party critic+verifier before U2 unfreezes.
