# R3 R1 boot — settlement / redeem command ledger

Date: 2026-04-27
Agent: codex
Status: implementation boot complete; pre-close review pending

## Topology / semantic boot

- Initial R1 navigation misrouted to `r3 heartbeat supervisor implementation`, so R1 first added a dedicated topology profile and digest regression before business logic.
- `python3 scripts/topology_doctor.py --navigation --task "R3 R1 Settlement / redeem command ledger settlement_commands REDEEM_TX_HASHED crash-recoverable redemption Q-FX-1 FXClassificationPending" --files ...` -> navigation ok, profile `r3 settlement redeem command ledger implementation`.
- `python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase R1` -> `GREEN=7 YELLOW=0 RED=0`.

## Proof answers

- Settlement value/source semantics are not changed by R1. R1 models redemption command durability after settlement and does not alter `SettlementSemantics`, winning-bin calculation, or HIGH/LOW identity.
- Q-FX-1 remains open unless `ZEUS_PUSD_FX_CLASSIFIED` carries a valid `FXClassification`; pUSD redeem request/submit fails closed before adapter contact while the gate is unset.
- Legacy USDC.e payout is not silently treated as pUSD; R1 records it as `REDEEM_REVIEW_REQUIRED` for operator classification.
- `REDEEM_TX_HASHED` is the crash-recovery anchor; receipt reconciliation follows tx hash to confirmed/failed without relying on in-memory submit state.
- Redeem failure/review states do not mark positions settled. Settlement terminalization remains owned by canonical settlement paths, not the redeem command ledger.

## Implementation boundary

Allowed:
- Add `src/execution/settlement_commands.py` durable ledger.
- Add `settlement_commands` / `settlement_command_events` schema to `src/state/db.py`.
- Have harvester create a redeem intent for winning pUSD claims when Q-FX-1 permits it.

Forbidden / preserved:
- No live redeem submission in tests or default startup.
- No production DB mutation.
- No lifecycle grammar changes.
- No direct adapter redeem call from harvester.
- No collapse of legacy USDC.e and pUSD accounting.
