# Boot — M5

Author: codex
Started: 2026-04-27T19:24:14Z
HEAD: 874e00cc0244135f49708682cab434b4d151d25d
Branch: plan-pre5

## Reads completed

- `AGENTS.md` / scoped `src/execution/AGENTS.md`, `src/state/AGENTS.md`, `src/venue/AGENTS.md`, `tests/AGENTS.md`, `architecture/AGENTS.md`, `docs/operations/AGENTS.md`
- `r3/R3_README.md`, `IMPLEMENTATION_PROTOCOL.md`, `CONFUSION_CHECKPOINTS.md`, `SELF_LEARNING_PROTOCOL.md`, `SKILLS_MATRIX.md`
- `r3/slice_cards/M5.yaml`, `r3/INVARIANTS_LEDGER.md`, `r3/operator_decisions/INDEX.md`
- Module references/manifests for execution/state/venue/control/topology and current M4/M5 status surfaces

## Drift status

- `python3 docs/operations/task_2026-04-26_ultimate_plan/r3/scripts/r3_drift_check.py --phase M5` → GREEN=10 YELLOW=0 RED=0.
- Initial topology navigation misrouted to heartbeat because no M5 profile existed; fixed with `r3 exchange reconciliation sweep implementation` profile and regression `test_r3_m5_exchange_reconcile_routes_to_m5_profile_not_heartbeat`.

## Working hypothesis

M5 is not a live actuator. The safe implementation is a read-only venue enumeration sweep (`get_open_orders`, optional `get_trades`, optional `get_positions`) that writes durable `exchange_reconcile_findings` and only appends linkable missing trade facts where a local command foreign key already exists. Exchange-only orders/trades become findings, never `venue_commands` rows.

## Operator gates blocking

- No operator gate blocks engineering M5.
- Live venue submission/cancel/redeem, CLOB cutover, production DB mutation, and R1 settlement/redeem side effects remain closed.
- Findings resolution policy is nonblocking for engineering; M5 includes an operator resolution queue API without silently resolving findings.

## Cross-phase touchpoints

- Consumes: Z1 cutover context, Z3 heartbeat-loss context, U2 order/trade facts, M2 unknown-side-effect command states, M3 gap marker, M4 CANCEL_UNKNOWN/proven-absence owner contract.
- Produces: `exchange_reconcile_findings` schema/API and `run_reconcile_sweep()` for future T1/G1 readiness and M4 proven-absence workflows.
