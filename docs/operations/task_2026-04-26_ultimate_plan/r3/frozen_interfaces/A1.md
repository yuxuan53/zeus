# Frozen Interface — A1 StrategyBenchmarkSuite

Phase: A1 — StrategyBenchmarkSuite + promotion gate
Status: FROZEN AFTER POST-CLOSE PASS; A2 unfrozen for phase entry
Date: 2026-04-27

## Public surface

`src/strategy/benchmark_suite.py` exports:

- `BenchmarkEnvironment`
- `PromotionVerdict`
- `SemanticDriftFinding`
- `BenchmarkObservation`
- `ReplayCorpus`
- `StrategyMetrics`
- `PromotionThresholds`
- `PromotionDecision`
- `StrategyBenchmarkSuite`
- `STRATEGY_BENCHMARK_RUNS_DDL`
- `metrics_to_jsonable()`

`src/strategy/data_lake.py` exports:

- `MarketFilter`
- `DataLake`

`src/strategy/candidates/` exports non-executable candidate stubs named in the A1 slice card.

## Candidate guarantees

- `promotion_decision()` returns `PROMOTE` only when replay, paper, and shadow metrics all pass thresholds and have no unwaived semantic drift.
- `evaluate_live_shadow()` is read-only and consumes preloaded shadow corpora; it does not contact a venue or activate credentials.
- `record_benchmark_run()` writes only to a caller-supplied SQLite connection.
- Candidate stubs do not implement alpha and do not authorize live trading.

## Non-goals

- No live strategy promotion.
- No live venue submit/cancel/redeem.
- No production DB/state artifact mutation.
- No CLOB cutover or credentialed live-shadow activation.
- No A2 allocator/governor logic.

## Review state

- Pre-close critic Ohm the 2nd: APPROVE.
- Pre-close verifier Harvey the 2nd: PASS.
- Pre-close artifact: `docs/operations/task_2026-04-26_ultimate_plan/r3/reviews/A1_pre_close_2026-04-27.md`.
- Post-close critic Carson the 2nd APPROVE and verifier Maxwell the 2nd PASS; A2 unfrozen for phase entry while G1 remains blocked by A2/live-readiness gates.
