# Topology Context Efficiency Task

This folder holds active evidence for the topology context-budget and packet
prefill work. It is operational evidence, not authority.

## File registry

| File | Purpose |
|------|---------|
| `plan.md` | Active plan/evidence for navigation-tier, packet-prefill, and planning-lock usability improvements |
| `work_log.md` | Concise factual record of changes, verification, and next/archive routing |

## Closeout

When the task closes, extract durable lessons to `architecture/history_lore.yaml`
if needed, update map/registry surfaces, then archive this folder under
`docs/archives/work_packets/branches/<branch>/<program_domain>/YYYY-MM-DD_slug/`.
