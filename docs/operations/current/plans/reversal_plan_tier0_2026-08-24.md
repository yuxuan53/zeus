# Reversal Plan — Tier-0 research mode + measurement repair (2026-08-24)

Goal: durable positive capital growth via evidence-gated amplification. Derived from the 2026-08-24 full-book investigation (evidence: session scratchpad `investigation_evidence.md`) collided with a GPT-5.6 Pro consult (REQ-20260824-034739-ed68c1, verdict verified). Operator directive: implement completely.

## Standing verdicts (evidence, not opinion)

- Cardinal q is disqualified from sizing: paired log-loss loses to decision-time price in every month and every |q−p| bucket; deficit grows with disagreement (+0.033→+0.518). Confidence 0.96.
- Ordinal selection value UNRESOLVED: cheap-band (<0.25) frequency edge +1.9pp, clustered t≈0.73, CI [−3.2,+7.0]pp. P(real) ≈ 0.35.
- Rich resting makers toxic (−10.3pp, n=51) but explain only ~22% of rich deficit; rich book disabled entirely in Tier 0.
- Measurement organ defective: grader Bug A (multi-tranche purity gate) blinds 56% of Aug non-randomly; fills table double-counts (0x placeholder + UUID); skill categories are circular (own-q yardstick).
- Runtime absence 51% of Aug: pause 117h / riskguard-DATA_DEGRADED 98h / auction-collapse 107h (3 mechanisms: preflight staleness storm, bootstrap Event starvation, preemption churn) / quiet 58h.
- Aug 1-3 −$642: kelly_multiplier 0.25 in untracked settings.json + dampeners deleted 7/24 + peak bankroll. q unchanged. Fixed to 1/32 on 8/2 (now 1/8 + boot guard).
- Aug 8+ cheap-contrarian tilt: admission allowlist deleted + rerank-past-unqualified-winner (both 8/07), deployed with 245 commits on 8/8.
- 84% of convex early exits overrode the position's own belief-consuming exit law via GLOBAL_CAPITAL_OPTIMAL_SELL (capital-velocity objective, structurally anti-convexity).

## Status ledger

| # | Item | Status |
|---|---|---|
| 1 | Entry pause (runtime) | DONE 2026-08-24T09:22:49Z — `control_plane:global:entries_paused` reason `reversal_plan_2026-08-24_entry_pause_until_tier0_release`, no effective_until. Zero resting entry orders confirmed. |
| 1b | Sizing-governance lock | DONE b0c342208 — config/risk_policy.yaml (content-addressed, policy_version+sha256 logged at boot) + assert_risk_policy_artifact as Guard 5 in _run_boot_guards; 4 levers pinned (kelly_multiplier 0.125, max_correlated_pct 0.25, max_portfolio_heat_pct 0.5, max_single_position_pct 0.1), 3 deliberately-not-pinned documented; direction law (lower-only overrides) enforced structurally; 36/36 tests. |
| 2 | Bug A repair (multi-tranche cert aggregation) | DONE 558229342 — 74 tests pass; live shadow validation: 111/174 Aug UNATTRIBUTABLE now resolve (all size-weighted, 0 fallback), remaining 63 = all-tranche failures (30 single-tranche Bug-B cohort + 33 shared-upstream), fail-closed intact. Fill-dedup handled in panels (P3/P4). |
| 3 | Decision certificate: add decision-time executable side price p₀ + candidate-set provenance | TODO — scouting done: certs already carry q_live/q_source/global_limit_price/global_jit_book_snapshot_id; gap = explicit top-of-book side price + candidate set beyond sha256 |
| 4 | Four non-circular panels (forecast/selection/execution/lifecycle) | DONE f77ec1c91 — scripts/scoreboard_panels.py, 19/19 tests, registered (script_manifest.yaml + AGENTS.md + writer-lock allowlist); live smoke reproduces P1 verdict (market beats q all buckets) |
| 5 | Pipeline repairs | 5a bootstrap stall alert DONE d1aeeeb52. 5b riskguard stuck-alert DONE a7da84b26 (+ test rot repair 583cdb52d). 5b Type-C preemption churn DONE (source inside 9df4569f0 via worktree sweep — md5-verified; tests 20080e298, 15/15): bounded revalidation N=3/300s env-overridable, day0/fill wakes hard-veto (never coalesced), generation = adapter-factory lifetime (DRAIN automatic), Type-A five gates untouched with structural non-regression test. Design doc 24756dda1. Open: INV-47 registry entry for the new gate site (governance decision); Type-A first slice (CAS-fence ×205 orthogonal to Tier-0) deferred — Tier-0 taker-only already disables gates 2+5-depth. |
| 6 | Tier-0 live research mode | DONE 0653ed8bb + 9df4569f0 — admission gate (price<0.25, taker-only typed rejection, one-per-(city,target_date) cluster incl. same-cycle race closure), flat stake = venue min_order_size shares at the single sizing chokepoint, aggregate 2% open-cost ceiling live, drawdown-kill live-wired (seed-once start-equity in control_overrides_history `tier0:start_equity:epoch:<N>`, epoch bump = new episode via reviewed commit; realized P&L via canonical R0-a reducer, never forbidden columns; breach → sanctioned auto-pause). Flag `feature_flags.TIER0_RESEARCH_MODE` default OFF. 80 tests across the two slices. **Activation gate: Item 3 (true p0 certs) + release checklist below.** |
| 7pre | Selection-lift preregistration | DONE — tier0_selection_lift_preregistration_2026-08-24.md FROZEN before first Tier-0 settlement: within-city-date price-matched contrast, permutation inference, city-date + date-block clustering (larger governs), single evaluation at 100 clusters, decision rule incl. selector-retirement branch. |
| 7 | Ordinal-selection discriminator (prospective; historical candidate sets not persisted — hash only) | TODO (depends 3) |
| 8 | Convex hold-to-settle | PARTIAL 274054532 — auction SELL candidacy blocked for positions with avg entry price <0.25 (blocks GLOBAL_CAPITAL_OPTIMAL_SELL AND auction-routed q-scored sells; hard-fact zero-support direct sells, RED force, emergencies untouched; avg_price via EffectiveExposure fill/chain authority, lawful). Remaining: scale-in deletion for Tier 0 (moot while entries paused; enforce at Tier-0 release), historical exit audit (done in investigation, exit-review agent). |
| 9 | Market-anchored calibrator | DONE 5f969c590 — module + walk-forward harness + report script, 21/21 tests, all registries. **LIVE RESULT (537 walk-forward predictions, p0-proxy caveat): β converges ~0.10-0.12 (λ=10 heavy shrinkage). Paired log-loss ALL: p0 0.5481, raw q 0.8988, calibrated r̂ 0.5580 — even optimally-shrunk q does NOT beat market price out of sample (marginally worse overall; better only in June and the <0.15 agreement bucket). Gate A verdict as of today: q_cal ≈ parity-at-best → per the two-gate law this licenses NOTHING for capital; live posture stays market-anchored, q has no cardinal authority. Raw q's 0.90 vs 0.55 confirms the original indictment.** |
| 10 | Two-gate promotion evaluator | DONE c6d520be4 — promotion_gates module + CLI + anti-peeking ledger + tier1 formula (pure, unwired), 30/30 tests, registered. **LIVE VERDICT TODAY: Gate A = FAIL_CATASTROPHIC_DEGRADATION (3 of 4 |q−p| buckets breach the 0.05 upper-bound margin; pooled upper 0.044 > δ_A 0.01; date-clustering governs, se 0.021) — q_cal licenses NOTHING; Gate B = no tier0 sample yet. The machinery produces honest refusals, as designed.** |
| 11 | Storage retention (decision_log) | DONE — scripts/migrations/202608_decision_log_retention.py (dry-run default, `--apply` required, chunked DELETE under db_writer_lock(BULK)). Addresses the 2026-08-24/25 RiskGuard storage_capacity DATA_DEGRADED incident (decision_log 73GB/565,879 rows, no prior retention, largest of the three oversized trade-DB tables). Deletes rows older than `--keep-days` (default 30, floor 7); rows anchoring the tier0 preregistered selection-lift study (mode='global_single_order_auction' joined to tier0_candidate_set_provenance via selection_epoch_identity) are protected indefinitely. Consumer-window audit (module docstring) found one unbounded consumer (settlement_skill_attribution's only_new=True backfill); live evidence 2026-08-25 shows 0 currently-at-risk rows (all 27 stale-unattributed positions already fail upstream of decision_log) but this is a live invariant, not a structural guarantee — re-verify before lowering `--keep-days`. **IMPORTANT: DELETE alone does not relieve the storage gate** — auto_vacuum=0 on the live trade DB, so freed pages stay in SQLite's internal freelist, not the OS; the file (~198GB, ~80GB free on volume) needs a `VACUUM INTO` on an external-capacity path (or repeated retention + the other two oversized tables' retention, out of scope here) before disk usage actually drops. 5/5 tests (tests/test_decision_log_retention_migration.py), registered (script_manifest.yaml + db_writer_lock.py allowlist). Remaining: executable_market_snapshots (48GB) and execution_feasibility_evidence (25GB) retention slices, the external-volume VACUUM INTO execution itself, and a daily recurring-job wiring decision (operator enables after the first manual run). |
| 12 | Storage retention (executable_market_snapshots + execution_feasibility_evidence) | DONE — scripts/migrations/202608_executable_market_snapshots_retention.py + scripts/migrations/202608_execution_feasibility_evidence_retention.py, slice 2 of the item-11 pair. executable_market_snapshots (48GB, 11,532,310 rows, 2026-05-15..present) is APPEND-ONLY by DB trigger (NC-NEW-B); each delete chunk drops/verifies-changes()/re-creates the trigger inside one BEGIN IMMEDIATE (precedent: scripts/repair_executable_snapshot_corruption.py), and only rows NOT referenced by venue_commands.snapshot_id or position_events.snapshot_id (INV-NEW-E anchor) are deleted — live evidence 2026-08-25: the anchor set is <0.05% of the table (3337 + 1642 distinct referenced snapshot_ids). execution_feasibility_evidence (25GB, 28,277,164 rows, 2026-06-19..present) has no trigger and no external FK reference into it — plain time-window DELETE. Both add a one-time prerequisite plain index on their cutoff column (neither table's existing indexes lead with the time column alone, which would otherwise degrade chunked scans to near-full-table at this row count) — building these needs modest temporary disk on the first `--apply` run. Consumer-window audit (both scripts' module docstrings) covers ~117 call sites across both tables; the longest confirmed bounded consumer is qkernel_arm_replay.py's `now - 16 days` rolling window, setting executable_market_snapshots' `--keep-days` floor at 17 (execution_feasibility_evidence keeps the item-11 floor of 7, no comparably long consumer found). Companion launchd plists (com.zeus.executable-market-snapshots-retention.plist, com.zeus.execution-feasibility-evidence-retention.plist), same inert-until-installed pattern as item 11. 11/11 tests (tests/test_executable_market_snapshots_retention_migration.py including an append-only-trigger-intact-after-apply check, tests/test_execution_feasibility_evidence_retention_migration.py), registered (script_manifest.yaml + db_writer_lock.py allowlist + test_topology.yaml). Same VACUUM caveat as item 11: DELETE alone does not shrink the file (auto_vacuum=0) — still needs the external-volume `VACUUM INTO` or staged-retention path. |
| 13 | Bounded-by-construction storage (steady state, supersedes items 11-12's periodic-cleanup framing) | Operator redirect 2026-08-25: storage must be bounded by construction, not periodic cleanup. Three slices: **Slice A (content-hash short-circuit for delta receipts) SKIPPED** — measured live (300 consecutive global_single_order_auction_delta/_duplicate row pairs): 0% content-identical after stripping timestamps/self-referential hash fields; the existing field-level dedup (`mode='global_single_order_auction_duplicate'`) fired 0 times in 1.5+ days of live data (0 of 8,769 global-auction rows). Root cause: probability_manifest and candidate_evaluations_sha256 genuinely change almost every cycle because the system continuously re-evaluates "current-evidence" posteriors (AGENTS.md §0 time-decay law) — not redundant duplication. Below the 50% threshold, per instruction: skip. **Slice B (inline ring-buffer expiry) DONE** — `_inline_expire_decision_log` (src/state/decision_chain.py, piggybacked in store_artifact/store_settlement_records; per-mode windows: 30d full auctions, 7d everything else, tier0-anchor exempt, shared `TIER0_EXCEPT_CLAUSE` now imported by scripts/migrations/202608_decision_log_retention.py rather than duplicated), `_inline_expire_executable_market_snapshots` (src/state/snapshot_repo.py, piggybacked in insert_snapshot; rowid-modulo throttled ~1-in-500 firings since the table is APPEND-ONLY by trigger NC-NEW-B — each firing drops/verifies-changes()/re-creates `no_delete_executable_market_snapshots` inside one SAVEPOINT, safe to nest inside the caller's already-open implicit transaction; venue_commands/position_events-anchored rows protected), `_inline_expire_execution_feasibility_evidence` (src/events/triggers/market_channel_ingestor.py, piggybacked in insert_execution_feasibility_evidence_batch; no trigger, no external FK anchor). All three explicitly exclude the row(s) just inserted in the SAME call from their own delete candidates (caught via existing-test regression: a naive age-only predicate would delete a legitimately old-timestamped write, e.g. a backfill/catch-up insert, on the very insert that created it — confirmed via 3 pre-existing test suites, 117+20+1055 tests, with 18 unrelated pre-existing baseline failures spot-checked and reproduced against a clean pre-edit checkout of this branch). Also wires a no-op-today `PRAGMA incremental_vacuum(1000)` into all three write funnels, activating automatically once Slice C's one-time reset (if ever run) converts the DB to auto_vacuum=INCREMENTAL — zero additional wiring needed later. 22/22 new tests (tests/test_decision_log_inline_expire.py, tests/test_executable_market_snapshots_inline_expire.py, tests/test_execution_feasibility_evidence_inline_expire.py), registered (test_topology.yaml, source_rationale.yaml). Items 11-12's periodic migration scripts + launchd plists remain as one-time backlog-drain tools for rows written before this inline mechanism existed; their plists are optional in steady state. **Slice C (one-time VACUUM reset) DOCUMENTED, NOT RUN** — see scripts/ops/vacuum_reset_trades_db.py. |

| 14 | Tier-0 gate price wiring defect (found 2026-08-25 post-activation) | DONE PR #515 (merged d90d3cc6d) — the admission gate read `candidate.limit_price`, a field GlobalSingleOrderCandidate never had; getattr returned None for EVERY candidate → float(None) → typed rejection `TIER0_MAX_ENTRY_PRICE_EXCEEDED:inputs=missing` unconditionally. **Tier-0 could not have admitted a single entry since activation regardless of market conditions.** Fix: `tier0_decision_price()` in tier0_policy.py derives the price from `economic_cost_curve.levels[0].price` (== executable curve for TAKER_LIMIT); the wiring-test fixture carried the same phantom field (why 8 wiring tests were green through the defect) and is realigned to the real candidate surface + a trap test pins "never reads limit_price". Live receipt evidence: 2026-08-25T20:28Z auction, 19 BUY/NO rejected inputs=missing with executable asks on book. NEEDS DAEMON RESTART to take effect (in-process code). |
| 15 | Open-Meteo quota starvation of the live probability path (chronic since ~08-10) | PR #516 (open) — three structural leaks: (a) BPF live single-runs downloads ran on the maintenance lane (cap 8500) alongside backfill instead of the source-clock tranche (cap 9000) — helper existed, never applied at the 2 hot call sites; (b) AnchorAvailabilityProbe paid the metered single-runs probe FIRST per call (1 quota unit per pre-publication 400, ~1150 attempts/day) with the free signals (cached meta, S3 bucket manifest) consulted only after — reordered free-first; (c) per-city ladder re-asked a run-scoped 400 for each of ~146 cities per pass — one shared refusal set short-circuits siblings after the first 400. Effect chain when starved: openmeteo_ecmwf_ifs_9km anchor leg (SOLE replacement-chain leg post-AIFS) fetch-fails for all non-rung-3-whitelisted cities → LEG ARTIFACT MISSING (9339/day observed) → REPLACEMENT_RAW_INPUT_HWM staleness → 20/27 families probability-ineligible every afternoon → auction has nothing admissible to trade. Quota evidence: day_count 8968/8500 maintenance cap at 21:00Z; exhaustion logged daily since 08-10 (worst 08-21: 10,254 block lines). |
| 16 | Tier-0 flat-stake sizing bypass (found 2026-08-27 after first two fills) | DONE 89cacc410 (live, deployed 2026-08-27 ~06:20Z) — the item-6 flat-stake override at the `_robust_stake_usd` chokepoint rewrote only the receipt/reservation/Kelly cert; the W3 global solver owns the executable share count (`exact_taker_shares` binds `global_decision.shares` through cert build, depth re-sweep, and FOK submit), so Kelly-sized orders shipped: Moscow 28sh $3.08 (22:28Z) and Jinan 12sh $1.02 (02:12Z) vs flat ~5sh, burning the 2% aggregate open-loss ceiling in two orders ($5.85 projected > $5.30) and re-freezing every entry (TIER0_AGGREGATE_OPEN_LOSS_CEILING_EXCEEDED carrier dead-letters). Same root cause explains the earlier 39x "final intent notional_usd exceeds live cap reserved notional" dead-letter class: reservation $0.55 (flat) vs intent notional $3.08 (solver) — two authorities disagreeing about one order's size. Fix: `tier0_flat_stake_notional_cap_usd()` (pure, no q/edge surface) caps `_current_entry_capital_limit` at flat_shares x cheapest-ask x (1+1e-6 headroom), so the solver's own concave objective sizes the order to the smallest venue-legal stake; SELL never capped; unusable curves return None (price gate rejects downstream). 45/45 tier0 policy tests; wiring tests green vs live baseline. |

## Key consult corrections adopted

- No 32-cell capital ladder (small-n multiple-testing machine). Two gates + one bounded Tier 1: f = min(25bp, 1/4·max[0,(r_L−p_fill)/(1−p_fill)]), aggregate unsettled ≤3%.
- Parity with market NEVER unlocks Kelly (parity = zero edge = zero Kelly).
- Flat micro is a research expense, not growth-optimal; diversification unit = city-date cluster (one entry, one risk budget per city-date).
- No per-bucket isotonic; no per-city params (city = robustness veto); lead gets 3 regularized intercepts.
- High-|q−p| stays ELIGIBLE at micro-risk (diagnostic value; fixed risk already removed the magnitude channel).
- Absence = learning loss, not proven EV loss; command volume must never become a success metric.
- Two-way city/date clustering (weather systems correlate cross-city same-date); use the larger uncertainty.
- Tier-0 caps: 10bp target/city-date, 20bp hard ceiling, 2% aggregate unsettled, 10% research drawdown kill.

## Local verifications of consult's risk assumptions

- V2 (candidate provenance): auction receipts store winner + candidate_evaluations_sha256 + nearest frontier only → historical selection-lift impossible; Item 3 adds prospective provenance; historical marked unknown.
- V3 (venue min): min historical filled entry notional = $1.00 → Tier-0 20bp cap infeasible at $268 bankroll → Item 6 BLOCKED (operator fork above).

## Tier-0 activation checklist (execute in order; any failure = stay paused)

1. Branch landed on `live` via the sanctioned path (PR or cherry-pick — operator decides), daemon restarted via deploy_live.py (mesh-coherent), boot log shows: risk_policy artifact policy_version + sha256, Guard 5 pass, TIER0_RESEARCH_MODE state.
2. Item 3 verified live: a dry-cycle certificate carries decision_p0 + decision_p0_source; tier0_candidate_set_provenance receives rows on a completed auction (check after first post-restart auction).
3. `feature_flags.TIER0_RESEARCH_MODE: true` set in live config/settings.json (operator edit — untracked file, verify against risk_policy ceilings at boot).
4. Resume entries: sanctioned control-plane `resume_entries` command (clears `control_plane:global:entries_paused`). The Tier-0 admission gate is now the only entry path.
5. First-hour verification: every submitted ENTRY has price<0.25, TAKER_LIMIT, size == venue min_order_size, one per (city,target_date); scoreboard_panels P3 shows the fills; any violation → re-pause immediately.
6. Standing cadence: run scoreboard_panels + calibrator_walkforward_report + promotion_gates_report --dry-run weekly; selection_lift_report stays locked until 100 clusters (~8-10 weeks); NO formal Gate-B evaluation before the preregistered stopping count.

## Rollback

Global entry pause is the universal rollback. No path from Tier 0 back to q-Kelly. Tier-0 drawdown >10% → auto-pause (live-wired). Any cap/certificate/reconciliation breach → full pause.

## Item 17 (2026-08-27): auction coverage classification fix + AMBER artifact refit

- **Fix 37c4e38fc** (pushed origin/live): live-epoch VENUE_METADATA_STALE holdings were classified `book_state="STALE"`, colliding with 7b008793e's coverage invariant (STALE ⇒ epoch expired) → whole-batch `GLOBAL_HOLDING_AUCTION_COVERAGE_INVALID` requeue loop (575 log occurrences), lengthened reactor cycles, starved exit_monitor (3283 skips, 9 positions >520s monitor debt vs 150s threshold). Now `NO_EXECUTABLE_BOOK` with venue detail in reason. Regression test appended to test_w3_solve_seam_g3.py. Baseline parity verified (16F/248P engine, identical pre/post).
- **posterior_age_inflation refit**: artifact was 41 days stale (2026-07-17); refit 2026-08-27 (targets 1481→3435). AMBER 18h band v: high 0.367→0.558 degC², low 0.244→0.289. Loader is lru_cached per process — daemon picks it up on next restart.
- **Audit verdicts on file** (5 agents): exec latency decision→fill median 5.6s (9-min figure lives upstream: posterior computed_at→decision gaps measured 86s/88s/1072s, n=3, mean ≈6.9min); exit path real (387 filled SELLs) but reactor-lock starved + single-slot EV contention; EMOS receipt stamps SHRUNK_EMOS while strength is structurally 0.0 (never moves center); sigma discards same-day ensemble spread (tail-overconfidence mechanism candidate); price enters only at coherence gate + solver beta-pull.

## Item 18 (2026-08-27): held-belief HWM exemption wiring (exit-organ blindness)

- **Fix**: `load_replacement_belief` (held-only reader; sole callers monitor_refresh.py:6050/6363) never passed `held_redecision=True` to `replacement_live_input_lag_reason`, so the held-continuity exemption (97db58d8a) was architecturally unreachable from the monitor. A newer anchor artifact merely REGISTERED as downloaded (06Z in raw_forecast_artifacts, not yet materialized into a model run) marked every held belief `fresh=False` (`source_cycle_time_raw_forecast_artifacts_lag`) → BELIEF_AUTHORITY_FAULT ("exit organ blind on a live position", 526 occurrences since 8/26) despite a 1.6h-old FULL_CURRENT posterior being served. Layer-2 readthrough cannot compensate: it is deadline-gated off on the live monitor (monitor_refresh.py:2069). Live-reproduced pre-fix (HK 08-28 high/low fresh=False) and post-fix (both fresh=True, lag=None). Wiring regression test spies the kwarg. test_live_safety_invariants failure set byte-identical pre/post (70F, revert-diffed). ENTRY unaffected — no entry path reads this loader.

## Item 19 (2026-08-27): collateral WriteLeaseTimeout verdict (traced, NOT yet fixed)

- Chronic: ~400/day `owner=collateral_snapshot_persist` lease timeouts (2942 total); worst case 10-min snapshot gap 11:48→11:58Z → CURRENT_WEALTH_COLLATERAL_EXPIRED on every auction batch + KELLY_PROOF_MISSING.
- Root (vacuum-audit, verified): (a) DOMINANT — `chain_sync_read_cycle` (post_trade_capital.py:609) and `run_tier0_candidate_settlement_fold` (:279) write zeus_trades via RAW sqlite3 connections, never through write_coordinator.lease() — an implicit DEFERRED txn holds the real WAL write lock up to 75s (child killed at 77s), invisible to the coordinator's fairness system; "database is locked" on tier0-fold is the signature. (b) COMPOUNDING — harvester_pnl_settlement holds a 5000ms MONITOR budget vs everyone else's 250ms, and MONITOR-vs-MONITOR has zero arbitration (bare Lock + 10ms flock spin). (c) No backoff: failed 30s tick just retries; ~20 consecutive losses = the observed gap.
- Provenance guard: main.py:2459 `_refresh_global_collateral_snapshot_if_due` looks like a ready-made second refresh path but was DELIBERATELY orphaned by 7ff49556d ("remove live-trading collateral refresh production" — single-writer-in-sidecar design). Re-wiring it reverses that decision; do NOT take that route without deciding the dual-writer question explicitly.
- Fix route when scheduled: wire both raw writers into write_coordinator.lease() (bounded, fair), and/or shard harvester's 5000ms hold. Not urgent-critical: bursts self-heal, trading continued; but each burst blinds Kelly for its duration.

## Item 20 (2026-08-27): deploy of items 16-18 fixes + storage-watermark entry freeze

- **Deployed** boot_sha=27b0024a (14:01:39Z, boot #39): auction coverage fix (37c4e38fc) + held-belief HWM exemption (1a17bd6dd) live. Full agent-side deploy: ff via the total_loss_loop-sanctioned lane, durable pause 13:09:35Z, CANCEL_PENDING blocker resolved by recovery point-read ("order missing at venue"), 10/10 pre-stop monitor handoff green, verified restart, CAS resume executed 14:01:48Z. Post-boot: BELIEF_AUTHORITY_FAULT=0, coverage churn=0, AMBER 20260827 artifact loaded.
- **Entry freeze root-caused**: RiskGuard DATA_DEGRADED (storage_capacity) since 13:11Z — free 93.4GB < required 99.4GB (10% ratio watermark, riskguard.py:142-143; entries-only block, monitoring/exits unaffected). Cause: purgeable APFS TimeMachine local snapshot (2026-08-26) pinning ~55GB. `tmutil deletelocalsnapshots` → free 148.6GB (ratio 0.149) → GREEN 14:18:57Z. Same signature as the 8/25 episode. Chronic driver: zeus_trades.db 186GB + zeus-world.db 90GB + zeus-forecasts.db 74GB on a 926GB volume; world DB has 11.1M terminal opportunity rows prunable via scripts/prune_terminal_opportunity_events.py (needs PYTHONPATH, cutover lease; deferred — operator-scheduled maintenance, not urgent).

## Item 21 (2026-08-28): steady-state latency verdict + overnight ops summary

- **Steady-state posterior→decision latency (post-deploy, no restart window): median 1.22 min, p90 1.97, mean 3.75, n=66, 60/66 under 2 min** (decisions 08-27T15:00Z→08-28, joined decision_audit.posterior_id → forecast_posteriors.computed_at). The 8.09-min median measured earlier (n=93) was deploy-churn pollution. Trigger path (limit=1 queue + 1s priority lane + per-family wake) is HEALTHY at steady state; the planned limit=1/wake-drop attack is DOWNGRADED to tail-only. Tail cases >2min: Jinan 94.6min (posterior 13:34Z landed in the 13:09-14:01Z deploy pause window), Moscow 52.6min, Istanbul 23.3min (wake-forensics tracing; today's succeeded_latest receipts all show committed=True wake=True).
- **Overnight (parallel session, 21 commits e47ef9b62→a68e1d025)**: 11 further deploy restarts Aug27 10:24→Aug28 03:45 local; each boot pays ~50-120s held-monitor bootstrap warmup (BUY-blocked; verified lines on file). Deploy churn, not steady state.
- **Entry=0 today is MATH, not blockage**: latest auction receipt (decision_log id 597898, 09:02Z, zeus_trades) evaluates full width, `hold_cash.selected=true`, `no_trade_reason=NO_CURRENT_EXECUTABLE_POSITIVE_ORDER`; nearest rejected buy frontier Taipei @0.95 probe_expected_delta_log_wealth=-4.6e-05 (NON_POSITIVE_EXPECTED_OBJECTIVE). 36 entries filled 08-27; riskguard GREEN; monitor 10/10; auction 29-31 families every ~10-20s.
- **Storage watermark re-breach + self-heal**: TM snapshot 2026-08-27-114653 regrew; free 92Gi < 92.6Gi line → DATA_DEGRADED 03:11 local; deleted → 140Gi free, GREEN 03:14. Recurring ~daily until world-DB prune runs (item 20).
- **CERT_EXPIRED residual (4 families, 08-29 targets: Tel Aviv/Taipei/Moscow/Istanbul high)**: entry-screen redecision lane still holds beliefs with valid_until 08-27T20:05/08-28T08:05 past τ_next; belief rows (probability_trace_fact) refresh only via decision receipts (write_belief_row sole producer, reactor.py:3637), and fresh 06z posteriors haven't produced a decision for these yet. Main auction path is NOT affected (reads posteriors directly). Self-heals on next decision; if chronically starved, the redecision lane loses its cheap-screen for exactly the families with no recent decision — candidate future item.

## Item 22 (2026-08-28): capital-gains scoreboard verdict — bottleneck is probability quality, not plumbing

- **Settled-chain alpha (settlement_attribution, n=206 since 8/14)**: win-rate 28.2% vs avg price 31.5% = **-3.3pp edge, -$318**; last week (8/21+, n=85) **-5.5pp, -$220**. By band: mid 0.25-0.75 is the bleed (buy_no mid n=104, -5.3pp, -$267); lo band roughly breakeven-negative. Capital curve (CHAIN pUSD): peak 552 (8/16) → ~200-225 (8/28), incl. ~$10.8k gross settled proceeds churn.
- **Tier-0 candidate labels corroborate tail overconfidence** (tier0_candidate_set_provenance, 573 labeled): hi band p0≥0.75 mean p0 0.955 vs settled_y 0.854 = **-10.1pp overconfident**; mid+ 0.5-0.75 the ONLY positive class (+4.6pp, n=68); lo<0.25 ~calibrated (-1.9pp). Matches forecast-tail-overconfidence memory (full_transport_v1 high-tail under-shrink ≈2x) and the buy_no mid-band loss mechanism (buy_no at mid price = selling the overconfident high tail short).
- **Market-anchored calibrator (live since 8/27T14:39Z) corrects in the right direction**: high q_raw 0.808→0.728/0.643 down-corrected; 0.390→0.123 hard-corrected; low 0.132→0.141 mildly up. n=72 decisions on file; post-calibrator cohort (5 entries) too small to score — settled-chain re-measure needed after ~1 week.
- **Plumbing is NOT the constraint** (today's verdicts): latency median 1.22min; auction full-width every ~10-20s; hold_cash selected on MATH (nearest rejected frontier ΔlogW=-4.6e-05). Low entry rate = correct response to thin measured edge.
- **Attribution grader gap**: 90 terminal positions ungraded (65 July + 22 Aug + 3 May) — mostly awaiting/missing VERIFIED settlement_outcomes; grader idempotent, self-heals as outcomes verify. Watch that 8/26-8/28 terminals get graded; scoreboard blind otherwise.
- **Mainline (priority order)**: (1) hold and MEASURE the calibrator on settled chain (win-rate vs price weekly, this section's query); (2) tail overconfidence at the SOURCE — de-bias under-shrinks full_transport_v1 high tail (EMOS strength structurally 0.0 never moves center: fusion-audit item 17) — fixing the posterior tail is upstream of every band; (3) EMOS receipt honesty fix rides along.

## Item 23 (2026-08-28): latency tail mechanism CLOSED — auction serialization + liquidity gate, not wake plumbing

Wake-forensics + family-hash trace (probe: DecisionProofAccepted payload decision_audit → family hash → grep gap window) decomposed all three >20min tail cases. WAKE_LOST ruled out for all (queue reports show reactor_wake_published_count=1 within 7-20s of every commit; the missing per-family INFO line is a logging artifact — _surface_subprocess_warnings forwards only WARNING/ERROR from subprocess stdout).
- **Jinan 94.6min** = 12min daemon restarts + **50min WINNING every ~15s cycle but rejected ×102 by GLOBAL_BUY_JIT_PRECLIFF_LIQUIDATION_CAPACITY_INFEASIBLE** (required_shares 35.25 vs precliff_bid_shares 6.57 — book too thin to guarantee pre-cliff liquidation); passed when depth recovered. The gate worked as designed: don't submit size you cannot exit.
- **Istanbul 23.3min / Moscow 52.6min** = **single-slot global auction serialization**: the family simply wasn't the winner (Istanbul won its first cycle 14:01:17 local, proof 14:01:44 — seconds once selected; Moscow waited 45min while the Istanbul family held the winner slot, then ~8min GLOBAL_PREFLIGHT_WEALTH_SUPERSEDED churn).
- **Consequence**: no trigger-path code change warranted. The tail is (a) economic protection (precliff), (b) one-order-at-a-time auction throughput. If throughput ever becomes the binding constraint (it is not today — entry rate is edge-limited per item 22), the lever is multi-slot auction admission, an explicit design decision, not a bug fix. Moscow's posterior recommitted 4× in <5min pre-decision (upstream data instability) — flagged, uninvestigated.

## Item 24 (2026-08-28): probation gate rework — remove the in-flight freeze, floor the nonpositive latch

Operator directive: waiting is not a locked-in state; a global continuous market should convert well-processed data into continuous decisions. Found the binding constraint: the overnight revision-probation gates (f6989dcf4 qkernel, 95c083ff0 day0, 2a1854ce1 revision-binding) had two arms that froze ALL entry:
- **in_flight arm**: `open_position_count > 0` gated the strategy. Offline replay against live DBs: qkernel curve status=positive (+$0.34 net on 4 realized closes, window 7d) yet 10 pre-existing open positions held the gate closed — a fifth concurrency throttle stacked on the four pinned sizing levers (risk_policy.yaml) + drawdown kill, and one that cannot drain while the strategy holds any book. Violates the no-caps/no-gate-accretion standing law.
- **nonpositive latch at n=1**: day0 latched on one -$1.07 close. The latch blocks the next probe, so the sample can never grow — a permanent lock, not evidence-gated probation. Shadow lane cannot substitute: 17 day0 RISK_GUARD shadow rows in 9 days, 0 outcome-bound yet.
**Change** (src/riskguard/riskguard.py `_revision_probation_gate_reason`): in_flight arm removed (open positions never gate; concurrency belongs to sizing levers); nonpositive latch requires `realized >= _PROBATION_MIN_REALIZED_SAMPLE = 3`. truth_degraded arm and validated-cohort RESET unchanged; gate rows auto-expire when the reason disappears (`_sync_riskguard_strategy_gate_actions` expires non-recommended rows each tick). Tests reworked: in_flight params dropped, sub-minimum-noise + open-positions-never-gate asserts added; 20/20 targeted pass.

- **Deployed 2026-08-28 10:30Z**: cherry-picked onto the main live tree (59233e79e merge commit carrying a55a9d71a + sibling's ac5e2ff8b line), pushed, riskguard-live sidecar reloaded. First post-restart tick expired BOTH probation gate rows (`riskguard:gate:forecast_qkernel_entry` in_flight open=10 and `riskguard:gate:day0_nowcast_entry` nonpositive n=1 → status=expired, effective_until=10:30:38Z). Entry admission is again owned by economics (auction solver) + pinned sizing levers + drawdown kill, not by the open book.

- **Post-ungate funnel walk (10:30-10:45Z)**: gates expired → auction immediately selected a BUY winner (Tel Aviv NO @0.35, $7.21) and produced 3 DecisionProofAccepted with full chains (proof→plan→reserve→command). First two submits were killed by Gate 5 `deployment_freshness_mismatch` (boot_sha=ac5e2ff8 vs current 59233e79 — MY landing changed the tree under the running daemon; the deploy-freshness kill switch worked exactly as designed), third by GLOBAL_REAUCTION_EPOCH_EXPIRED (10s one-shot token vs 12-52s reactor cycles under churn). Daemon restarted (launchd/sibling) onto boot_sha=59233e79 at 10:31:28Z — freshness mismatch cleared. Calibrator visibly active on these proofs: q_raw 0.916 → q_corr 0.578/0.568. Watcher armed for first post-ungate ENTRY fill.

- **Continuous throughput restored (first 10 minutes post-restart)**: ENTRY #1 10:38:30Z Tel Aviv 08-29 buy_no — GTC post_only maker 20.25sh @0.36 ($7.29), venue-acked LIVE, resting (fill watcher armed). ENTRY #2 10:40:48Z Hong Kong 08-29 — 12sh @0.20, **MATCHED 12.63sh @0.19 within 3s** (price improvement). Both decisions calibrator-corrected (0.916→0.578; 0.509→0.341 — the second entered at 0.19 vs corrected q 0.34, +15pp measured edge). Position registration of the HK fill pending at write time (watcher armed). Note: ENTRY #1 is a POST_ONLY MAKER — verify against the tier0 taker-only container expectation if it was meant to ride the Tier-0 lane; it rode forecast_qkernel_entry (Kelly lane), which permits maker rest.

## Item 25 (2026-08-28): EMOS receipt honesty fix (fusion-audit item 17 rider)

`build_center` stamped `center_method="SHRUNK_EMOS"` whenever EMOS produced a proposal, but at `emos_oos_strength=0.0` (the structural production value — no fitted OOS weight is threaded onto ForecastCase) `shrink()` returns `mu_consensus` exactly: every production receipt claimed an EMOS-adjusted center that EMOS never moved. Now stamps SHRUNK_EMOS only when strength>0; the EMOS proposal stays in the telemetry note. Downstream `predictive_distribution_builder` accepts both stamps (no behavior change); 7/7 center tests pass. Takes effect on next forecast-live restart — rides the next deploy, no dedicated restart warranted (telemetry-truth only). The REAL EMOS question (should the center get a fitted nonzero strength) is part of the tail-overconfidence workstream (item 22 mainline); a GPT-5.6 Pro consult on the fix locus (source tail vs market anchor vs sigma conditional floor) was fired but BLOCKED pre-send (model_not_selectable — needs a human at the ChatGPT window; re-fire with request-key zeus-tail-overconfidence-fix-locus-20260828 after clearing).

- **Throughput confirmed continuous (first 25 min post-ungate)**: 5 ENTRY commands 10:38-10:53Z — Tel Aviv 08-29 maker 20.25sh @0.36 CONFIRMED on-chain 10:52; Hong Kong 08-29 MATCHED 12.63sh @0.19; Moscow 08-29 14sh @0.14 acked; Istanbul 08-29 6sh @0.53 FILLED (q_corr 0.688 vs price 0.53 = +16pp); Tel Aviv 08-30 8.3sh @0.40 FILLED (q_corr 0.43). Every decision calibrator-corrected; fills span the whole band incl. the mid prices where the settled-chain bleed lived — the calibrated cohort now accumulates exactly the evidence item 22's weekly scoreboard needs. Open book 12 positions, $57.93 cost.

## Item 26 (2026-08-28): tail-overconfidence fix locus — consult verdict (REQ-20260828-072509-6b7b41)

GPT-5.6 Pro verdict (88% conf on the week decision): **this week ship (iv) decision-layer discipline only, NO source change** — the market anchor already removed 97.2% of raw-q's excess log loss (0.3507→0.0099); a source fix has only β≈0.10-0.12 leverage while frozen and is weakly identified against β (compression c and β trade off). Concretely: freeze deployed β, clamp 0≤β≤0.12 for the current source version; unknown/new probability-semantics revision → β=0 (market-only), no inheritance; key calibrator fit/lookup by source family+version; semantics change without version change = release-gate failure. λ=10 unchanged. **Reject tail isotonic** (small-sample overfit, gate-policy contamination, bucket incoherence). Conditional sigma = shadow candidate only, as mean-preserving multiplier σ²=floor²·exp(γx)/E[exp(γx)] (NOT max-rule — re-counts spread already in the marginal floor), one γ per lineage, needs demonstrated out-of-sample spread-skill slope first. EMOS strength: do NOT enable just because 0.0 is accidental; fit against continuous residuals in the same date-blocked diagnostic. Premise challenge on file: the -10.1pp hi-band gap may be admission-gate winner's curse + settlement-date clustering — deciding test = pre-gate universe calibration split (eligible vs rejected at matched q) with date-block bootstrap; source hypothesis survives only if the gap exists PRE-gate, within-dates, family-localized. Deployment floor for any future source fix: ≥60 independent settlement-date blocks, ≥200 held-out tail forecasts, ≥30 adverse tail outcomes, frozen-β paired log-loss on date-level means. Center vs width prior: center/regime-selection modestly ahead (std(z)=0.86 argues against broad under-width; bleed is side-concentrated). ONE verify-locally flag: confirm admission (not just sizing) is controlled by r — if raw q still selects positions, THAT reroute is the week's single change instead. Three agents dispatched: admission-r-audit, calibrator-version-audit, pit-diagnostic.

- **calibrator-version-audit verdict (agent, file:line on record)**: (1) β is refit essentially EVERY batch — `_market_anchored_correction_resolver` (global_batch_runtime.py:6094) constructs a fresh MarketAnchoredFitProvider per process_current_global_batch call, so the 6h TTL state (market_anchored_live_fit.py:159-190) never survives; there is no single "deployed β" to freeze. (2) No version keying anywhere: load_fit_rows (market_anchored_live_fit.py:81-132) filters no revision; settlement_attribution schema has no semantics column → new source revision silently POOLS into the fit (the exact violation the consult forbids). (3) No cap: beta=float(coef[-1]) unclamped (market_anchored_residual.py:301); only the residual is clipped. (4) λ already 10 (max of LAMBDA_GRID). Implementation dispatched (beta-discipline-impl, 3 slices): clamp [0,0.12]; provider lifetime fix so TTL is real; unknown-revision fail-to-market guard WITHOUT schema migration (full revision-keyed fit windows deferred until settlement_attribution grows the column). Direction trap flagged to the implementer: "no correction" today falls back to RAW q — the mandate for unknown versions is β=0 = market-only, the opposite end; the guard must clamp toward p0_market, not fall back to q.

- **admission-r-audit verdict (agent)**: the consult's verify-locally flag is CLEARED — admission on the live qkernel-spine path IS controlled by corrected r: solver ranking/EV/NON_POSITIVE_EXPECTED_OBJECTIVE/sizing all consume corrected payoff_probability_mean (solver.py:7475-7484, 7676-7682 → _score_global_single_order_buy_expected:5812); the one remaining raw-q admission gate (CandidateEvaluation.admitted, selection-calibrator lineage) is OR'd away by selected_qkernel_authority for every qkernel-spine proof (event_reactor_adapter.py:30381-30384). Week's ship stays (iv) β discipline. REAL gap found upstream: the entry pre-screen (continuous_redecision.py enqueue_live_redecisions ~1088-1123) filters on RAW q_lcb/q_posterior vs min_edge before candidates ever reach the solver — a FALSE-NEGATIVE filter (drops candidates that corrected r would admit; never admits wrongly). Cost unknown; discriminating probe dispatched (pit-diagnostic task C: recompute screen edge with r≈sigmoid(logit(p0)+0.11·d) over prescreen rejects since 8/16, count flips). If flip count is materially nonzero, routing the pre-screen to r becomes the follow-up ship after β discipline lands.

- **pit-diagnostic results (agent, n=1454 settled tuples, 27 date blocks)**: mechanism picture REVISED. mean z = +0.272 (uniform WARM center bias, ~flat across ensemble-spread strata: 0.272 low vs 0.271 high) — supports the consult's center-first prior. Width defect is real but concentrated on LOW-spread days (std z 1.215 below-median spread vs 0.948 above): sigma is too tight exactly when the ensemble AGREES — the OPPOSITE sign from the "widen on dispersed days" hypothesis; in the consult's mean-preserving multiplier the data wants gamma<0. Feasibility: all three legs live in forecast_posteriors.provenance_json (anchor_value_c / predictive_sigma_c / ensemble_within_sigma_c); caveat — anchor_value_c==served-center is an assumption (fusion appears to widen sigma only, never move mu). Outliers |z|>5 on 3 dates unaudited. Pre-gate winner's-curse test NOT yet runnable from tier0_candidate_set_provenance (only 2 eligible settled rows; instrumentation born 8/26; 2 date blocks vs required 60). UNLOCK: no_trade_regret_events has 102,348/157,699 rows joinable to VERIFIED settlements today and the enrichment writer (enrich_after_settlement) has never had a caller — backfill script dispatched (regret-enrichment-impl). Round-2 consult fired with these numbers (REQ-20260828-081225-3e5f50): does low-spread overconfidence flip the sigma-fix form; is a fitted uniform center-bias subtraction now identified; which rejection strata are valid winner's-curse controls.

- **Prescreen false-negative probe: CLEAN NEGATIVE (task C)**. The raw-q entry pre-screen leaves zero persisted trace (score<min_edge at continuous_redecision.py:1121 is a bare continue; TRADE_SCORE regret rows are a different stage entirely). Reconstructed the candidate universe from probability_trace_fact joined to executable_market_snapshots (18,062 pairs since 8/16, 14,275 price-matched, 13,188 raw-q-rejected, min_edge=0.01 live): applying r=sigmoid(logit(p0)+0.11·d) to the exact screen formula flips **0 of 13,188** rejects to pass. Mechanism: at beta≈0.11, r hugs the market price, and score_r ≈ −(fee+tick) for any candidate whose apparent edge WAS the market disagreement — the correction rejects MORE aggressively than raw q, never less. The admission-side gap is closed: no prescreen reroute needed; the raw-q prescreen is a conservative outer filter and the corrected solver remains the deciding surface. (Caveats on record: no lead-bucket alpha/clip in the proxy — both bias TOWARD finding flips, so the zero is robust.)

- **β discipline landed (slices 1-2), slice 3 DEFERRED by decision**: a1be2a3cb (clamp [0,0.12] + binding WARNING; 2 pre-existing fixture repairs documented in-commit) and 9d4496faf (provider singleton — the 6h TTL had NEVER worked; live refit the calibrator every batch since wiring) on origin/live; 246 tests green. Slice 3 (unknown-revision → market-only) rejected in BOTH offered forms: day0-only degraded guard would force β=0 permanently for qkernel-sourced day1/day2 leads (gutting 2/3 lead buckets — worse than no guard); full plumbing (forecast_conn thread-through + per-candidate strategy_key + settlement_attribution revision column) is its own scoped follow-up. Interim risk bound: the clamp caps unknown-version damage at 0.12 logit leverage. Deployment: handed to the sibling session's next landing (its main tree is 8-ahead/8-behind; message sent 13:4xZ) — calibrator fixes ride its next daemon restart.

- **Outlier audit (task D)**: the low-spread width defect is ROBUST — excluding the 1 likely defect (std z 1.2147→1.1930) or all three |z|>5 rows (→1.1526) leaves it well above high-spread 0.9482; a ~725-row broad effect, not outlier-driven. Verdicts: Madrid 8/13 high z=6.5 = likely DATA_DEFECT_POSTERIOR (day0 tracking froze mu=38.91 sigma=0.167 on an already-elapsed local day, settlement 40.0 — same-day extrema capture gap; independent station check impossible: observation_instants dead, hourly_observations stops 5/02); Tel Aviv 8/25 low and Moscow 8/22 low = HONEST false-consensus misses (tight ensemble, 3.7-5.9°C overnight divergence — exactly the low-spread overconfidence class). Two follow-up leads on record: (a) day0 extrema-capture defect class (Madrid pattern) — check same-day high tracking completeness before settlement eve; (b) settlement resolution_state is BLANK on all three VERIFIED rows — completeness gap in the settlement writer.

- **Madrid-class quantified (task E): real but NARROW**. Pool "served late-in-local-day AND sigma<0.3°C" = only 10 rows in all of Aug; 5 candidates with |z|>2 (6.6x enrichment vs 7.6% base rate), **4 of 5 are Madrid high** (8/02, 8/11, 8/13, 8/19 — all within ±2h of local EOD), 1 Buenos Aires high; **0 low-metric candidates** (0/186 late-bucket lows — directionally confirms the extrema-capture mechanism: a late-served HIGH claims "already observed" falsely, a late low doesn't). Scoped follow-up item: audit MADRID's day0 same-day high extrema tracking/observation source (secondarily Buenos Aires) — NOT a cross-city rewrite; other ~48 cities clean in this window. Caveat: standard-UTC offsets without DST, none of the 5 within 1h of a bucket boundary.

- **Regret enrichment landed (648aeecac)**: scripts/enrich_no_trade_regret_outcomes.py — reuses grade_receipt() truth (no second outcome law); market_events.outcome fallback REFUTED live (0/1689 — unresolved sentinel persists after VERIFIED weather settlement; grading goes range_low/high + settlement_value like production callers); idempotent chunked db_writer_lock(WORLD,BULK); registered (manifest + allowlist, topology_doctor clean); 10/10 tests. **N correction for the methodology review: winner's-curse usable sample is 1,689, not 102k** — only rejects naming a concrete condition_id+direction are gradeable (95k EXECUTOR_EXPRESSIBILITY rows never bound a bin; correctly left NULL, not guessed). Dry-run verdict on the gradeable set: 100 would-have-won / 1589 lost / 46 fillable. --apply backfill executed agent-side (hindsight columns on an analysis ledger, non-money-path). 1,689 still beats tier0_candidate_set_provenance's 2 eligible settled rows by ~3 orders — but stays below the 60-date-block floor until dates accrue; feed to round-2 consult question (d) on valid control strata.

- **Backfill APPLIED (agent-side, live world DB)**: 1,689/1,689 enriched, zero conflicts — 100 would-have-won / 1,589 lost / 46 fillable. (First run trap for the record: the script's canonical DB default resolves under the WORKTREE's empty state dir when run from a worktree; explicit --db/--fcst-db paths required — candidate hardening: refuse when the resolved world DB lacks the target table.) Winner's-curse first pass dispatched (task F): rejected-side calibration by q band vs the admitted -10.1pp hi-band gap; RISK_GUARD alpha-shadow rows as the clean governance-rejected control stratum.

## Item 27 (2026-08-28): round-2 consult verdict (REQ-20260828-081225-3e5f50, verified) — center-first source repair, blocked on probability replay

Mechanism REVISED: leading source defect = identifiable COLD center bias (z=+0.272 means served center too cold — correction is mu+b, sign trap noted); "widen high-spread days" REJECTED; negative-gamma inverse pattern = descriptive only, NOT yet a mechanism (pooled-median split may be city/metric composition; 3 outliers could inflate low-stratum SD 1.215→~1.15). Immediate posture UNCHANGED: versioned capped market anchor stays; no source change at 27 date blocks. **BLOCKER before anything**: prove anchor_value_c/predictive_sigma_c actually generated the served bin probabilities (replay all last-served settled bins, receipt-level agreement within tolerance) — if replay fails, every z is diagnosing the wrong distribution (mu-replay agent dispatched). Then: (1) paired high-vs-low falsification (both metrics same city-date must move together; ±0.10σ equivalence band) + leave-one-city-out (task G dispatched); (2) spread normalized within city/metric causally, robust scale (Qn) beside SD; (3) center correction = additive CELSIUS offset (not b·sigma — entangles with width), robust date-equal Huber, walk-forward; deployment bar = 60 causally out-of-sample date blocks AFTER burn-in, primary endpoint date-equal paired ΔCRPS (one-sided 95% block-bootstrap upper < 0), calibration equivalence |mean z_corrected|≤0.10, decision-layer noninferiority paired log-loss upper < +0.001 with β frozen, then NEW immutable source version deployed with the tested β. Width fix (if ever) = one-sided low-spread uplift σ*=σ·exp(η·max(0,−x)), η≥0, cap 1.25, only AFTER center lands (else width absorbs center heterogeneity); reject max(floor,f(spread)). Rejected-candidate ledger: TRADE_SCORE rows = reliability-surface estimation only (selected on score — not exchangeable controls); EXECUTOR_EXPRESSIBILITY = source-calibration analysis only, excluded from decision-layer controls; best quasi-controls = post-gate capacity/duplication rejections; collapse repeated scans to one canonical opportunity snapshot (effective independence ≈ 27 dates, NOT 1689 rows). Outlier law: exclude only PROVEN mechanical defects (taxonomy frozen: units/station/timezone/metric semantics/revision/staleness/sigma-floor/duplicate); meteorologically valid extremes STAY in proper-score evaluation unwinsorized; report Huber+Qn beside raw.

- **Task G: BOTH round-1 mechanisms FALSIFIED — real signal is diurnal-range under-prediction.** (G1) Paired high/low on 298 city-date pairs, 27 dates: z_high=+0.324, z_low=−0.200 date-equal-weighted; contrast +0.525 = 5x outside the predeclared ±0.10σ equivalence band; 17/27 dates OPPOSITE signs; leave-one-city-out max mover 0.027 (broad-based, not leverage). The served distribution systematically under-predicts the day's RANGE (highs settle warmer, lows colder) — NOT a uniform cold center shift; uniform +b center correction is DEAD. (G2) After causal within-city-metric spread normalization: ordinary-SD gap 28%→6%; Qn ~2%; IQR/1.349 REVERSES — the low-spread width defect was a between-city/metric composition confound and fails the predeclared both-tests rule. Consequence: the round-2 center-correction candidate is withdrawn before it was ever fitted (the paired falsification did its job); the source target becomes per-metric range/dispersion miscalibration. Round-3 consult will fire ONCE with complete evidence after (a) mu-replay blocker verdict and (b) task F2 grader verification — both in flight; every z-based conclusion (including these falsifications) remains conditional on the replay blocker.

- **mu-replay: BLOCKER FIRED — REPLAY_FAILED for the dominant regime.** anchor_value_c/predictive_sigma_c reproduce served q EXACTLY (1e-16) only for q_shape='fused_normal_direct' (pre-observation; formula = renormalized Φ-mass with settlement-preimage offsets, materializer.py:4926-4980 + emos.py:580-655 + settlement_semantics.py:57-98; °F-market half_step≈0.2778°C trap on record). For day0-conditioned rows — 29/30 real settled tuples — the served center is anchor∓day0_remaining_center_delta_c (stamped provenance keeps the RAW pre-correction value, materializer.py:6000-6002 vs :6926) and the per-bin form is an extreme-absorbing conditioner (or 500-sample carrier / residual-likelihood), NOT a CDF: naive replay off by median 6pp, max 95pp per bin. **Consequence: every z-based verdict (tasks A/D/G incl. the diurnal-range signal) is contaminated for day0 rows — the "range under-prediction" may partly BE the unaccounted day0 center delta (its sign matches: high center shifted down, low shifted up).** Task H dispatched: rerun A/G diagnostics restricted to the clean pre-day0 regime where the replay is exact. Round-3 consult holds for task H + F2.

- **Day0 regime is REPLAYABLE_OFFLINE (all 3 sub-shapes)** — provenance carries everything: shape 1 needs anchor_value_c + day0_remaining_center_delta_c (sign from metric) + predictive_sigma_c with k/floor flags + day0_conditioning.observed_extreme_c + bin_topology → day0_conditioner pure functions; shape 2's served q is stamped VERBATIM (day0_remaining_carrier_q + 500 samples); shape 3 needs base mu/sigma + fast_residual_likelihood.scenario_weights → _transport_probability_row_to_observed_extreme (materializer.py:5165-5228, pure). Only the q_lcb/q_ucb bootstrap bounds need RNG re-seeding (deterministic seed, not needed for point q). Full correct replay + TRUE PIT under the served distribution dispatched to mu-replay — this supersedes task H's pre-day0-only fallback as the primary uncontaminated diagnostic (H still runs as the cross-check on the exact-CDF regime).

- **Task F2: grader CORRECT (10/10 hand-verified, both units/directions/outcomes), but task F's numbers INVALID by duplication** — the consult's "collapse repeated scans" warning empirically validated: 667 hi-band rows = only 48 distinct (city,date,metric,bin,direction) bets; repeat count ANTI-correlated with winning (a stale-wrong candidate sits above the q filter and re-logs every redecision cycle: Miami buy_yes n=311 all-lose; top-6 repeat counts all losers). Dedup-corrected hi-band: mean q 0.860 vs realized 0.375 = gap **0.485** (date-equal 0.548) — the 13x alarm drops to ~2.3x vs admitted trades' 0.854. Still direction-against pure winner's-curse (rejects worse-calibrated than admitted), but future regret-ledger analyses MUST dedupe to canonical bets first. Minor data-quality notes: zeus-world market_events table is EMPTY (bin truth came from bin_label text + settlement winning_bin cross-check); some bin_labels are hashes not text.

- **mu-replay FULL verdict (268 tuples, 100% replay agreement to float precision)**: on the CORRECT served distribution — (1) NO material center bias (mean PIT 0.48-0.58 every bucket n≥8); (2) NO tail overconfidence — the served day0-conditioned q is OVER-dispersed (pooled high tail rates 0.79%/1.19% vs 5% calibrated benchmark); (3) diurnal-range contrast −0.004 = not supported (low n=16 thin). Φ(z) is INVALID as PIT once day0 absorption is active (point mass at observed extreme; median gap ~0.50) — every z-based table in items 26-27 is retired. **Reconciliation insight**: last-served ≈ post-observation, so its calibration is near-tautological; the −10.1pp candidate gap and −3.3pp settled bleed live at DECISION-time posteriors. The capital-relevant diagnostic is PIT at decision/entry time — task H (last PRE-day0 posterior per tuple, exact-CDF regime) approximates exactly that and is now the decisive pending measurement. uniform_mixture/city_calibration layers: 0/268 active. Replay machinery on scratchpad (replay_full.py) — candidate for a registered walk-forward diagnostic script when the source experiment formalizes.

## Item 28 (2026-08-28): DECISION-TIME PIT — the capital-relevant defect located

mu-replay on the ACTUAL decision posteriors of 962 settled positions (settlement_attribution.decision_posterior_id join; 846/937 replay-agree at 0.01, the 9.7% disagreements all cleanly flagged uniform_mixture_w June-July rows, mechanism inert since; city_calibration 0/937 ever):
- **Structural fact**: 67% of decisions trade against PRE-day0 posteriors (fused_normal_direct) — the settled-chain last-served sample was 85% day0-conditioned, which is why it looked clean: the conditioner absorbs the observed extreme between decision and settlement ("has seen the answer"). Decision time is where capital is exposed.
- **CENTER BIAS, real and large**: mean PIT 0.63-0.66 vs calibrated 0.50 (all-time n=846 AND the 8/14+ bleed window n=209, 100% agreement there) — settlements land systematically in the UPPER half of the decision-time distribution. Concentrated in HIGH metric (0.64; low 0.54-0.58 thin n).
- **ASYMMETRIC upper-tail failure, not generic width**: P(PIT>0.95) = 16-21% (~3-4x nominal) while P(PIT<0.05) = 0-2% (below nominal). Real highs blow past the forecast's own p95 ~1-in-5. Matches the bleed sign exactly: buy_no on high-temperature bins = shorting an upper tail that realizes; 91% of settled positions are HIGH metric.
- Retires the round-1 "~2x tail overconfidence" framing AND the round-2 cold-center framing (both measured on wrong/contaminated distributions): the true defect = decision-time (pre-observation) forecast center TOO LOW with UNDER-dispersed upper side, standing since June (not recent).
Round-3 consult fires next with the complete chain (this + F2 dedup gap 0.49 + G falsification history + 268-tuple replay method); task H cross-check (all-candidate pre-day0 sweep) folds in when it lands.

- **Task H (clean pre-day0 regime, all-candidate sweep, n=1433/27 dates) — cross-check CONFIRMS source-level**: z_high=+0.29-0.35 in BOTH weightings, z_low≈0.009 (lows ~calibrated pre-day0; the earlier "lows cold" half of the diurnal story was a day0-conditioning measurement artifact). Uniform-shift still falsified (contrast 0.28 > ±0.10σ band). Width: ordinary SD gap 25%, robust Qn 6.5%, IQR 2.2% — direction consistent (no reversal this time), weak pass. Because this sweep is NOT conditioned on trading, it resolves the round-3 contingency: the HIGH-side warm bias (center low in PIT terms = settlement above center) is a SOURCE property of the pre-day0 forecast, not decision-time selection — consistent with mu-replay's decision-position PIT (mean 0.63-0.66, upper tail 16-21%). Madrid 8/13 reclassed: its clean-regime row is a T-1 forecast (z=5.66) — extrema-capture defect theory weakened, honest-miss more plausible. Evidence chain COMPLETE; round-3 consult (REQ-20260828-085514-95fb26, in flight) will land onto a fully-confirmed mechanism: pre-day0 HIGH-metric center-low/upper-tail-thin defect.

## Item 29 (2026-08-28): round-3 consult verdict (REQ-20260828-085514-95fb26, verified) — preregistered +b translation, two gates before fit

Design verdict (92% on ordering): **first candidate = single positive Celsius TRANSLATION of the entire exact pre-day0 HIGH distribution** (push-forward T_b#F: parametric→shift location; carrier→+b every sample; scenario→+b every support point; atoms translate; then recompute bins via existing preimage semantics — never edit a "center field", never per-bin isotonic, never two-piece-normal replacement). Location-only already predicts 12-14% upper-tail from mean PIT 0.63-0.66 — only the excess above that supports a width defect; center and upper-scale form an unidentifiable ridge at 27-60 dates, so fit b, FREEZE, re-ask. Rank 2 (only if translated posterior still exceeds tail band): exact monotone upper transport T_k around the shifted median, k≥1. Rank 3: single monotone quantile map (far later). Scope: b_high only, LOW bit-for-bit unchanged (negative control); guards = LOCO positivity, no sign reversal across lead strata, scope frozen pre-OOS. Day0: NO compensating change — conditioner must consume the translated parent (Conditioner(T_b#F_pre,obs)); its absorbing the correction is correct behavior; compositionality audit dispatched. Fit: b_t = argmin date-equal walk-forward CRPS + λ_b·b², cap from development distribution not guessed. Formal rates: RANDOMIZED PIT (mid-PIT thresholds invalid under atoms; exact conditional tail probabilities, receipt-hash seed). Acceptance (60 OOS date blocks AFTER development, current 27 dates are development only): ΔCRPS date-block bootstrap upper<0; upper-quantile-weighted score (α∈[0.90,0.99]) improves; mean randomized PIT 90% equivalence within [0.47,0.53]; both tail rates within [0.025,0.075] (upper must fall from 16-21% to ≤7.5%) + paired upper-rate reduction; decision-layer noninferiority ≤+0.001 with β frozen (buy_no×high subset must not worsen; trading-layer significance NOT required — β≈0.11 attenuates by design); post-day0 and LOW noninferiority as safety strata. **TWO GATES BEFORE ANY FIT**: (1) BLOCKER branch — matched all-candidate pre-day0 HIGH PIT (standardized to traded city/lead mix, unique posterior tuples, no decision-variable conditioning): clean ⇒ ABORT source experiment, run β=0 comparator in the selected stratum instead (mu-replay dispatched); (2) de-bias-lag rival — signed raw-error-trend regression e_served = α_city + θ·s_causal + lead effects: θ>0 with shrinking intercept ⇒ fix de-biaser response rate, NOT a permanent offset (pit-diagnostic dispatched). Receipts schema for the shadow version (parent_posterior_id, correction_version, b_c, q_raw before/after, r before/after, frozen β) + atomic rollback (b=0 identity / route to old version) on record.

- **Compositionality audit (gate 3)**: Path A (materializer, the served forecast_posteriors) = COMPOSITIONAL — single injection point for +b: `bias_shift_c` → anchor_value_corrected_c at materializer.py:5714; sole read-back :5973; day0 delta stacks on top (:5998-6002, magnitude independent of mu); exhaustive grep confirms no second anchor read or cached mu. Two flags: **Path B** — NOAA-preliminary shared carrier (materializer.py:5825-5942) takes NO mu at all (sampled from a separate NOAA future-obs ensemble, built before _mu_anchor exists) → a translation is silently DROPPED for those families (bounded: 0/937 decision posteriors, 3/268 last-served are carrier-shape — but scope needs the routing answer); **Path C** — a second independent day0-conditioning implementation (condition_day0 via qkernel_spine_bridge.py:1253 + predictive_distribution_builder.py:318/359, live-imported by both engine entry files) — dual-maintenance risk; routing probe dispatched (does it produce served/decision q or post-process a Path-A product?).

## Item 30 (2026-08-28): both pre-fit gates CLEARED — source experiment proceeds (shadow), one operator decision flagged

- **Gate 1 SOURCE_CONFIRMED** (mu-replay branch test, n=624 all-candidate HIGH tuples, 510 replay-validated): mean PIT 0.58-0.64 and randomized upper-tail rate 9.8-15.7% (vs 7.5% threshold) across ALL FIVE weightings (raw, equal-date 49 blocks, city×lead match, both marginal matches) — the displacement exists in candidates never touched by trading decisions. Selection explanation DEAD. 846 position rows collapse to 748 distinct posteriors (verify-locally item). Known exclusion: 74 uniform_mixture_w rows (June-July mechanism, cleanly flagged).
- **Gate 2 LEVEL_ERROR by construction** (pit-diagnostic): there IS no active de-biaser to lag — DebiasAuthority is unconditionally _NoOpDebiasAuthority (qkernel_spine_bridge.py:180, "legacy bias-maze strip 2026-06-17"); the materializer's anchor_representativeness_debias is doubly dead (artifact file absent AND bias_shift_c hard-coded None at materializer.py:5709 under the **2026-06-18 operator RAW NO-DE-BIAS LAW** — "NO fitted forward per-city de-bias"). e_served ≡ e_raw identically; the warm HIGH bias lives in the raw multi-model consensus / fusion combination itself.
- **OPERATOR DECISION FLAGGED (not yet needed)**: deploying any fitted +b translation would REVERSE the 2026-06-18 RAW law. Shadow evaluation (no serving change) does not — and 60 OOS date blocks (~2 months) must accumulate first regardless. The reversal decision goes to the operator WITH the completed evidence chain when the preregistered acceptance tests pass, not before.
- **Gate 3 partial**: Path A compositional (injection point materializer.py:5714 — note: the +b enters through the very bias_shift_c seam the RAW law pinned to None, so the law reversal and the implementation share one line); Path C routing probe in flight (deployment precondition only, not a fit precondition); Path B carrier bypass bounded (0/937 decision posteriors).
- **Next: shadow fitter implementation dispatched** — walk-forward date-equal CRPS fit of the single b_high parameter on the exact replayed distributions, development-phase run to freeze the estimator form + engineering cap, preregistration doc per item 29's acceptance bars. Existing scripts/fit_anchor_representativeness_debias.py (per-city median, EB-shrunk, HIGH-only, do-no-harm gated) audited for reuse vs new single-parameter fitter.

- **Gate 3 CLOSED — single injection point confirmed for the whole live surface**: Path C verdict = DIFFERENT_SURFACE (qkernel spine's condition_day0 is gate/receipt context; traded q comes from _served_joint_belief_from_proofs which rehydrates the reactor-served Path-A posterior — its own docstring forbids rebuilding a second point probability; build_joint_q fallback is dead on the live path, served_joint_q unconditionally precomputed with typed no-trade on failure). PredictiveDistributionBuilder has exactly one live instantiation (inside the spine bridge). Path B = narrow NOAA-preliminary finality window (data-finality gate day0_authority.py:117, not an entry surface). **Consequence: a +b translation at materializer.py:5714 covers the entire live decision surface; no second injection, no dual-maintenance conflict.** All three pre-fit gates green; awaiting the b_high development fit (mu-replay) to freeze the estimator form for preregistration.

## Item 31 (2026-08-28): b_high development fit COMPLETE — preregistration parameters frozen

Walk-forward shadow fit on the 510-row validated population (49 dates, burn-in 10, strictly-prior training, exact replayed distributions, step-CDF CRPS over bin_topology center_c points):
- **b converges**: λ=0.02 trajectory 0.13→0.534, last-5 within 0.04, no drift/sign flips; λ-sensitivity smooth (0→0.02→0.1 gives 0.588→0.534→0.394).
- **Dev out-of-sample (each date scored with its own prior-fitted b)**: ΔCRPS mean −0.0808 (−9.6%), 32/39 dates improve (82%); PIT recenters 0.6057→0.5087 (ideal 0.500); upper randomized tail 0.1305→0.0772 (already at the ≤0.075 acceptance boundary), lower stays under nominal (0.0378). Non-overcorrecting.
- **Guards pass**: LOCO 49/49 positive (0.456-0.622, no city >15% influence); per-lead positive both buckets (day0 0.41, day1 0.63 — sensible gradient).
- **Independent cross-validation**: the dormant scripts/fit_anchor_representativeness_debias.py estimator (different data source: single-model OM9 raw vs VERIFIED, full history, 52 cities) agrees in direction and scale (EB mean δ=−0.665 vs our −0.40/−0.46) AND in between-city dispersion (τ≈0.85 vs 0.77) — cold-center is broad across ~50 cities (Jakarta/Busan/Seoul cluster negative; LA/Chicago/Jeddah positive), NOT a few-city artifact. Global-b is a defensible first-order correction; per-city EB-shrunk correction flagged as the natural iteration AFTER the global experiment, not a blocker.
- **FROZEN for preregistration**: b_high global, λ_b=0.02, burn-in ≥10 dates (limitation on record: first ~5 post-burn-in fits underserve until the window matures — extend to 15-20 if the operator prefers), hard cap 0.55°C (p95 of dev trajectory ≈ max ≈ 0.53), fit form = date-equal walk-forward CRPS + λ_b·b², LOW untouched, injection at materializer.py:5714, acceptance bars per item 29 (60 OOS date blocks, ΔCRPS bootstrap, PIT equivalence [0.47,0.53], tails [0.025,0.075], decision-layer noninferiority with β frozen).
- **Status**: development phase closed; the 49 dev dates are burned. NEXT: (a) operator decision on the 2026-06-18 RAW-law reversal (evidence chain complete: items 26-31); (b) if approved, implement the shadow-version fitter + receipts (item 29 schema) and start accumulating the 60 OOS blocks (~2 months at current settlement cadence). The market anchor (β clamped [0,0.12], frozen) remains the live decision guard throughout.

## Item 32 (2026-09-01): capital-first realignment — bleed localized, valve outage root-caused, decision package

- **Capital (chain-truth reducer** over deduped venue_trade_facts + settlement_attribution.won, validated 60/60 against payout_observations; position_current economics columns untouched): 14-day net **−$402.16**; last 7d −$156.67 over 100 settled. The entire loss is ONE stratum: **buy_no entry price ∈[0.30,0.70] = 56 bets, −$175.63, alpha −13.5pp** (win 33.9% vs price-weighted breakeven 47.4%); the other 44 bets net +$18.96 at alpha **+11.9pp**. Open exposure still in-band: 6 positions $35.31 (Tel Aviv ×5 incl. $18.34 HIGH 9/02, Istanbul ×1) — same cities that top the loser table.
- **Causal chain on the 37 losers** (all joined to decision posteriors; forecast_posteriors lives in zeus-forecasts.db): effectively 37/37 are governed by pre-day0 HIGH-family centers — the 11 nominally day0-conditioned rows were ~03:50Z single-METAR decisions taken before the daily high, center untouched. |center−bracket| ≤1.51°C, 70% <1°C, directional split 56.8% vs Φ(0.53/3.0)=57.0% predicted by the frozen b_high — **the items 26-31 mechanism now has live capital confirmation, and the 0.53°C translation covers this loser population**.
- **Independent second defect ROOT-CAUSED — admission valve dark**: the market-anchored correction (live 2026-08-27T10:04Z) shows applied=true only for ~26h; zero since 2026-08-28T12:07Z (4395/4740 certs false). Not an exception: loop-line commit **4ae10eb7** "fix(engine): keep live auction on replacement q" hard-codes `payoff_q_correction_resolver = None` in global_batch_runtime.py (feeds both consumers, selection + capital-proof), and its added test asserts only `[None, None]` — it cannot distinguish a scenario-scoped exemption from a global kill. No documented reason for a GLOBAL disable exists; the scenario named in the title got no scenario-scoped branch. While alive the valve measurably compressed the overconfidence gap (market/q ratio mean 1.78 vs 3.38 dark); the dark bucket carries >60% of loser dollars ($87.21 of the joined losses).
- **β bound check (offline replication of the daemon-line fitter, n=665)**: current β=0.080 IN [0,0.12], but the unbounded fit has genuinely excursed: >0.12 at the −17d and −40d training windows (max 0.178), and **negative for ~3 weeks** (−45..−70d, min −0.129) — signal inversion, not shrinkage. The clamp (a1be2a3cb, origin/live) has no counterpart on the daemon line.
- **Divergence facts**: daemon line (local `live`, boot_sha 6080fd647) is ahead 229 / behind 27 vs origin/live since 2026-08-28 05:46. It independently built a TTL live-fit provider (market_anchored_live_fit.py: 6h TTL, settled-only walk-forward, in-process cache) — supersedes the 9d4496faf concern. RAW-law seam intact there (bias_shift_c=None, now :5846/:5851).
- **DECISION PACKAGE (operator)**: (1) **re-enable the valve**: `git revert 4ae10eb7` on the daemon line (falls back to re-wiring per e47ef9b6 if context drifted) + β clamp (cherry-pick a1be2a3cb or native 3-line clamp in fit()) + restart; if a real held-replacement conflict motivated the disable, the correct form is a scenario-scoped exemption, not global None. (2) **RAW-law reversal for b_high**: evidence complete (items 26-31 + this item). Per the standing no-shadow law the direct path is live deploy under the frozen cap 0.55°C with item-29 receipts + b=0 atomic rollback, scored on the live settled chain; the 60-OOS-block shadow remains the conservative preregistered alternative. (3) **branch reconciliation**: minimum viable = the two commits in (1); full merge of the 27 origin/live commits is a separate call.

## Item 33 (2026-09-04): operator mandate executed — three live landings on the daemon line

Operator: "不要再玩文字和拼凑数字 … 大刀阔斧的革新 … do whatever it takes" (03:30Z). Landing lane = `git cherry-pick -x` onto local `live` (the main-tree guard's one permitted local lane) + `deploy_live.py restart all --allow-unpushed`; entries resume via the daemon's own guard recovery.
- **03:02Z valve re-enabled + β clamp** (live e5cc3509d, edcd10f9f): first certificate 03:13Z applied=true, β=0.071, n_train=695; 0 `PREDICTIVE_MEAN_SUPERSEDED` rejects (the 8/27 26h window had 115/115 corrected decisions reach an ExecutionCommandCertificate, so the recurrence risk raised against the revert is empirically absent). Disable motive resolved: loop incident 6d7b04ad (entry q corrected 0.509→0.341 while HOLD math used raw q) generalized into a global kill; the loop's own later diagnosis (cdafcec7, root split_q_point_authority) argued the correction should exist.
- **03:54Z served-center de-bias, HIGH only** (live fdcb543ab, 3f63b3cd2) — the 2026-06-18 RAW law is reversed. `src/calibration/center_debias_live_fit.py`: per-city EB residual mean (settled − served fused center, pre-day0 winners lead 1-2, VERIFIED, `settled_at < cutoff`), cutoff = UTC 6h floor, MIN_ROWS 200, N_MIN_CITY 8, |shift| ≤ 2°C, LOW disabled (walk-forward do-no-harm CI crosses zero). Applied at `_mu_anchor` (post-fusion, pre-day0-delta); provenance `center_debias_c/_param_hash/_training_cutoff`; `anchor_value_c` stays uncorrected (fitter basis). Verified: 8/8 HIGH posteriors since restart shifted per city (Taipei +1.141 → E[T] from served q_json moved +1.13; LOW 0/5), in-process fit reproduces the study table to |Δ|≤0.0042°C.
- **04:07Z grader fix + backfill** (live 69bb2076d, 64c263dd4; `scripts/regrade_unattributable_settlements.py --apply`): receipt closure is an audit note, q authority = identity+hash-verified certificate; `global_actuation_identity` (non-empty on 100% of certs) no longer misread as an auction declaration. 364 UNATTRIBUTABLE rows re-graded → 269 recovered (SKILL_LOSS 86 / SKILL_WIN 85 / MISCALIBRATED_LOSS 94 / …), 95 remain; calibrator training rows 735 → 1004. Offline replication of the next live fit: n_train=964, β=0.014, α={day0 +0.09, day1 +0.41, day2 −0.20}.
- **Alignment fact**: on the full table β→0.01 — raw q carries almost no residual information beyond the market price; the surviving structure is the lead-bucket intercept map (day2+ NO high band the only significant +alpha cell; day1 buy_no the only significant −alpha cells; day0 −$661 outside the valve's reach). Rejected: static per-cell Kelly multipliers (expanding-window counterfactual 0 bets / −$280).
- **Open**: day0 mechanism (anatomy in flight); whether q_corrected reaches the final stake (sizing reproduction in flight); throughput under the β≈0.01 fit; decision_log 30-day retention vs certificate references (audit-only now).

## Item 34 (2026-09-04): day0 anatomy — one bias, amplified by size; two rules rejected, none added

- **Mechanism (day0 n=284, −$642):** the served posterior is over-sharp by ~+0.37 in P(our token wins) at EVERY lead (day0 NO +0.395, day1 NO +0.371, day2 NO +0.173); on traded positions our q is worse than climatology at every lead (Brier reliability 19× the market's on day0) while within-direction AUC is fine (0.67-0.78) — a level error. Not latency (post-buy drift is a coin flip), not exits (exited cohort net +$215). Kelly is monotone in the fake edge, so 22 oversized positions (7.7% of day0 count, 40% of capital) carry 77% of day0 loss; 2026-08-01 alone (14 positions, mean $51, kelly_multiplier 0.25 that day) is 57%.
- **Valve algebra:** for buy_no the market-anchored valve rejects iff α_lead ≥ −β·clip(logit q − logit p0) — a per-(lead,direction) switch with within-cell AUC ≈ 0.47. Under the served n=964 artifact this is now the live ban table: day0/day1 buy_no rejected, day2 buy_yes rejected; day0 buy_yes / day1 buy_yes / day2 buy_no admitted at 6-25% of raw size.
- **Counterfactual on the 927 settled rows with q, stake deflated by the live artifact's edge_cal/edge_raw:** raw −$1,076 → valve alone **+$60**; adding the 10% concentration ceiling +$45; adding a 3× trailing-median size cap +$44; adding a day0 buy_yes<0.30 block +$51. The two rules the anatomy proposed are redundant once the corrected q reaches the stake (verified item 33) — **not added** (no-gate-accretion).
- **Open, separate:** exit plumbing (exit_pending_missing 5,293 rejections all-time, −$88 on day0); scale-in tranches exceeding the 10% ceiling (e.g. 8/20 Shenzhen 101-share second tranche → $39 on a $21 ceiling) — under audit.
- **Growth lever:** the only significant +alpha cell (lead≥2 × buy_no × high band, +$188) receives 6 entries/week vs 61 for day0; supply-chain audit in flight (market existence at T−2, posterior coverage, funnel, depth, scheduling).

## Item 35 (2026-09-04): growth-lever hunt — the far-lead high-band NO cell is survivorship; no ranking/admission change ships

- **Supply chain:** the only settled cell with significant +alpha (lead≥2 × buy_no × NO price >0.70: n=52, wr 0.885 vs 0.766, +$181) has had zero supply since 2026-07-26. Market existence (T−2 markets >90% for the international cluster), posterior coverage (82% of T−2 slots), and scheduling are not the bottleneck. Root: loop commit 4205ae1c8 → e5d89e680 (07-26) switched the BUY objective from robust/LCB to the posterior mean; the `q_lcb > price` admission gate (live_admission.py:287-292) survived but is clearable via qkernel_spine_bridge.py:2300-2320 (introduced 06-29 for low-price YES), so the mean ranker now selects mid-band NO with q_lcb ≪ price (week34: 342/344 selected buy_no had failed the LCB gate). Posterior width did not change; the option set (opportunity_book best buy_no q_lcb 0.79-0.88 weekly) did not shrink — only the choice.
- **Refutation on the full candidate set** (ActionableTradeCertificate.opportunity_book.candidates, 2026-07..08, parser validated 2706/0 vs settlement_attribution.won): the high-band far-lead NO cell has ~zero alpha over ALL its candidates (Jul +0.007, Aug +0.014 per unit; 0.90+ sub-band Aug −0.030); the settled +$181 is selection-conditional and half of it is two Seoul trades. Ranking counterfactuals, August, $1-unit: live mean ranker +35.3 (337 picks) vs robust/LCB ranker −45.4 (83 picks, 254 no-trade) vs LCB admission gate on top of mean ranking +8.6. **Nothing ships.** The 2-D (lead×band) calibrator is statistically better OOS (Δlogloss +0.0049, CI all-positive) but not the lever; parked.
- **Live posture after the four landings:** the served calibrator (n=964, β=0.014) admits day0 buy_yes / day1 buy_yes / day2 buy_no at 6-25% of raw size and rejects day0/day1 buy_no in typical inputs; today's books show 5 day0 buy_no selections at $13 total. Honest full-set scoreboard for the live ranker by month×lead (Aug, $1-unit): day0 +255 / day1 −416 / day2 +35 — day1 buy_no remains the structural bleed and is exactly what the valve now suppresses (Aug-27 26h window: 108 day1 buy_no decisions, $728; since 03:02Z today: 0).
- **Deployment-gate note (loop discipline):** `deployment_freshness_mismatch` (gate_runtime.py:88) blocked 81% of lead2 submissions over 14 days because loop commits (784 in 14d) land without restarts (419 restart-guard pauses); each pause costs ~3.5 min of entries (5% of wall time). Operator's call; no code change.

- **Item 35 addendum — the LCB admission gate, fully adjudicated.** The full-book replay (78,452 candidates, 3,566 batches, parser 876/876 and 2706/0 vs `won`) found the eligible pool averages 1.20 candidates/batch (execution-economics gates act upstream of ranking), so the 07-26 ranking flip itself is worth −$39 and every dollar of the "robust" gain comes from the `q_lcb > price` ADMISSION gate skipping batches (+$144 over 07-19..09-03 on raw stakes). Against the live valve (walk-forward artifact deflating stakes) the gate's increment at lead≤1 is +$22 in August (CI [+17,+27]) — but it is one week (ISO 34: +$12.9 of the total) and one cell (day1 buy_yes <0.30: +$11.4), negative in weeks 33 and 36, and today it would reject 8/8 live decisions (every pick has q_lcb ≤ price; 465 of 2,503 August picks carry q_lcb≈0 from FORECAST_BOOTSTRAP). Adding q_lcb as a second calibrator feature does not improve walk-forward log loss (Aug −0.0017, Sep +0.0054). Verdict: a switch that turns the book off, not a discriminator; not added. The one signal it does carry (day1 buy_yes low band: gate-PASS wr 0.099 vs FAIL 0.045 in Aug) is the same cell the served calibrator already sizes at 15-25% of raw.

## Item 36 (2026-09-04): slice-time mandate — market leads q; two money-path defects fixed live; growth lever relocated to day0 conditioning

Operator reframed the objective to slice-time convergence (decide ahead of the market, harvest the differential, exit before the expectation decays, hold if credible; never infer the decision from the outcome). Five read-only lanes + two ChatGPT consult rounds (REQ-20260904-011326-2892f6, REQ-20260904-020707-22d27b) measured it on 60 days / 720 entries / 480 market-days, cluster-bootstrapped by city×date. Full ledger: session scratchpad `MANDATE_2026-09-04.md`; lane reports `convergence/REPORT_V2.md`, `diurnal/REPORT.md`, `exit_rejects/REPORT.md`, `exit_rule/REPORT.md`, `latency/REPORT.md`, `polarity/REPORT.md`, `dataface/REPORT.md`.

**Verdicts (all CIs cluster-bootstrap, city×date).**
- The market leads our posterior at every NWP horizon: sign(q−p)→Δp hit 0.470/0.421/0.320 at 1h/6h/24h; sign(p−q)→Δq 0.595/0.633/0.715. Brier(q)−Brier(p)=+0.044 [+0.033,+0.055] in every hours-to-close bucket; worse on the traded subset (adverse selection). Reverse event-time slope +0.85 vs forward κ +0.04: our revision reconstructs the price.
- Executable markout bid_{fill+h}(held) − P_fill < 0 at every horizon with CIs excluding zero (−2.4¢ @0, −7.6¢ @24h); 97% of entries underwater at fill; no maker/taker/lead/side split has a positive LCB. Exit rules cannot fix an entry book whose optimal hold time is zero.
- The only genuine lead: day0 observation arrival. Fetch 0.2 min, belief 0.5 min, market absorbs in ~3 min (half of prints never move 1¢), our belief moves first 25:2 — and our q jump is ~7× the market's price move (−59pp vs −8¢), 52-58% same-sign. The lead exists; the reaction magnitude destroys it. Chain latency is not a bottleneck anywhere (ingest→posterior p50 0.3 min event-aligned; decision→fill 4 s).
- Day0 remaining-day posterior is overconfident on the running-extreme bin pre-peak: at local 09-11 a stated q_floor of 0.90-0.95 realises 0.307 (market Brier 0.089 vs ours 0.281 on confident pre-peak slices). A station diurnal-residual nowcast (empirical hazard on final−running extreme by hours-to-peak × NWP gap band, EB-shrunk, walk-forward) beats our posterior on executable edge (the set "model trades, nowcast vetoes" is −0.020/unit HIGH [−0.040,−0.001], −0.043 LOW, negative in 6/6 OOS windows) but never beats the market's Brier ⇒ wire as a VETO on day0 entries, not as q. Live day0_nowcast_entry positions: −$800 on $1,900 (510 settled), worst cell HIGH local 09-11 −82%.
- "127/159 exit intents rejected" was per-retry events on 7 dead-bin positions with bid 0.00 ($0.03 recoverable); SELL commands fill ~80% (101/127, $469). 30-day dollars lost to rejections: $52.61 (17 positions, one FAK no-match never retried = $28). The exit ORGAN is silent by construction (hold value on raw q ≥ bid; 0 statistical sells in 7d; only panic fires), but that is downstream of the entry problem.

**Landed on the daemon line (both verified, entries resumed).**
1. `d2df69d05` fix(calibration): market-anchored α applied in held-token space for buy_no. The calibrator is FIT in in-bin/YES space (settlement_attribution complements q and price for buy_no) but was APPLIED to the held token's (raw_q, p0) with no direction ⇒ +α for every side; the correct NO law is logit p_NO − α + β·x. Since 08-27 every applied buy_no certificate carried +α: day1 buy_no 108 certs / $728 stake at α=+0.58 — the correct algebra rejects 108/108 (mean edge −0.119). That cell was the post-valve cohort's only loss and the August day1 −$416 mechanism. Fix = complement in, apply, complement out; `side` is a required argument that raises on unknown vocabulary; certificates record the effective signed α. 37 calibrator tests pass.
2. `a6aaa648b` / `214326fa0` / `5707034b6` fix(exit)/(risk): protective triggers with no executable bid fold to the budget-exempt liquidity-wait instead of `hard_fact_sell_authority_invalid` retry burn; direct reduce-only triggers (RED/hard-fact/panic/zero-support) are never released into the global-sell reauction (whose adapter carries no hard-fact authority ⇒ deterministic permanent rejection, 42/42 in 14d); reduce-only SELL submits exempt from `allocator_not_configured` (a restart-time entry gate that also blocked exits, 12 rejects/7 positions in 30d). Entry-size-below-venue-minimum guard already exists (execution_intent.py:2088); not duplicated.

**Refuted / closed.** Corrected q as an exit comparator (≈mid+α, never fires). Token price polarity as a live bug (live paths are direction-aware; 0/1,583 buy_no rows lack no_token_id; it was an analysis join error — memory `held-side-price-needs-direction-aware-token`). Hong Kong "absorbing-floor leak" (HKO settles by oracle truncation, 29.7→29; the leak metric used half-up). Latency as a lever.

**Operator decisions pending (not taken).** Consult (0.96) recommends: new entries OFF, whitelist empty, reopen only via simultaneous one-sided LCB on post-fill executable P&L per precommitted cell (target-date max-t cluster bootstrap, weekly looks); emergency exit rule for the existing book = sell at external size-adjusted bid unless payout is mechanically locked. My position: the sign fix removes the one identified negative cell; the post-valve cohort is otherwise flat; measure a week under the fix before deciding the posture.

**Next builds, in order.** (1) Day0 nowcast veto on the entry path (walk-forward negative veto set in 6/6 windows). (2) Calibrator training population = intention-to-trade certificates (filled+unfilled+rejected), side-normalised, lead×side, cross-fitted — never generic q_in_bin rows. (3) Fill-conditioned market-only continuation rule for exits (bid ≥ max{π_fill − ρ, max_h[bid + μ(h) − ρ(h)]}); raw q leaves the exit law. (4) Reaction-magnitude damping on observation arrival, re-tested by post-fill markout against the 1-4¢ spread — the only place a lead has been measured.

### Item 36 addendum (2026-09-04 ~09:55Z): third landing, entry blackout root-caused, riskguard self-gate, three builds in flight

- **Landed `97ca7531d`** fix(execution): M5 ws-gap reconcile now absorbs a chain-synced `pending_exit` holding whose fresh exchange size equals `chain_shares` (`_CHAIN_CONFIRMED_HELD_PHASES` += pending_exit, the 92f2f3040 pattern). Root cause of a 3.5h entry blackout (05:08→08:40Z): a same-token re-entry (Chengdu 09-04 HIGH 32°C YES) after a fully exited prior position nets the journal negative (`_journal_positions_by_token` keeps inactive-phase SELLs but drops their BUYs: −15.33 vs exchange 10.92) → `position_drift` finding → `ws_gap_guard.blocks_market` True for EVERY market. Finding 530deb22 resolved `position_drift_chain_confirmed_active_holding` on the first sweep after restart. 208/208 tests; the new test reproduces the finding without the fix. Memory: `m5-latch-freezes-on-journal-asymmetry`.
- **Riskguard self-gate (09:37:54Z, not a bug, not overridden):** `riskguard:gate:forecast_qkernel_entry` OPEN with `market_relative_alpha_unproven(status=rejected, model_evalue=0.0032, required=10, clusters=10, law=executable_min_order_capital_gain_v2)` — the engine's own sequential e-value test (market/model likelihood, one claim per city-date) rejected the lead-1/2 law on the last 10 clusters, i.e. the same verdict as today's slice-time measurement. Release is automatic when the e-value crosses 10 under the current law; no manual clear exists. `day0_nowcast_entry` remains open. This is the consult's "whitelist empty until proof" implemented by the codebase itself; the reopening path is the work in flight.
- **Withdrawn:** the diurnal lane's "absorbing-floor leak" (top-bin label parsed as a scalar; the real Hong Kong leak was the pre-08-24 `fused_normal_direct` day0 path, superseded by the remaining-carrier). Surviving invariant: a day0 row with an active observation must not serve `fused_normal_direct` (today: Jinan only).
- **In flight (worktree/opus):** (1) day0 diurnal-residual nowcast VETO — artifact `state/day0_diurnal_residual.json` + fitter, `DAY0_DIURNAL_NOWCAST_VETO` in `day0_admission.py`, receipt fields, replay 08-05→09-03 on certificates and on the 635 `day0_nowcast_entry` positions; (2) intention-to-trade calibrator study (all certificates, side-normalised, lead×side, cross-fitted; decision-impact per model); (3) fill-conditioned market-only exit continuation law (π_fill, μ(h), rule replay with true sizes, seam in `predicted_bin_law`).
- **Sign-fix live verification pending** on the first buy_no certificate written after 07:47Z; day0 buy_no certificates cluster at 16:00-03:00Z, so the first test lands this evening UTC. `verify_signfix_deploy.sh <restart_iso>` prints alpha_recorded per direction (expect buy_no = −buy_yes per lead).

### Item 36 addendum 2 (2026-09-04 10:30Z): day0 diurnal-residual nowcast veto LANDED; intent-population calibrator refuted

- **Landed `d3a49f9f6` + `52867f2af`** feat(day0): station diurnal-residual nowcast veto on the Day0 entry path. Estimator = empirical distribution of D = final − running extreme by (metric, hours-to-peak, NWP-gap band), EB-shrunk pooled→gap→city, fit walk-forward; artifact `state/day0_diurnal_residual.json` (156 KB, 2.25M station-hours, 54 cities, fit_date 2026-09-04; loader fail-open, stale after 14 days). Gate 9 `DAY0_DIURNAL_NOWCAST_VETO` in `day0_admission.py`: veto iff the held token's decision price p0 ≥ nowcast q_held. Stamp `_edli_day0_nowcast_{q_held,basis,fit_date}` sealed into the certificate hash; no DB reads in the hot path. 45 tests; estimator bit-exact vs the study. OOS replay 08-05→09-03: 2,192 certificates — vetoed set −0.046/unit, kept −0.036 vs full −0.040 (point estimates; CIs straddle zero on 30 days); on the 305 filled `day0_nowcast_entry` positions the veto drops 77 positions at −82% ROI (−$383 on $467) and keeps 228 at +0.029/unit. Refit plist `com.zeus.day0-diurnal-refit` (daily) written to the session scratchpad, **not loaded** — operator action; the artifact is inert after 14 days without it.
- **Intent-population calibrator (consult §d) REFUTED** (scratchpad `intentcal/REPORT.md`): decision_certificates are 6.5× duplicated (5,955 settled rows = 922 claims); "train on all certificates" is significantly worse than the live law; at matched price the traded subset shows no winner's curse (price mix); no model beats the market price OOS (live law A: Δ log-loss +0.0014 [−0.0013, +0.0043] vs p0). Day1 buy_no α is negative in held-token space in every variant (97% bootstrap mass < 0). Ship-worthy delta = a 1/claim-count soft weight in the existing fit (hygiene, queued). Ranker selection bias is real (coefficients move up to 0.41) but fitting on never-selected candidates recovers no edge. Memory: `certificate-table-duplication-and-price-mix`.
- **Exit fixes verified live** (events since 08:07Z): one correctly labelled `exit_no_executable_bid` on the dead-bin position, zero `hard_fact_sell_authority_invalid`, zero `allocator_not_configured` on SELL, SELL commands 2/2 filled.
- Remaining in flight: fill-conditioned market-only exit continuation law (study); sign-fix live verification on the first buy_no certificate (day0 buy_no cluster 16:00-03:00Z).

### Item 36 addendum 3 (2026-09-04 10:40Z): exit continuation law refuted; the exit organ ships only a consistency change

- **Fill-conditioned exit law (consult §e) REFUTED** (scratchpad `exitlaw/REPORT.md`, 695 positions / $4,675 / 740k held-side slices, chain-truth labels, fee-net bids). π_fill beats raw monitor q (ΔBrier −0.062; raw q bias +0.170) but is significantly worse than the served market-anchored π (+0.0011 [+0.0002, +0.0021]) and equal to the mid; μ(h) has OOS R² ≤ 0.002 at every horizon. Replay: hold −$1,288; current organ −$936; R* −$870…−$991 (loses to immediate exit with an excluding CI and destroys the one profitable cell, lead2 buy_no +$17 → −$109); the v2 "3¢ continuation" −$333 is immediate exit in costume (median hold 0.0h); sell-at-first-refresh −$301 (+$634 [+36, +1,275] vs organ). An oracle at max bid is +$1,820, so dispersion exists but no exit-time observable finds it. **Not deployed:** selling at first refresh is the round-trip spread paid on every entry — an entry-cost measurement, not a capital gain — and it would erase the post-veto day0 kept set (+0.029/unit). Memory: `exit-rule-cannot-fix-entry-book`.
- **Ships (worktree, in flight):** (1) `Position.evaluate_exit` evaluates the stop on the served market-anchored corrected probability instead of raw `last_monitor_prob` (one argument; fail-open to raw; +$227 [−117, +587] on the cohort; rationale = entry and exit act on the same calibrated q); (2) calibrator training rows weighted 1/claim-count (hygiene; guards the 6.5× certificate duplication should the training surface ever change).
- **Today's landings on the daemon line (all verified, entries resumed):** `d2df69d05` NO-side α sign; `a6aaa648b`/`214326fa0`/`5707034b6` exit execution; `97ca7531d` M5 latch on same-token re-entry; `d3a49f9f6`/`52867f2af` day0 diurnal nowcast veto. Rollback chain 2a62883ab → d2df69d05 → 5707034b6 → 97ca7531d → 52867f2af.
- **Where the edge is not:** NWP-cycle lead (market leads), settlement calibrator (= market), exit timing (negative drift, no observable), latency (chain is fast). **Where it is:** the day0 observation channel — Zeus's belief moves 3 min before the market on prints, but reacts 7× too hard and only 52-58% same-sign. The nowcast veto removes the −82% tail of that lane; the next build is the reaction-magnitude model (station diurnal residual sized against the market's own revision), promoted only on post-fill executable markout LCB > 0 in a precommitted cell (consult framework, REQ-20260904-011326-2892f6).

### Item 36 addendum 4 (2026-09-04 21:30Z): sixth landing; storage watermark; end-of-day state

- **Landed `3211616de` + `c5a4d9933`**: `Position.evaluate_exit` now evaluates the stop on the served market-anchored probability (fail-open to raw q; `exit_q:market_anchored|raw` recorded in applied validations; provider reached through a module accessor registered by the global batch, no DB open in the monitor path); calibrator training rows weighted 1/claim-count. Post-restart refreshes show `exit_q:raw` until the first global batch registers the provider — by design; a watcher confirms the flip.
- **Riskguard DATA_DEGRADED 13:00→21:20Z was `storage_capacity: LOW_DISK / ENTRY_RESERVE_BREACHED`** (free 8.9% < 10%), not a model state; two Time Machine local snapshots. `tmutil thinlocalsnapshots` recovered 27 GB → GREEN within one tick. `state/` is 378 GB (zeus_trades 186, zeus-world 93, zeus-forecasts 83); `executable_market_snapshots` alone adds ~55k rows/day. **Operator item:** retention on executable_market_snapshots / MONITOR_REFRESHED payloads / execution_feasibility_evidence, or the reserve breaches again within days and silently blocks entries.
- **Verification state:** veto stamp confirmed on the first remaining_day certificate (London, q_held 0.345 city cell, not vetoed), 0 tracebacks; sign fix confirmed on buy_yes (α +0.09, refit n=977), buy_no still unseen today (cluster 16:00-03:00Z; watcher armed); exit fixes verified (1 correctly labelled no-bid reject, 0 regressions, SELL 3/3 filled).
- **Today's book:** 5 settled −$5.98; open 2 day0_window $17.9 + 3 pending_exit $5.26; entries: 1 fill since 10:27Z. Three stacked entry suppressors today: riskguard e-value gate on forecast_qkernel_entry (correct), the storage watermark (fixed), and the M5 latch (fixed).
- **Daemon line end of day:** `c5a4d9933`. Rollback chain 2a62883ab → d2df69d05 → 5707034b6 → 97ca7531d → 52867f2af → c5a4d9933.
- **Storage retention (operator decision, nothing pruned):** on-disk by table — zeus_trades: executable_market_snapshots 49 GB, decision_log 28 GB, execution_feasibility_evidence 14 GB, position_events 7 GB; zeus-world: opportunity_events 37 GB, no_trade_regret_events 14 GB, execution_feasibility_evidence 11 GB; zeus-forecasts: forecast_posteriors 45 GB, calibration_pairs 12 GB (+6 GB index). Growth: snapshots ~55k rows/day, MONITOR_REFRESHED ~0.5 GB/day. Candidates that touch no decision path: snapshots >90d, feasibility evidence >14d (both DBs), opportunity/no-trade-regret events >60d (riskguard shadow rows need ≤7d), calibration_pairs if unread. ≈150 GB reclaimable; the 10% reserve (99 GB) is 11 GB above current free space.

### Item 36 addendum 5 (2026-09-05 00:35Z): operator "全程、不准等" — both measured leads tested as ENTRY laws and refuted; maker-side test running

- **Observation reaction as an entry law (day0), measured on 297 book-covered raising METAR prints (14d):** naive "buy the new floor bin at the ask" −6.2¢ @+5m, −6.8¢ @+15m (CIs exclude 0); nowcast-pmf-driven rule (buy YES where q_react − ask > 0.05, buy NO where bid − q_react > 0.05, bins rel 0..3) negative in every (side, rel, horizon) cell with excluding CIs (NO rel0 +240m −23¢; NO rel2 +60m −7.4¢; YES rel1 +15m −3.9¢). The market's post-print price beats both our posterior and the station climatology within hours. Veto stands; entry law does not.
- **NWP-release drift trade (all leads), scratchpad `nwpdrift/REPORT.md`:** 46,189 capture events, 3,968 with a causal book; taker entry +3m → −4.5¢ @+60m [−7.2, −2.8]; 0/18 cells LCB > 0; direction capture 42.6% (< coin); bigger innovations lose more; κ(+60m) = 2.15¢ per unit Δq vs median |Δq| 0.082 ⇒ 0.18¢ expected response vs 2.0¢ spread (30× short); regional models κ 2.8× global, still negative; hold-to-catch-up never turns positive. Only condition that changes the arithmetic: a family whose spread compresses below ~0.3¢.
- **Conclusion so far:** our lead exists in belief space (we move first on prints and on captures) and is worthless as a taker because the executable response is an order of magnitude smaller than the spread. Remaining untested monetisation: MAKER posting on the innovation side (earn the spread instead of paying it); fill-rate and post-fill markout measurement in flight (`makerlead`). If that also fails, the honest statement is that Zeus's information sources do not beat this market's price at any lead, and the profit lever is not in the decision chain.
- **Operations tonight:** a second session (zeus-53) cherry-picked 3d761227b and daf63b3d5 with a mesh restart each; each restart arms the entries pause guard, which cannot prove green while every open position is on a closed/dead market, so entries stayed paused until CAS-resumed (twice). A pause-guard watcher now CAS-resumes any restart-guard pause older than 3 min when runtime is green and all blocking positions are closed-market (operator's no-idle order). Guard design gap queued: closed-market positions must count as settlement-recoverable regardless of payload shape.

### Item 36 addendum 6 (2026-09-05 01:20Z): the hunt is complete — every mechanism measured; where the money is and is not

Measured today on 47-60 days of Zeus's own data, all leads, cluster-bootstrapped by target date, post-fill executable markout as the only promotion statistic:

| mechanism | result | report |
|---|---|---|
| NWP-cycle posterior vs price (day1/2) | market leads at every horizon; Brier(q)−Brier(p) +0.044 | convergence/REPORT_V2 |
| Observation reaction as ENTRY (day0), naive and nowcast-pmf | negative in every cell, CIs exclude 0 | ledger §PMF |
| NWP-release drift, taker (all leads) | −4.5¢ @+60m, 0/18 cells LCB>0; κ·‖Δq‖ ≈ 0.2¢ vs 2¢ spread | nwpdrift/REPORT |
| NWP-release drift, maker-posted | −4.4¢ net, 0/12 cells | makerlead/REPORT §4-5 |
| Settlement calibrator variants (intent population, lead×side) | none beats the market price; dedup weight only | intentcal/REPORT |
| Exit continuation law (π_fill, μ(h)) | worse than immediate exit; no exit-time observable predicts keeps | exitlaw/REPORT |
| Latency (any hop) | not a constraint (+1 min = +3 min) | latency/REPORT |
| Resting longer instead of cancel-and-cross | execution saving +3.7¢/share real; but the counterfactual fills mark −3…−10¢ (negative book) | makerlead §3, ledger §REST |

Standing conclusion: Zeus's information sources do not beat this venue's price at any lead; every lever found today reduces loss (α sign, exit execution, latch, veto, exit consistency) rather than creating gain. The engine's own riskguard e-value gate independently closed `forecast_qkernel_entry` on the same evidence. The live book is the day0 lane behind the diurnal-residual veto (replay: kept set +0.029/unit, CI not established).

What would change this: a spread compression below ~0.3¢ on any family (drift becomes tradeable), or an information source the market does not have (a station/obs feed or model the price demonstrably lags by more than the spread). Neither exists in the current data.

Landed today on the daemon line (mine): d2df69d05, a6aaa648b/214326fa0/5707034b6, 97ca7531d, d3a49f9f6/52867f2af, 3211616de/c5a4d9933. Foreign (zeus-53, no coordination): 3d761227b, daf63b3d5, 967938219, ffa4f4b7a — each with a restart that re-armed the pause guard; a watcher now CAS-resumes stale restart-guard pauses when runtime is green and every open position is closed-market.

### Item 36 addendum 7 (2026-09-05 01:45Z): chain-latency defect found (posterior waits for ENS); not a separable lever

- The materializer refuses to build a family posterior on a new deterministic cycle until the ENS snapshot for that cycle exists (`replacement_forecast_live_materialization_queue._seed_source_cycle_boundary` → `awaiting_current_ensemble_hwm`; 684 deferrals/24h). ENS lands 6-7 h after issue time (18Z ENS available 01:16Z next day) while deterministic single_runs land ~2 h earlier; the HWM gate then blacks out any family whose served posterior cycle is older than the latest ENS cycle. At 01:10Z, 88/89 families for target 09-06 served a belief a mean 10.9 h behind the latest ECMWF cycle. The design lane's "78% of family-cycles ineligible" is this mechanism.
- P&L attribution (3,053 settled lead-1/2 certificates since 08-01, $1/unit): certificates decided on a stale cycle do worse on day1 (buy_no −0.39 vs −0.19; buy_yes −0.25 vs −0.10) and better on day2 buy_yes (+0.27 vs −0.03); price-decile matched difference +0.05 with mixed signs. Not separable from the underlying negative signal. Queued as a hygiene fix (two-stage materialization: deterministic-first posterior, re-fuse on ENS arrival; never serve a posterior older than the latest deterministic cycle for entries), not promoted as a lever.
- The design lane's "+6.02¢ lead0 cell" is a mid-based markout on posterior ticks; the same lead0 measured in executable prices is −5.7¢ (nwpdrift). Mid ≠ fill. No lane.
- Foreign landings continued (967938219, ffa4f4b7a, f2fc1f0aa, 48516245d); guards self-reset or were CAS-resumed; entries open, GREEN.

### Item 36 addendum 8 (2026-09-05 03:20Z): latency lever closed; upstream throughput defect is the Open-Meteo daily quota

- **ENS-landing → posterior latency has no executable value.** Paired price of acting at the landing instead of at the commit: −0.01¢/share (95% CI [−0.15, +0.16], n=1,041; 2,687 with settlement) against a 2.0¢ median spread. The >2 h blackout cell (+1.43¢, n=70) fails its own placebo (same window ending 60 min before the landing: +4.40¢). Entering an hour before the data existed is indistinguishable from entering after the posterior (+0.04¢ [−0.65, +0.77]). The Δq-direction trade loses at every timing (−4.75¢ @+60m, −3.20¢ to settlement, no cell with a positive lower bound). Mean landing→posterior lag is 0.46 h over 10,661 family-cycles (6.2% > 2 h), not the 1.3 h first estimated. Two-stage materialization and queue re-ordering are not levers. Lane report: scratchpad enslag/REPORT.md.
- **Only positive signed window:** deterministic anchor available → ENS landed, +1.27¢ mid-move (lead0 +2.04¢ [+0.85, +3.42]); spread-free, below the spread, not shown capturable.
- **Upstream throughput defect (measured):** the free Open-Meteo tier (10,000 units/day; tracker hard cap 9,500) is exhausted daily (cap onset 14:12Z 09-02, 07:22Z 09-03, 10:44Z 09-04), after which only held families download anchors until 00:00Z. Without an anchor no cycle-advance seed is built, the family keeps serving the previous ENS cycle, the HWM gate blacks it out, and the auction scope collapses (<60 families for 4.7–13.8 h/day). Spend on 09-04Z ≈ 6,600 log-derived units: single_runs ukmo_global 1,448; previous_runs kma_gdps 1,089 (+~330 batched) — a model present in 0 of 10,940 served posteriors and with 0 rows ever persisted in raw_model_forecasts, so the immutable-value skip never matches and each (city, target, lead) is re-fetched ~7×/day; icon_d2 503; icon_eu 486; archive hourly obs 458; ecmwf_ifs anchor 321; ukmo_uk_2km 183 incl. 168 HTTP 400 probes for non-existent hourly runs; health probe 144; sunrise/sunset 108 (offline solver exists). Bucket rung (S3, unmetered) serves 34 whitelisted cities of 54 and carried 06Z/12Z after the cap.
- **Operator decision:** Open-Meteo paid tier removes the cap outright. In-law code levers, none a profit lever: stop or persist the kma_gdps fixed-lead fetch; offline sunrise/sunset; free meta gate before ukmo_uk_2km single-runs; anchor download ahead of the extras lane.

### Item 36 addendum 9 (2026-09-05 04:00Z): Open-Meteo quota waste — two fixes landed

- **Mechanism 1 (76% of single-runs units on 09-04):** the source-clock download plans every lead for every in-domain model; a target local day beyond the run's 120 h coverage yields a parser gap (`exact_run_unmaterializable`), no row persists, so nothing skips it; unless every scope is a gap the source reports TRANSPORT_RETRYABLE, the cursor does not commit, and the 15 s tick re-fetches the identical URL (gem_hrdps NYC run 09-03T18 ×96 in 5 h, all HTTP 200). Landed 905eb6616 `fix(data): memoize run-unmaterializable single-runs scopes`: per-run memo, memoized scopes skipped without HTTP and still reported so the source reaches PERMANENT_FAILURE (a cursor-commit status); status guard keyed on the gap set so an all-memoized pass cannot claim RAW_INPUTS_DOWNLOADED.
- **Mechanism 2 (~1,450 units/day):** kma_gdps/kma_ldps previous-runs legs; provider returns all-null series (probed at every lead), 0 rows ever persisted, 0 served posteriors reference them, so the immutable-value skip never matches. Landed 86a9cd929 `fix(data): skip previous-runs legs the provider never serves` (PREVIOUS_RUNS_UNSERVABLE_MODELS).
- Verified 210 tests green; 10 pre-existing failures identical on live HEAD (scheduler adapter, extras health surfacing, cycle availability, run selection) left untouched. Restarted data-ingest + forecast-live only. Acceptance: no daily cap onset on 09-05Z (previously 07:22Z–14:12Z); single-runs repeat ratio ≈ 0; auction scope stays >100 families through the afternoon.
- **Open (peer session):** a `restart all` at 03:44Z armed the deploy guard for 0058b6757 but live-trading stayed on f162ae1dc; entries paused by `deploy_live_restart_guard` since, and the monitor proof is red because canonical monitor appends on zeus_trades.db are deferred/timing out (0 successes in 20 min). Peer notified; not touching live-trading for 30 min.

### Item 36 addendum 10 (2026-09-05 04:35Z): entries open again; writer-hog diagnosed and cleared by the peer's restart

- **Cause of the 03:44Z–04:31Z entry pause:** the live-trading daemon (pid 21351, booted 03:13Z) held the zeus_trades.db WAL write lock continuously (fcntl F_GETLK on the -shm WAL_WRITE_LOCK byte named pid 21351 in 80/80 polls over 40 s while the WAL did not grow). Its own held-position monitor appends were deferred by the lock it held, so the restart-guard proof could never go green, and every foreign `restart all` since 03:44Z re-armed a guard it could not clear. Sidecars logged 100+/min `database is locked`, which was a symptom, not a cause.
- **Resolution:** the peer session landed 33a5f6462 + ab10f1f9b (deploy: hand off stale no-exit monitors; re-fetch the held-side book from the venue when the stuck writer cannot persist it) and restarted live-trading at 04:27Z. After the restart the write lock is free, sidecar lock errors are 0/5 min, monitor appends land, the guard retired at ~04:32Z, entries are open, riskguard GREEN. The parked-transaction thread inside 21351 was not pinned (three SIGUSR1 dumps showed no thread inside sqlite while the lock stayed held).
- **Quota fixes (905eb6616, 86a9cd929) verification pending** at the next source-clock cycle (00Z deterministic ~05:35Z, ENS ~07:50Z): expect memoized-gap log lines, zero kma_gdps previous-runs calls, single-runs repeat ratio ≈ 0, and no daily-cap onset (quota.log watcher records every 10 min).

### Item 36 addendum 11 (2026-09-05 08:00Z): trade-DB write-lock hog named and removed; forecasts-DB scans indexed

- **Correction to addendum 10:** the parked-transaction verdict from single SIGUSR1 dumps was wrong. A 10-15 min `F_GETLK` ledger polled at 50 ms and summed per holder pid (06:51-07:06Z, live-trading pid 85752: 467 s of 900 s held, single holds 58 s and 237 s), dumps at 3 s and every 40 s of a hold, and hold end-times aligned with the daemon log (the 330 s hold 06:39:53-06:45:23Z ended on the exact "M5 WS-gap reconcile failed closed" line) named the holder: `venue-background-maintenance` → `_run_ws_gap_reconcile_if_required` (src/main.py) → `run_three_phase` apply → `_bounded_write` BEGIN IMMEDIATE → `run_reconcile_sweep`. Inside that transaction: `_journal_positions_by_token` twice (6.6 s cold each), `_local_order_is_open` over 3,679 venue_commands, and `_is_market_in_zeus_domain`'s `SELECT COUNT(*) FROM executable_market_snapshots` (11.9M rows, 13.3 s cold). The final `clear_after_m5_reconcile` reads the raw module singleton, which in the order daemon never leaves the clean-boot `DISCONNECTED:not_configured` state (healthy sidecar evidence only reaches `summary()`), so every run raised and rolled back: 22 runs on 09-05, 116 on 09-04, 598 on 09-03, zero durable effect. It fires whenever the sidecar evidence is momentarily stale (180 s freshness), i.e. after every restart. Consequences observed: exit_monitor `ARTIFACT_WRITE_DEFERRED`, `CANONICAL_MONITOR_REFRESHED_RETRY_DEFERRED` (76 in the 25 min before the fix), restart-guard proof never green, entries paused. Write leases and `bounded_sqlite_write` bound acquisition (busy_timeout=0) only; reads inside BEGIN IMMEDIATE hold the writer for their full duration.
- **Landed f2b44d495** `fix(live): skip M5 sweep the order daemon cannot clear`: `ws_gap_guard.m5_reconcile_can_clear()` mirrors the exact clear precondition; the runner returns `{"status": "skipped", "reason": "m5_latch_not_clearable"}` before phase 1 when it cannot clear; `_is_market_in_zeus_domain` probes with `LIMIT 1`. Two tests added (tests/test_heartbeat_supervisor.py); 93 passed across the four pinned files, one pre-existing failure identical on live HEAD. After (pid 32462, 120 s ledger): 5.2 s held, max 3.1 s; deferrals 0 in the first 5 min; entries resumed 07:45:39Z.
- **Landed cafc73e92** `fix(data): index the forecasts DB reads held under the LIVE flock`: forecasts write-lock ledger 07:11-07:15Z showed live-trading holding 137 s of 240 s (single hold 92 s) in belief-reseed threads at `latest_baseline_coverage_for_replacement_seed` (bare `c.city` predicate → readiness index, walks every LIVE_ELIGIBLE row; 45 ms warm, seconds cold, per reseed per family, after `_delete_missing_owned_cycle_advance_marker` opened the write transaction) and in `persist_day0_hourly_vectors`' prune `DELETE … WHERE captured_at < ?` (no captured_at index, every 45 s under the LIVE flock). Fix: `idx_day0_hourly_vectors_captured_at`; the coverage query now filters on the canonical `city_id` (upper-snake name, the production writer's rule) plus the configured `city_timezone`, hitting `idx_source_run_coverage_scope` (0.4 ms). Fixture in tests/test_replacement_forecast_seed_discovery.py corrected (it echoed the display name as city_id). Pre-existing failures in the touched suites (14 in test_day0_remaining_day_pricing, 2 in seed discovery, 1 in cycle availability) identical on live HEAD.
- **Quota fixes at the real cycle (addendum 9):** the exact-run memo is process-local and data-ingest booted 17 times today (peer landings), so repeated `icon_d2 Amsterdam 2026-09-07` gaps across boots are expected, not a defect; the memo only pays off with daemon uptime longer than the source cycle. `kma_*` calls: 0 since landing. Remaining single-runs repeats in the last hour are HTTP-200 re-fetches of identical URLs (ecmwf_ifs 00Z ×3, ukmo_global 18Z ×2) that line up with ingest restarts, plus HTTP-400 `run_not_published` conditional retries (9 today vs 28 yesterday). Both are restart-storm costs, not new code paths.
- **Anchor lag is the ENS wait, not a fault:** ifs9 anchors land 7.3-7.9 h after each cycle (18Z→01:27Z, 00Z→07:56Z on 09-04); absence of 00Z anchors before ~07:50Z is the B137 ensemble HWM wait, already measured to zero executable value (addendum 8).
- **Open, operator-level:** peer restart cadence (~10 min) remains the throughput ceiling for the materializer (seeds 1,100-1,400 pending, scope 5-13 families); Spotlight still indexes state/ (no `.metadata_never_index`); disk at 130-280 MB/s sustained.

### Item 36 addendum 12 (2026-09-05 09:00Z): residual trade-lock holder was the inline retention walk; landed 96fb1a947

- **Correction to addendum 11's "inline expiry is secondary":** after f2b44d495 and cafc73e92 the 8-minute `F_GETLK` ledger on the new live pid still showed 113 s of 480 s held (9 holds >1 s, max 18.7 s), and every dump inside a hold was the exit-monitor artifact persist thread at `decision_chain._inline_expire_decision_log`. The earlier "0.08 s in isolation" replay was wrong because it re-walked the daemon's own cursor region (already in the OS page cache); on a region the daemon had not touched the same 500-row page cost 1-2 s, and 15 s under background I/O policy. Mechanism: the walk used the timestamp index and read `mode` from each row; every lookup landed behind another mode's 89-262 KB `artifact_json` overflow chain (2.1-3.6 ms per row cold). The exit_monitor cursor sat at 2026-07-25 with 60,438 rows of other modes still ahead of it, so ~120 more page walks (1.8 h of artifacts) remained before it could wrap, and the expired exit_monitor rows (08-28) were newer than the cursor, so nothing had been deleted for the whole cost.
- **Landed 96fb1a947** `fix(state): walk decision_log retention on a mode index` (worktree 2085d0d1c): `idx_decision_log_mode ON decision_log(mode)` added to `_TRADE_CLASS_DDL` and built at boot by `init_schema_trade_only` (leaf pages only; a `(mode, timestamp)` build would walk every overflow chain, 238 s measured). The walk now takes its age bound as the rowid of the newest row below the cutoff (one timestamp-index probe), descends the mode index below it index-only (LIMIT 50), and the DELETE re-checks `timestamp < cutoff` so a backfilled old-dated row above a fresh one cannot expose it (0 rowid/timestamp inversions over the live table). No-op until the index exists. tests/test_decision_log_inline_expire.py rewritten (17 pass); schema fingerprint re-pinned; wide suites (run_mode_failure_surfaces, w3 seam, unknown_side_effect, ddl copies, p2 byte equivalence, exit_safety): 49 failed in the worktree vs 50 on live HEAD, no new failures.
- **Verification pending in this addendum's follow-up:** post-landing 5-minute ledger on the new pid, `ARTIFACT_WRITE_DEFERRED` count, and cursor progress on the exit_monitor mode.

### Item 36 addendum 13 (2026-09-05 10:30Z): materializer priority lane wedged by a zero-progress fast path; two fixes landed

- **Symptom after addendum 12:** both DB write locks quiet (trade 0.1 s/60 s; forecasts 3 s/180 s) yet posterior commits fell to 0 per 10 min from 09:00Z, 31-100 requests accumulated on disk, and the held Hong Kong 2026-09-04 families went unrefreshed. Every pending request classified as priority (`_priority_map_with_names`), so only the priority lane could claim them, and that lane logged nothing after 08:15Z while its scheduler job ran every second.
- **Cause:** `_replacement_forecast_priority_materialize_job` returned the station-revision fast lane's report whenever it was not `NO_SEEDS`. The newest station seed (Hong Kong low 2026-09-05, cycle 00Z) is ahead of its eligible ENS frontier because the 00Z HK-low ENS row landed `REJECTED_BOUNDARY_AMBIGUOUS` (`AMBIGUOUS_CROSSES_LOCAL_DAY_BOUNDARY`, overlap 15 h), so the frontier stays 18Z; the fast lane correctly defers that seed in place and returns `DEFERRED` with `SOURCE_CYCLE_AWAITING_ENSEMBLE_HWM`. The job returned that report on every tick. Replays on scratch copies: priority claim (limit=1, as the daemon calls it) claims 1 request in 0.06 s; the fast lane returns DEFERRED in 0.01 s. Provenance: 3bdf5e5a8 + a2d5e41d1 (09-04 23:55 / 09-05 00:19) added the fast lane and the status-based early return; tests pinned only PROCESSED/NO_SEEDS.
- **Landed 730d2a99b** `fix(forecast): let a deferred station fast lane fall through` (forecast-live + data-ingest restart only). Within 3 min the lane ran but into the seed-first tranche (`seed_limit=3`): with ~2,000 seeds queued it moved three unrelated seeds per tick and returned early, requests grew 37 → 100, still 0 commits.
- **Landed 6d12a33eb** `fix(forecast): claim requests first while a station seed defers`: a DEFERRED fast lane now takes the request-first path (`seed_limit=0`) a tick without station seeds already uses. Within 4 min: 67 requests claimed and processed, priority lane PROCESSED ×64, 12 posteriors committed in 3 min (was 0 per 10 min), requests 100 → 49, auction scope 6 → 11 families. Test `test_priority_job_falls_through_when_station_seed_awaits_ensemble` pins request-first; 89/89 in tests/test_day0_extreme_updated_materialization_bridge.py.
- **Still open on the held Hong Kong 2026-09-04 positions:** their requests resolve to `BLOCKED_MISSING_PROBABILITY_AUTHORITY` (day0 carrier-vector preflight) and the monitor reports `GLOBAL_HELD_PINNED_COMPLETE_POSTERIOR_BLOCKED:REPLACEMENT_LIVE_CYCLE_AGE_EXCEEDS_BOUND` for a local day that ended 18 h ago; settlement state being read in the follow-up.
- **Heuristic recorded:** a "fast path first" job must short-circuit on progress, never on `status != NO_INPUT`; classify pending files by lane and check that lane's log cadence before blaming throughput.

### Item 36 addendum 13 follow-up (2026-09-05 10:45Z): verification and the held Hong Kong positions

- **Materializer after 6d12a33eb:** 108 live posteriors recorded 10:28-10:43Z (63 families in the first 12 min; per-minute 1-18), requests 100 → 0, auction scope 6 → 36 families, 102 global auctions in 15 min. Both DB write locks remain quiet apart from one 11.6 s trade-lock hold in a 60 s sample being classified in a follow-up ledger.
- **Held Hong Kong 2026-09-04 positions (2× day0_window, local day ended 16:00Z 09-04, ≈$17.9 exposure):** the monitor's pinned-complete read is BLOCKED by `REPLACEMENT_LIVE_CYCLE_AGE_EXCEEDS_BOUND` (12 h source-cycle bound; the family's newest posteriors consumed 00Z/06Z 09-04, computed 15:59Z 09-04), rebuild requests resolve to `BLOCKED_MISSING_PROBABILITY_AUTHORITY`, and the day0 hard-fact exit returns None for `settlement_source_type != "noaa"` (Hong Kong is `hko`). Observation authorities disagree: the VERIFIED `hko_realtime_api` row says high 30.0 / low 26.0; the `hko_hourly_accumulator` fact (143 samples) says 31.0 / 26.4. Under the first, NO-on-31 wins and NO-on-26 loses; under the second both lose. Both venue markets are still active and accepting orders. Nothing in current law lets the monitor act on an hko-settled complete day; they retire at Polymarket settlement via the harvester. Not touched; flagged to zeus-53.

### Item 36 addendum 14 (2026-09-05 22:45Z): live_venue_submit silently closed 12 h by deployment_freshness_mismatch — fixed, law recorded

- **What happened.** The two materializer fixes (730d2a99b 10:15Z, 6d12a33eb 10:23Z) were landed with an ingest-only restart (data-ingest + forecast-live). live-trading kept running on boot SHA 96fb1a947 while HEAD moved; `src/control/runtime_code_plane.py:13` classifies every `src/` path as runtime, so `gate_runtime._deployment_freshness_mismatch` (src/architecture/gate_runtime.py:88) closed cap `live_venue_submit`. Posture looked healthy throughout: riskguard GREEN, entries un-paused, auctions selecting winners; the only observables were `deployment_freshness_mismatch` in the winner-actuation warnings and boot SHA ≠ HEAD. First refusal 14:34:53Z, last 22:19:11Z; 35 distinct winner events refused at the submit seam; zero venue calls; zero entries on 09-05 UTC before the restart.
- **Fix.** Removed an orphaned 0-byte `.git/index.lock` (mtime 11:53Z, no git process across 3 samples), then `scripts/deploy_live.py restart all --allow-unpushed` (22:23:15Z→22:25:01Z, rc=0). Whole mesh on 6d12a33eb; gate reproduces False with boot=HEAD; entries un-paused; two Singapore 2026-09-06 HIGH buy_yes entries filled within 90 s of boot (22:25:59Z, 22:26:54Z); 0 gate hits since.
- **Same window, designed outcomes (not defects):** 331 `DAY0_DIURNAL_NOWCAST_VETO` on 88 candidates (veto d3a49f9f6, first live day with hits), 27 `TAKER_QUALITY_PROOF_NOT_PASSED` negative surplus, 8 wealth-superseded.
- **Law.** Any landing that touches a runtime-plane path ends with a full-mesh restart; the ingest-only landing script is retired. Memory `partial-mesh-restart-arms-freshness-gate` records the check: reproduce the gate with the live process's boot SHA before reading "no fills" as no edge.
- **Held Hong Kong 2026-09-04 positions:** dead by price (low-26 NO bid 0.001 on 43.2 sh / $14.95; high-31 NO no bid on 5 sh / $2.95); they retire at settlement. The terminal-partial repair for exit command b3ac5741 loops every recovery pass because the chain `cost` is 4-dp (src/data/polymarket_client.py:1570) against a 1e-6 tolerance (src/execution/command_recovery.py:12921); the failure only increments a summary counter and no gate consumes it.

### Item 36 addendum 15 (2026-09-06 00:40Z): main line resumed — quota defect landed; chain-truth scoreboard corrected; the loss gate that cannot fire

Operator (2026-09-05 23:40Z): the Open-Meteo limit "按照预期设计来说不应该 hit", a separate defect; the main line stays "概率计算领先市场 → 优势转变利得".

- **Quota, landed as one batch (00:28Z, live 27b39af68 + fdc5e434b, restart all rc=0).** The code's own budget test states the design ceiling (`tests/test_openmeteo_call_budget.py` SANE_DAILY_CEILING = 2,000/day); actual was 9,000+. Two leaks: (1) `ukmo_uk_deterministic_2km` was absent from the 3-hour single-runs grid list, so its hourly off-grid runs returned 400 and were retried forever (~100 calls/day); (2) the dominant one — `_EXACT_RUN_UNMATERIALIZABLE_MEMO` (bayes_precision_fusion_download.py) was a module-level dict, so live-trading and data-ingest each re-paid every proven-immutable (model, city, date, run) gap and every restart (9 SIGTERMs on 09-05) forgot all of them; the tracker showed it as successful keys with attempts=14, owner_pid alternating (≈1,400 units per 5 h). Fix persists the memo to a fcntl-locked JSON under state/, memoizable-reason whitelist unchanged, no throttles. 71 tests pass. Verdict = 2026-09-06 day_count.
- **Chain-truth scoreboard (fills deduped per command by round(size,6), real-tx price; payout outcome_index 0=YES/1=NO verified on 2,295 rows).** 21 d by target_date n=292: net −$308 [90% −562, −73], win 32.5%; 7 d −$33. `forecast_qkernel_entry` n=180 net −$25 [−124, +75] (riskguard e-value gated since 09-04 09:37Z); `day0_nowcast_entry` n=112 net −$283 [−484, −95], 7 d −$33 [−58, −12], win 17% — the only lane trading today and the loser. A first pass keyed on `(command_id, filled_size, fill_price)` double-counted 2-dp/28-dp price echoes of the same fill and showed the qkernel lane at +$142; that number is void.
- **Served q (servedq, window 08-28→09-04 07:46Z, n=108):** Brier(q_corrected) − Brier(fill price) = +0.094 [+0.057, +0.135], worst in q's top bucket (q̄ 0.888, market 0.496, realized 0.200). After the 09-04 landings: 4 fills, 1 settled — no power yet.
- **The loss gate that cannot fire (8th exists-but-unwired instance).** riskguard `_revision_probation_gate_reason` (riskguard.py:4648) would close `day0_nowcast_entry` (reproduced now: 14 realized closes, net −$16.92, capital_truth_degraded) but requires `capital_curve["selection_revision_bound"] is True` (4703); the curve builder initialises it False (3178) and nothing in riskguard sets it. The only setter is `scripts/evaluate_current_regime_capital_advantage._bind_live_curve_to_global_revision`, run as a post-trade-capital child that riskguard never reads — and that child is killed at its 75 s deadline (evaluator wall time 2:47), so its artifact froze at 09-03 20:32Z. The alternative law, the market-relative alpha e-value, only sees shadow rows written for candidates rejected with `risk_action:gate` (global_batch_runtime.py:5406-5415), i.e. only while the strategy is already gated: circular. Tests pass by hand-building bound curves (test_riskguard.py:6295-6402). **Not wired:** doing so closes the only lane until e-value ≥ 10, i.e. indefinitely under the current belief; the lever is the belief, not a gate. Operator decision.
- **Served day0 belief, measured now:** the carrier's path sigma (`day0_effective_path_sigma_c`) is flat in hours-to-peak (HIGH mean 1.2–1.3 at every k) while the floor-bin bias is +0.24…+0.48 at k=0..1 and negative at k≥4 (addendum 2's diurnal A1). The residual term named as fix step (3) is still absent. resmix (walk-forward q_model × nowcast-residual combiners scored against the market, executable consequence on the same candidate pairs) decides whether it is worth building.
- **Dead evidence stores found:** market_price_history (last 2026-05-28), edli_no_submit_receipts (06-29), tier0_candidate_set_provenance (09-04 08:09Z; writer fires only on a real winner), trades-db edli_live_order_events (07-09; the live copy is in the world DB). Any audit of entry quality or veto cost must use trades-db `executable_market_snapshots` (99k rows since 09-04) and world-db order events.

### Item 36 addendum 16 (2026-09-06 02:40Z): quota root cause is upstream replica skew; entry loss is adverse selection on repricing books; one admission cut under walk-forward test

- **Quota (separate defect, operator 23:40Z).** The r1+r2 landing (27b39af68, fdc5e434b) did not move the pace (4,300–7,700 units/day). Direct probe of `api.open-meteo.com/data/ecmwf_ifs/static/meta.json` (8 GETs one second apart, 01:08Z) returned two versions for the same 18Z run (etags 1788654459 / 1788656051, `last_run_availability_time` 00:27:39 vs 00:54:11; one carries `Server: nginx`); during the rollout window the replicas disagreed on the run itself (data-ingest's source clock: ecmwf_ifs 12Z↔18Z five flips in 12 min). Three consumers treat that as "new run": the day0 bundle identity compares the (initialisation, availability) pair (src/data/day0_hourly_vectors.py ~790-800, ~1261) so each flip re-fetches every model for the city (Qingdao ecmwf_ifs captured 7× in 30 min); the ingest cursor `v3:{init}:{availability}:{route}` (src/data/source_clock_update_probe.py `_cursor_for_updates`) fired 641 `SOURCE_CLOCK_UPDATES_CHANGED` on 09-05; the BPF path fetches one immutable run payload once per target date (comment "ONE HTTP call per (city, target_date, cycle)", ~line 2160). Two further flat consumers: the day0 refresh's incomplete-bundle retry re-downloads every successful model when one is unavailable (45 s cadence, day0_hourly_vectors.py:112-114), and `hourly_tick` re-pulls 3 local days × 54 cities every hour = 3,888 archive calls/day by design (40% of the cap; the design ceiling in tests/test_openmeteo_call_budget.py is 2,000 total). Fix in flight (quotafix2 worktree, uncommitted): identity = initialisation time with a monotone persisted per-model pin, cursor v4 on init only, a persisted payload cache keyed by the exact request identity inside `_fetch_single_runs_hourly_payloads_batched` (serves both daemons and both callers), all covered target dates parsed from one payload, need-driven archive pulls. No throttles or caps. Verdict = 9-minute tracker deltas after one batch landing and one mesh restart.
- **Adverse selection, mechanisms (servedq; splits reproduced by me).** Stale-quote lifting ruled out (quote age at fill 3–11 s, JIT_SUBMIT). Own impact / maker: the agent's "zero maker fills" was a join artifact; 34 GTC post-only fills exist since 08-22 and their markout is no better (maker 15 m −4.9¢, 60 m −1.5¢, 240 m −5.2¢ vs taker −4.2 / −3.3 / −1.7; LCBs all negative). The structured finding is same-information reaction: fills whose held-token ask repriced within the prior 10 min (n=22) markout 15 m −5.1¢ → 240 m −12.9¢ (LCB −21.2) → EOD −17.7¢; DAY0_EXTREME_UPDATED-triggered fills (n=25) 3 m −4.2¢ → 240 m −11.5¢ (LCB −13.1); quiet FORECAST_SNAPSHOT_READY fills flat at −3…−4¢. Settlement truth on the same positions (my dedup, cluster bootstrap by target_date, 90%): repriced n=20 net −$51.81 [−85.6, −21.7] on $78.31; quiet FSR n=82 +$42.00 [−28.3, +109.5] on $367. Live instance while writing: Taipei 09-06 high buy_yes filled 01:33:04Z at 0.206, book collapsed, exited 01:41:25Z at 0.097 (−53% in 8 min).
- **What this changes.** The bleed has a decision-time observable (ask repriced in the last N minutes; trigger = live observation print), i.e. an admission cut rather than a q tweak. Under this repo's law it is survivorship until replayed walk-forward on the full window and on candidate books (the 09-04 rejections of α_day0 tuning and price-band bans were sign-unstable). servedq is running that test now: N ∈ {5,10,30} min, ≥2/≥3 distinct asks, by ISO week and lane, false-removal cost, candidate-book grade, confounds (day0 / hour / city / price band). Bar: removed set negative in ≥5 of 6 weeks with pooled LCB < 0 and the kept set not worse than the full set in the last two weeks. Only then does it ship, as one predicate at the submit seam next to the diurnal veto.
- **Still standing:** the residual term inside the posterior does not get built (addendum 15 / resmix); the inert probation gate stays unwired pending the operator; the day0 lane remains the only live lane and the loser (−$283 / 21 d).

### Item 36 addendum 17 (2026-09-06 04:45Z): quota round 3 landed; the day0 ask-repricing cut is admission-grade and ships

- **Quota, round 3 landed 02:54Z (live a09999afd → 05e2e4c1e, restart all rc=0):** persisted payload cache keyed on the exact request identity inside the one fetcher both daemons and both callers use; run identity = initialisation time only with a monotone persisted per-model pin (a stale replica's older run or later availability never invalidates a persisted bundle); ingest source-clock cursor v4 on initialisation time; cache bounded by age and count; `hourly_tick` need-driven from the coverage ledger's `fetched_at` (54×3-day hourly re-pull → at most 54×2×4/day). Verification: 20-minute tracker deltas with (new owner, prior owner, prior outcome) attribution show zero same-process re-issues of a successful key in four consecutive windows; the residual pace (≈2,400/day at 03:40Z) is cache warm-up plus two named consumers — the ensemble-vector fetch after every city bundle (66 units by 03:50Z) and the ncep_nbm standard fallback's (init, availability) check (≈110/day) — both fixed on a worktree branch (34a1a11d3 + one more) and held until the 06Z ecmwf rollout window completes, because a restart resets the warm-up measurement. Design ceiling 2,000/day (tests/test_openmeteo_call_budget.py); yesterday 9,000+.
- **Adjudication of two contradictory lanes.** One lane reported the day0 lane profitable and an AND-rule passing; the other reported the day0 lane negative every week. Per-position chain nets differed by +$1,478 over 782 common positions because the first lane deduplicated venue_trade_facts by trade_id, and the placeholder row's trade_id is literally the tx_hash while the confirmed row carries a UUID, so every exit was doubled and every lane's sign flipped. Memory updated (third dedup shape). The tx_hash-identity numbers and my own third computation agree.
- **The cut (ships).** Day0_nowcast_entry n=315 net −$489 (−0.23, 6/7 weeks). Within it, "held-token best ask took ≥2 distinct values in the 10 min before decision_time": removed n=95 net −$382.53 on $678.89 (net/cost −0.563, 5% LCB −$646, 7/7 weeks negative); kept n=220 −$106.57 (−0.074). Train W30–33 / test W34–36: removed n=50 −$179.12 (−0.679, LCB −$290, 3/3), kept beats full by +0.276; W35–36: 2/2. Reprice direction: all four side × ask-direction cells negative (deepest: buy_no with a falling ask, n=41 −$246.82). ≥3-distinct variants are sharper per fill but fail the weeks bar; N=10 ≥2 is the pre-registered primary. Transfer to forecast_qkernel_entry fails (removed set +$16.85 on W34–36): day0-only, which the seam guarantees. Candidate-book grading (unfilled admitted orders) shows no pre-fill separation — consistent with a fill-conditional harm, and the reason this is an execution-time predicate, not a q change. Implementation in flight (worktree): gate 10 in src/engine/day0_admission.py next to the diurnal veto, stamped before certificate sealing, fail-open on read failure, registered as a terminal no-submit reason, with a replay script that must reproduce n=95 / −$382.53. Expected effect: removes 60–85% of the day0 lane's loss; the kept set is still slightly negative, so the lane is not made profitable by this.
- **Trigger clause rejected.** "trigger = DAY0_EXTREME_UPDATED" removes 314/315 day0 fills; it is a lane ban, not a cut. Whether to run the day0 lane at all remains the operator's decision; the riskguard probation gate that would have closed it is inert (addendum 15).

### Item 36 addendum 18 (2026-09-06 05:00Z): quota round 4 and the day0 ask-repricing veto landed; a storage watermark, not the landing, blocked entries

- **Landed 04:40Z as one batch (live 05e2e4c1e → 264827fc3, restart all rc=0):** `a5285bba9` ncep_nbm standard-fallback meta stamp keyed on initialisation time only; `7d880805f` day0 ensemble fetch skipped when the current bundle is already persisted; `78f263b9b` gate 10 `DAY0_ASK_REPRICING_VETO` in src/engine/day0_admission.py (held-token best ask took ≥2 distinct values in the half-open window [T−10 min, T), stamp `_edli_day0_held_ask_distinct_10min` written from executable_market_snapshots before certificate sealing, fail-open on read failure, registered as a terminal no-submit reason); `264827fc3` replay script (scripts/replay_day0_ask_repricing_veto.py, read-only, calls the shipped helper) and topology registration. Post-landing: freshness gate False with boot SHA = HEAD, entries not paused, zero `deployment_freshness_mismatch` hits, no new exception class in any of the six daemons (every post-boot count at or below the prior hour), 36 tests in the two day0 files green on the live tree. Agent worktrees removed and their branches deleted.
- **Half-open window, pinned.** The study's SQL was syntactically closed (`BETWEEN`) but compared a `+00:00` column against a bare-format string, so the decision snapshot at T never counted; the measured predicate is [T−10, T). A closed bound gives n=114 / −$419.41 and does not reproduce n=95 / −$382.53. The implementer's replay caught my own correction toward the closed bound before it shipped; the semantics the number was measured on are the semantics that ship. Memory `replay-must-call-the-shipped-predicate`.
- **Why no veto has fired yet.** The veto could not be observed because no winner reached actuation after boot: riskguard has been `DATA_DEGRADED driven_by=storage_capacity` since 03:29:13Z (`status=LOW_DISK reason=ENTRY_RESERVE_BREACHED free_ratio=0.0986` against the 0.10 entry reserve), the last ActionableTradeCertificate is 01:55Z, and live-trading logged "EDLI reactor skipped before runtime DB setup: new entries are globally blocked" on every cycle. Cause: one TimeMachine local snapshot on a 926 GB volume at 90% (zeus_trades.db 185.8 GB, zeus-world.db 93.1 GB, zeus-forecasts.db 84.0 GB). Fix applied 04:54Z: `tmutil thinlocalsnapshots / 40000000000 4`, free 91.3 → 123.2 GB (ratio 0.133). Fourth recurrence (08-25, 08-27, 09-04, 09-06): with 363 GB of DBs, any snapshot above ~0.14% of the volume breaches the reserve. The durable fix is retention on the trades DB (scripts/migrations/202608_*_retention.py plus the VACUUM path they document) in an operator-scheduled window; it is a separate defect from the main line, like the quota.
- **Quota, round 4 (verdict pending the full day).** Twenty-minute tracker deltas since round 3 show zero same-process re-issues of a successful key in six consecutive windows; residual spend is first fetches of new (model, run, location) keys during rollouts plus cross-process warm-up after each restart (the live pid changed at 04:40Z, so `prior_owner = 44112` in later windows is warm-up, not a repeat). Pace windows read 2.4k–8k/day because rollouts are bursty; the ceiling test (2,000/day, tests/test_openmeteo_call_budget.py) is the 2026-09-06 `day_count` at 00:00Z, not any window.
- **Open, unchanged:** the kept day0 set is still slightly negative (−0.074 net/cost), so the veto trims the loss and does not make the lane profitable; whether the lane runs is the operator's decision, and the riskguard probation gate that would have closed it stays inert (addendum 15). forecast_qkernel_entry transfer held (removed set positive out of sample). Hong Kong 2026-09-04 pair awaits settlement.

### Item 36 addendum 19 (2026-09-06 06:05Z): the ask-repricing veto is firing on the measured cell; a fifth quota source found by the same delta method and landed

- **Veto, live.** After riskguard returned to GREEN (04:55:59Z) the first auction ran at 04:58:30Z (131 families). Between 04:56Z and 05:45Z: 35 winner actuations, 0 admitted — 25 `DAY0_DIURNAL_NOWCAST_VETO`, 8 `DAY0_ASK_REPRICING_VETO` on 5 candidates, 2 wealth-superseded. The first ask-repricing refusal (05:16:06Z, YES at ask 0.17 with q_mean 0.559 — the overconfident cell from addendum 15) reproduces from executable_market_snapshots on the held YES token: asks 0.15 from 05:09Z, 0.17 from 05:15:55Z, window [05:06:06, 05:16:06) n=8 distinct {0.15, 0.17} → 2 ≥ 2. Zero stamp faults. The stamp itself is observable only on an admitted day0 certificate (the admission raise sits before the caller persists), and none has been admitted since 01:55Z, so the lane is currently fully vetoed rather than trading. That is the expected shape of a lane whose kept set was still −0.074 net/cost: the veto removes the worst 30% of fills; it does not create admissible ones.
- **Quota round 5 (found 05:30Z, landed 05:48Z as live ec5b38d2c, restart all rc=0).** The 20-minute tracker deltas that had shown zero same-process re-issues for six windows showed a new one as soon as 2026-09-08 entered target scope (04:34Z): data-ingest `bayes_precision_fusion_previous_runs_batched`, 13 keys re-fired every 60 s maintenance pass (155 attempts in 6 h, all `success`). Reconstructing the looping request identities from the repo's own model lists and `request_identity()` named two mechanisms, both "HTTP 200 + nothing persists ⇒ the R4a immutable skip never matches ⇒ re-fire": (1) previous-runs-api serves `temperature_2m_previous_dayN` only within a short-range model's own horizon and answers an all-null series past it (probed: icon_d2, arome, dmi, knmi, met_nordic, gfs_hrrr, gem_hrdps, ukmo_uk, nam at previous_day2; jma_msm and icon_2i at previous_day3, both 24/24 one lead lower); `_model_in_domain` deliberately bypasses the polygon lead cap so history accrues at every lead — right for single_runs, wrong for this endpoint; (2) the batched need-check tested a fixed (high, low) pair while the write loop persists only the pass's own targets, so a city with one market (Cape Town, Karachi, Lucknow 2026-09-08) could never satisfy it. Fix: `PREVIOUS_RUNS_MAX_LEAD_DAYS` per short-range model checked on both legs (dropped tag `previous_runs_beyond_horizon`) and the batched need measured on `required_metrics`. Four tests; the 44 related test files fail identically on live and on the branch (56 pre-existing, unrelated files). First 90 s after the restart: previous_runs re-issues 0; the only same-process re-issues are `source_clock_model_meta_*` probes, which are unmetered (quota_cost 0).
- **Ceiling verdict for 2026-09-06 is already lost:** day_count 2,147 at 05:45Z against the 2,000/day design ceiling, dominated by round 5 (≈16k/day pace while it ran). The clean measurement is 2026-09-07's full-day count. The law recorded: any skip keyed on "a row exists" needs a provider-side reason a row can never exist (horizon, domain, unservable model, metric not carried), or it loops forever on a 200.
- **Storage:** the 04:54Z snapshot thinning holds (free 13.3%); the durable fix is trades-DB retention in an operator window (addendum 18).

### Item 36 addendum 20 (2026-09-06 06:25Z): the veto's first full cycle observed on one token; served HIGH center verified uncorrected

- **One token, refused then admitted.** Chengdu 2026-09-06 HIGH buy_yes (YES token 10040926…7358): 18 `DAY0_ASK_REPRICING_VETO` refusals 05:16–06:00Z while the ask moved 0.15 → 0.17 (2 distinct in the trailing 10 min), then admitted at 06:10:38Z on a stable book — certificate stamp `_edli_day0_held_ask_distinct_10min = 1`, recomputed from executable_market_snapshots as [06:00:38, 06:10:38) n=4 distinct {0.28} → 1 < 2. Order buy_yes 10 @ 0.280, venue "matched" 06:10:43Z, position ffe75d47 (day0_window). The same restart-proof path also carried the nowcast stamp (0.490). This is the first live sample of the veto's deferral cost: the fill happened 11¢ higher and 54 min later than the refused book; the study's removed set averaged −0.563 net/cost, so the position's outcome is the first data point on whether the deferral was a false removal. Not to be read alone.
- **Served HIGH center is uncorrected, verified in the posteriors table.** HIGH `forecast_posteriors` rows carrying `center_debias_c`: 2026-09-04 2,131 of 3,020 (mean +0.29 °C, the landing day); 2026-09-05 60 of 2,908 (until the 03:39Z restart on the peer's 0058b6757); 2026-09-06 0 of 1,016. The seam is gone (the materializer sets the field to None unconditionally); no later commit re-added it. Today's winner details show 52 of 101 candidates with q_mean − price > 0.30 (median 0.409 on the Chengdu HIGH family): the overconfident HIGH center the 09-04 landing had measured (walk-forward ΔCRPS −8.1%) is what the veto is now refusing at the book. The removal carried no measurement; re-enabling means re-adding the seam, which is the peer's and the operator's call. Not touched.
- **Quota after round 5:** in a 120 s post-restart sample every metered same-process re-issue after success is zero; the residual re-issues are the model-meta probes (`count_toward_quota=False`). Verdict remains the 2026-09-07 full-day count.

### Item 36 addendum 21 (2026-09-06 07:10Z): quota rounds 6 and 7 landed; the first window under the design ceiling; the veto's deferral cost has its first live sample

- **Round 6 (live 16bf8b6d8, 06:35Z).** The day0 ENS carrier (51 ecmwf_ifs025 members, one unit per city) was persisted only after the deterministic bundle of the same refresh pass had passed every completeness gate; each deterministic `continue` discarded an already-paid complete bundle, so `_current_ensemble_bundle_already_persisted` never matched and the identical request re-issued every priority pass (Los Angeles 19 attempts, Seattle 16, Lucknow 15, San Francisco 9; zero member rows ever persisted for any of them, while a direct fetch parsed 51/51). The ensemble bundle now persists right after its fetch through the same strict selection; the discard, when it still happens, is logged instead of silent. Test fails on base at the persist assertion and passes on the fix; the 21 day0_hourly_vectors test files fail identically on live and on the branch (249 vs 253, the four extra live failures are fee/JIT tests untouched by the change).
- **Round 7 (live 503f29b47, 07:07Z).** With round 6 the discard became visible: "available=51 expected=51" for Los Angeles and San Francisco. The refresh's priority probe (src/events/reactor.py, since 470894bd5 on 2026-08-03) folded each city's remaining-window start to the OLDEST metric boundary. The West-Coast LOW facts froze at 16:53 local (the low was set at 04:55 and re-emitted once on a plateau; the durable event stream emits a metric only when its extreme moves) while HIGH advanced hourly to 20:53. A fetch issued after boundary + past_hours can never cover [16:53, day end], so the pass failed REMAINING_WINDOW_INCOMPLETE, both carriers were discarded, the fetch re-issued each pass, and the coverable metric's day0 authority went dark. Replayed over 2026-09-05's event history: 340 of 1,165 city-hours (29%) had a coverable newer boundary behind an uncoverable folded one (Cape Town 15 h, Chongqing 15, Jeddah 14, Shanghai 14). Consumers prove their own metric's window on read (materializer: `current_state.observed_at`; live health: the event's `observation_time`), so the fetch gate was the only coupling; it now uses the newest boundary. Test fails on base and passes on the fix; the five reactor/day0 suites fail identically (43 pre-existing) on live and on the branch. A past_hours patch on the ensemble request was written, measured against the real boundary ages (21 cities older than 1 h, three at 6.8 h), found unable to help, and discarded without landing.
- **Law recorded (memory):** a fetch gate must never be stricter than the strictest consumer that reads the fetch, and never fold per-metric boundaries; and any skip keyed on "a row exists" needs a provider-side reason a row can never exist, or it loops on a 200. Seven instances of the second law were found today by one method: the tracker's `reissued_after_success` column per job and owner in a 20-minute delta, then reconstructing the looping request identities from the repo's own model lists.
- **First window under the ceiling.** Delta #11 (06:40–07:00Z, the first clean window after round 6): 24 units, a 1,728/day pace against the 2,000/day design ceiling; previous_runs 0 re-issued; the only same-process re-issues after success were the two fold scopes (Los Angeles, San Francisco) that round 7 addresses. Today's day_count (2,504 at 07:00Z) is dominated by rounds 5–7 before they landed and by four full-mesh restarts of my own; the clean verdict is 2026-09-07's full day.
- **Deeper cause, recorded not changed.** A metric's authorized-fact `observation_time` is the time of its last CHANGE, not the last observation; every consumer prices a post-peak LOW's remaining window from the plateau time although later station reports bound it. Moving the causal cut to the last observation is a probability-authority change in the Day0 conditioning domain (2941ff710), with its own measurement; not a quota patch.
- **Veto, first full cycle on one token.** Chengdu 2026-09-06 HIGH buy_yes: 18 `DAY0_ASK_REPRICING_VETO` refusals 05:16–06:00Z while the ask moved 0.15 → 0.17, then admitted at 06:10:38Z on a stable 0.28 book (stamp `_edli_day0_held_ask_distinct_10min = 1`, recomputed from executable_market_snapshots), filled 10 @ 0.280, position ffe75d47. Its outcome is the first live sample of the veto's deferral cost; the study's removed set averaged −0.563 net/cost. One sample decides nothing.
- **Served HIGH center remains uncorrected** (0 of 1,016 HIGH posteriors on 2026-09-06 carry `center_debias_c`; the peer's 0058b6757 removed the seam without measurement). Operator/peer item, untouched.

### Item 36 addendum 22 (2026-09-06 07:45Z): first clean window after round 7; two day0 exits

- **Quota, first clean window after the last landing (delta #13, 07:20–07:40Z):** 12 units, an 864/day pace against the 2,000/day design ceiling, with zero same-process re-issues after success of any metered key; the two ensemble fold scopes from delta #11 are gone. From 9,000+/day on 2026-09-04 to 864/day in a quiet window across seven rounds of one law. Today's total (2,562 at 07:40Z) is the sum of each leak before its landing plus four restart warm-ups; the full-day verdict is 2026-09-07.
- **Two day0 exits this hour, both `GLOBAL_CAPITAL_OPTIMAL_SELL`:** Chengdu ffe75d47 (the veto's first deferral-cost sample: refused 18× at asks 0.15–0.17, admitted at 0.28) sold 10 @ 0.71 at 07:23Z, gross +$4.30 on $2.80; Qingdao 7c1fac44 (buy_no 6 @ 0.47, entered 23:20Z before the veto landed) sold 6 @ 0.93 at 07:37Z, gross +$2.76 on $2.82. Both are chain-confirmed fills; both retire at settlement via the harvester. Two samples decide nothing about the veto; they are recorded because the deferral counterfactual (a fill at 0.15–0.17 on the repricing book) would have made more on the Chengdu sample, which is exactly the shape the walk-forward study says loses on average.
- **Open positions:** Hong Kong 2026-09-04 pair (dead by price, awaiting settlement), Singapore 2026-09-06 HIGH buy_yes 12 @ 0.13 (pending_exit), Moscow 2026-09-03 dust (harvester).

### Item 36 addendum 23 (2026-09-06 11:50Z): the day0 causal cut, measured — last-change and last-observation are the same belief; the bound does the work; last-observation + current-state conditioning wins

- **Replay (scratchpad/boundary, read-only, scripts alongside):** 3,634 (city, date, metric, hour) rows over 48 NOAA cities and local days 2026-09-03…09-05 (the only days with persisted hourly carriers), VERIFIED settlement truth, cluster bootstrap by (city, date), 83 clusters; every cut computed through the shipped helpers. Cuts: A = last change (today's `observation_time`), B = last observation (`read_day0_current_temperature_state`), C = running-extreme bound from station prints, B* = B plus the shipped `remaining_day_extremes_c_with_current_state` conditioning (what the NOAA day0 producer already serves).
- **As conditioning, A and B are the same distribution:** paired CRPS A−B = +0.000 [+0.000, +0.001]; on 331 of the 412 rows where the cuts differ the member sets are identical. **As a fetch window they differ only in coverability:** A is uncoverable on 75 of 1,777 fresh-bundle rows (72 LOW, cut age median 9.1 h), B on none. Round 7's newest-boundary fold was therefore the right direction and not the end: the gate should key on the last observation.
- **The bound does most of the work:** CRPS ≈1.7 without it, ≈0.3 with it; it already equals the settled value on 54% of rows. **B* wins where it pairs:** CRPS 0.307 vs 0.354 (A) / 0.344 (B), A−B* = +0.039 [+0.026, +0.053], integer-bin log score B*−A = +0.052 [+0.038, +0.064], stable across kernels (σ 0.6/1.0/1.5) and hours-to-end buckets; the gain sits in rows with |innovation| ≥ 1 °C.
- **Served posterior on the same rows:** loses to B* on HIGH at every kernel (−1.43 vs −1.16/−1.27 nats, the shape the removed HIGH center de-bias predicts), on stale served rows (> 3 h, 18% of rows) by ~0.55 nats, and on the trade-deciding bound ≠ truth rows (−2.01 vs −1.44/−1.51); wins where the extreme has already happened and on LOW. A spread comparison as much as a cut comparison, so a lead.
- **Read:** the "boundary" is three objects the code conflates — a hard bound, a process state, an event clock — and only the event clock is stored. Contract to build: bound explicit, last observation as the window/process object for fetch gate, materializer and live health. Shippable as a belief change only after a ≥ 2-week replay through the shipped helper with the HIGH center corrected. Sent to the consult thread as a follow-up; the fork's dataset accepts a fourth cut in one function.
- **Gaps recorded:** three local days; persisted captures cover a median 52% of local-day hours (the availability problem rounds 5–7 and the fold fix address); DISPUTED scopes (~13% of 09-04/05) excluded; the bound ignores settlement margin rules.

## Addendum 24 (2026-09-06): goal review — no positive cell at any executable price; three consults verified; two builds refused by precommitted rules

- **Chain truth (tx_hash-deduped fills + settlement_attribution.won):** since 08-27 n=123, cost $518, 31W/79L/13 open, net −$31; by lead since 07-20 settled: day0 lane −$615 (n=259, ROI −29%), qkernel day1 −$403 (−19%), day2+ −$118 (−8%). Weekly net W29→W35 −278/−719/−45/+31/−251/−20/−8 with weekly cost 1,880→235: losses shrank because size shrank, not edge (W35 wr 0.286 vs px 0.292). No cell with win-rate > price. PUSD $222 (08-20: $307–446).
- **Funnel 09-05/06:** winner-actuation 510/306 lines → venue orders 7/1. Refusals: DAY0_DIURNAL_NOWCAST_VETO 685, negative_expected_after_cost 27, DAY0_ASK_REPRICING_VETO 26, wealth_superseded 25, deployment_freshness_mismatch 43 (peer landed 6d12a33eb 09-05 17:20Z without restart). qkernel lane has no entry since 09-02 23:15Z: riskguard's own e-value law holds `riskguard:gate:forecast_qkernel_entry` (model_evalue 0.0095 vs required 10, 12 clusters) — consistent with chain truth, not a defect.
- **Consult REQ-20260906-054302 (veto code review, NO-GO as validated law):** P1 claims verified locally — stale-stamp reuse is latent (actionable payload built fresh per candidate at adapter:22826); logger escape needs a raising handler (live has StreamHandlers); "unbounded history query" refuted on the live DB (idx range scan, 0.01–1.3 ms, 40 tokens median 0.00 ms); whole-second timestamps 451 rows total, 0 of the 317 study decisions; replay rescale branch 0 hits on entry and exit fills; refusal evidence not persisted is true by construction (raise before persist) and reconstructible for 30 d. Veto stays as damage containment, Day0-only; no machinery added.
- **Consult REQ-20260906-054222 (design) + follow-up 055428:** the A→B cut question was wrong as posed — the object is one immutable Day0 observation state (bound witnesses + latest authorized process observation τ + required grid footprint) read by the reactor probe, materializer and live_health; A→B is coverability only (75/1,777 rows, 72 LOW; paired CRPS +0.000); B* gain = process conditioning the NOAA path already serves ⇒ contract unification with a parity property, promoted only by a full-helper walk-forward replay (one-sided 95% LCB>0 on paired final-bin log score, ex-ante population, never the bound≠truth slice). Bound = pushforward max(C,R)/min(C,R) with an atom, never truncate-renormalize. Quota class not closed without a fulfillment plan (SATISFIED/FETCHABLE/UNAVAILABLE/CONTRACT_UNRESOLVED). **Not built:** it moves no cell's P(win)−price; worktree removed unbuilt.
- **Exposure-point test of B* (scratchpad/boundary/TRADE_REPORT.md):** market beats served in every cell (served −3.5pp [−5.8,−0.7]); kernel-mass B* −2.6pp, uncalibrated (never states P>0.5 on bins winning 65–95%) ⇒ refutes the surrogate, not the belief (memory `kernel-mass-on-raw-members-is-not-the-served-belief`).
- **HIGH center de-bias, held-out walk-forward on the exact served shape (scratchpad/debias_holdout/REPORT.md; q reproduced 60/60 to 0.0, live fitter reproduces all 26 stamped shifts):** belief improves on 08-28..09-05 (PIT 0.61→0.52, upper tail 9.4%→5.2%, CRPS +0.05; untouched 09-04/05 log +0.12) but paired payoff at the exposure point is negative (eb−off −0.030 [−0.066,+0.003]; global b −0.018 [−0.042,−0.002]) because on the 62 rows with a book the belief delta is zero. Rule: no re-land. Peer's removal rationale still wrong; correct disposition = shadow stamp + intention-to-trade replay over ≥2 weeks of untouched dates.
- **Obs-arrival lead at the pre-absorption price (scratchpad/obsarrival/REPORT.md; 7,313 events, 3,880 with a book, 858 clusters, shipped nowcast, causal artifact):** no positive-LCB cell for the nowcast or the served belief at the first executable ask after possession (nowcast ALL +3 min −0.023 [−0.037,−0.008]; the pre-ask +0.086 post-peak cell inverts to −0.032). Item 36's Brier basis was already pre-absorption, so the lead was never alive at the belief level. Survivor: dead-bin NO (mechanical exclusion by the rounded extreme), band-legal +0.004 [−0.044,+0.044] at +3 min, depth median $13 — dust. settlement_capture's July "10/10" was $186 cost for +$1.5 net.
- **Corrections on record:** the DAY0_EXTREME_EVENT capture hook is live (6,690 stamped rows in the last 150k; the "zero rows" read was a timed-out unindexed COUNT); both exposure forks priced buy_no at the NO token's own ask while the engine takes min(NO ask, 1−YES bid) — conservative, changes no sign, must be fixed before any buy_no cell is promoted.
- **Standing decision rule (precommitted, executed):** open only cells with a positive one-sided LCB at the first executable post-possession price; none exists ⇒ belief-driven entries are zero-EV-at-best at every measured price and the correct capital action for them is zero, which riskguard already applies to the qkernel lane. Research direction that remains: the 7× over-reaction of our q vs the price move on observation arrival (item 36) is the only structural lead; compress it to market scale via the nowcast hazard and re-measure at the pre-absorption ask.

### Addendum 24b (2026-09-06 ~14:20Z): observation-arrival over-reaction — measured, and the last belief-side lead closed

- Over-reaction quantified on 7,313 DAY0_EXTREME_UPDATED events (post capture ≤15 min, |Δq|≥0.05): where the held side's q rises our post-q overshoots ~2× in probability space and the market's post-ask is the better forecaster (yes q_up Brier(ask_post)−Brier(q_post) = −0.098 [−0.136,−0.057]; no q_dn −0.067 [−0.101,−0.030]); where q falls it is a tie. Mechanism: additive per-model residual with `day0.current_state_innovation_e_fold_hours` = 4.2 (src/signal/day0_window.py).
- Walk-forward one-parameter candidates at possession time (shrink, market-anchored shrink = the live valve's form on the pre-ask, logit blend, nowcast blend): all lose to the market's post-ask on paired Brier; shrink κ fits to 1.0 (s_post is calibrated per decile — the served q is not "too big", the market just knows more). The single positive-LCB cell at the reachable +3 min price (nowcast blend, margin 0.10, yes, n=58–65, LCB +0.007–0.021, fee-adjusted +0.014) fails the equal-window pre-information placebo (edge at +3 min +0.059 vs at +15 min +0.057; paired treatment−placebo markout −0.006 [−0.027,+0.007]); mean q 0.46 vs win-rate 0.18 = the nowcast's own 0.2–0.4 miscalibration selected by the rule. Capacity ~$2.4/day. Not opened.
- Event-driven wakes armed for the session: state monitor (HEAD, riskguard, qkernel gate, quota day roll), live-log monitor (freshness gate, stamp faults, venue orders, entry blocks, new error classes), quota day-roll verdict one-shot. Known noise suppressed: `EXIT_MONITOR_CYCLE_INCOMPLETE:COVERAGE_INCOMPLETE` ~100/h since 09-04 10:14Z while SELL commands still fill (6 today) — reporting class, not a blocker; unowned.
- **Standing verdict:** with the belief lane closed at every measured price and the mechanical lane at dust, the system has no positive cell to size. Capital growth requires information the market lacks; none of the three candidates (faster observation, station bias, settlement mechanics) survives at the executable price. Next research that could change this is a different information source, not a different transform of the same one.
- **Resting (bid-side) lane closed the same day:** every ENTRY command since 07-20 priced at its own limit against chain payouts. Filled lo-band edge −0.004 [−0.046,+0.041], mid −0.041 [−0.084,−0.000]. The unfilled lo-band "edge" (+0.080) is orders whose limit the ask never touched within 24 h (win-rate 0.307 vs 0.128 when reachable): selection, not a maker edge. The executable ask over-states every decile on both sides at the reachable post-possession price, so the book pays neither taker nor maker at Zeus's information. Stopping rule now in force: no entry cell opens until a candidate information source passes a positive one-sided 95% LCB at the first reachable price with an equal-window placebo; the wakes report state changes, the research consult (REQ-20260906-084117-072f9e) supplies candidates.
- **Sell mirror (true bids, depth, per-level fee, min size, 440 settled positions since 08-06; scratchpad/sellmirror/REPORT.md):** HOLD −$357, EXISTING −$305, best mirror (m=0.05) +$199 vs EXISTING but every governing simultaneous one-sided LCB is negative (−0.27..−0.30; date blocks govern); +$179 of it is five held-winner positions; belief-blind and stale placebos both lose. Not demonstrated; no exit rule ships. The ask-proxy +0.13/$ was the artefact the consult predicted.
- **Exit organ audited on the same window with sell fees:** every trigger class nets ≥0 or CI-through-zero vs holding to payout (ALL +$52; RED_FORCE_EXIT +$47 [+17,+83] and FLASH_CRASH_PANIC +$13 [+9,+17] the only significant ones, both positive; GLOBAL_CAPITAL_OPTIMAL_SELL +$35 [−35,+105]). The organ is not a loss source; item 8's anti-convexity concern is not supported here.
- **Consult REQ-20260906-084117-072f9e (edge sources):** weather-information search closed; two book-only hypotheses (cross-bin quote lead, isolated liquidity dislocation) with frozen preregistered specs, placebos, simultaneous LCB hurdles and a $1/week dollar hurdle are the only falsifiable residuals; basket certificate = upper-bound audit (route non-atomic, capacity zero). Stopping outcomes: refuted / capacity-insufficient / underpowered-and-retired. Running on the archive now (scratchpad/bookonly/).

## Item 37 (2026-09-06): stopping statement — no demonstrated exploitable source under current coverage, routes, costs and capital

- **Book-only falsification (scratchpad/bookonly/REPORT.md, consult-frozen specs, 31 d):** cross-bin quote lead REFUTED at spec (28 arrivals, 20/20 fills lost, ΣG/Σcost −0.495, LCB −0.61; placebo empty); isolated dislocation UNOBSERVABLE (0 events; own-mid 60 s move p99 3.5¢); basket certificates CAPACITY-INSUFFICIENT ($6.36 displayed in 31 d, executable route capacity zero).
- **Why unobservable:** `executable_market_snapshots` is a family-burst record (whole family in one second, bursts 34 min apart at median, second complete state within 120 s at 3.6% of instants, 31% ask-only) and `token_price_log` records only Zeus's own tokens. Any faster-than-burst book hypothesis cannot be measured from what is captured. A continuous full-family book feed (CLOB websocket or REST `/books` ≤30 s) is the precondition; that is a data-acquisition and storage decision (item 13 law), not a replay, and is left to the operator.
- **Classification of every candidate measured 09-04..09-06:** weather-belief transforms (debias, cut B*, shrink/blend) REFUTED at the exposure point; observation-arrival timing REFUTED by the equal-window placebo; taker at the ask REFUTED (over-states every decile both sides); resting at the bid REFUTED (unfilled-limit "edge" = never-reachable selection); dead-bin NO CAPACITY-INSUFFICIENT; cross-bin lead REFUTED-at-spec; dislocation UNDERPOWERED-BY-COVERAGE; sell mirror NOT DEMONSTRATED (all governing LCBs negative); exit organ audited, no class a net loser.
- **Stopping statement (consult §6 form):** no demonstrated economically exploitable source remains among the preregistered policies under the observed coverage, executable routes, costs and $220 capital. Reopen only on changed evidence: a possession-timestamped new information source, a full-family book feed, or a fill-supported policy result clearing the stated bounds. A new transform or threshold on the same sample is not changed evidence.
- **Runtime posture:** unchanged and correct — riskguard's e-value gate holds the qkernel lane closed; the day0 lane is contained by the nowcast and ask-repricing vetoes (1 order today); no operator-mandated change is pending from this session. Wakes armed: state monitor, live-log monitor, quota day-roll one-shot (verdict 09-07).
- **Item 37 addendum (2026-09-06 ~14:30Z):** "faster observation feed" closed on latency — Zeus possesses US METARs 2.1–3.3 min after the station minute (aviationweather); NWS latest-observation API +17 min, NOAA tgftp identical report, IEM 1-min ASOS empty for today. The dead-bin NO "pre-ask" edge is pre-observation quotes by construction (pre-ask a median 19.7 min before the station observation; 8/12 rows captured within 60 s of possession had already repriced >0.95). Precondition build in flight: standalone full-family CLOB websocket capture + coverage census on branch `research/family-book-capture` (unlaunched, unlanded; the launch is the operator's data-acquisition decision).
- **Precondition delivered (research/family-book-capture @ da15dfba4, unlaunched):** standalone full-family CLOB websocket capture (6,622 tokens / 301 families, change-deduped top-of-book + 60 s depth, possession vs exchange clocks, WAL, 21-day retention) and a coverage census; 9/9 tests; smoke ≈285k rows/day, ≈51 MB/day. One command launches it; the operator decides. After ≥14 days the cross-bin lead and dislocation tests re-run as measurable.

### Item 37 amendment 2 (2026-09-07 ~01:50Z): adversarial review — AMEND; classifications corrected, compact archive censused, price authority corrected, collector repaired

- **Review (REQ-20260906-100535-e3c13b, gpt-6-pro, verified envelope): AMEND ~95%, keep no-promotion.** Every load-bearing claim was verified locally before acting; the narrow stopping sentence stands, the surrounding classifications and one plan claim are corrected below.
- **Classification law (accepted; Addendum 24's "zero-EV-at-best" withdrawn):** a non-positive one-sided LCB blocks promotion and says nothing about sign. Three bins from now on: DEMONSTRATED (LCB > hurdle), EXCLUDED (tested cohort; UCB < hurdle), UNRESOLVED (interval spans the hurdle; retired when the research budget ends, never "proven absent"). Item 37's "REFUTED" labels are re-read per this table:

| candidate | prior label | governing interval | class now |
|---|---|---|---|
| B*/A cut as tested entry rules | REFUTED | served −3.5pp [−5.8,−0.7]; NO cells ≈ −7¢ | EXCLUDED (tested cohorts); the belief itself not refuted (surrogate only) |
| HIGH center de-bias re-land | REFUTED | eb−off −0.030 [−0.066,+0.003]; untouched −0.010 [−0.016,−0.005]; `off` absolute +0.024 [−0.007,+0.056] | EXCLUDED as a paired improvement on the book subset; absolute policy UNRESOLVED |
| obs-arrival at first executable ask | REFUTED | ALL +3 min −0.023 [−0.037,−0.008]; k≥6 first-[0,3 min) +0.058 [−0.034,+0.141] (n=117) | ALL EXCLUDED; k≥6 and small-innovation cells UNRESOLVED (few first-window prices) |
| shrink / blend transforms | REFUTED | paired Brier vs market post-ask −0.03…−0.06 | EXCLUDED as belief improvements; the YES/margin-0.10 nowcast-blend cell UNRESOLVED (see Q2) |
| taker at the ask | REFUTED | pooled post-fill markout negative, CI excludes 0 (09-04) | EXCLUDED (pooled); per-decile intervals not all adjudicated |
| resting at the bid | REFUTED | filled lo −0.004 [−0.046,+0.041]; mid −0.041 [−0.084,−0.000] | mid EXCLUDED; lo UNRESOLVED; the unfilled-limit counterfactual is withdrawn both ways (ask-touch is a proxy, not a fill model) |
| dead-bin NO | CAPACITY-INSUFFICIENT | +0.004 [−0.044,+0.044], depth $13 | UNRESOLVED-retired under band + coverage; venue-wide capacity unbounded |
| C1 cross-bin lead | REFUTED-at-spec | 20/20 lost, 1 date, 3 clusters, placebo empty | negative observed ledger; mechanism UNADJUDICATED (coverage); the ask-only midpoint proxy is not uniformly conservative (raises the sibling sum → lowers YES donors, raises NO donors; 19/20 episodes were YES) |
| C2 dislocation | UNDERPOWERED | 0 events | stands |
| basket certificates | CAPACITY-INSUFFICIENT | route non-atomic; $6.36 displayed / 31 d | stands |
| sell mirror | NOT DEMONSTRATED | governing LCB −0.27…−0.30 | stands (exit policy, not an entry edge) |

- **Compact archive (review [HIGH] "omitted population", resolved):** `executable_market_snapshot_compact` exists — 1,219,539 rows 07-28 → now, all `DISCOVERY_SWEEP`, ~30k rows/day, 21–31% ask-only, top-5 depth JSON — and the book-only extract read only `executable_market_snapshots`. Family-completeness census 08-06 → 09-06 (scratchpad `compact_census/family_census2.py`, conditions mapped through `executable_market_snapshot_latest`, 1,855–1,970 families): fraction of 60 s grid minutes with every bin of the family observed within 60 s — compact 1.04%, full 0.48%, union 1.03%; within 120 s — 1.71 / 1.10 / 1.97%; complete at t and at t−120 (the C1 precondition) union 0.14% (60 s) / 0.37% (120 s); zero family-days ≥50% complete in any table. The omission changes no verdict: sub-burst hypotheses stay unobservable on the archive and the continuous feed stays the precondition. Compact rows are research evidence, never submission evidence.
- **Execution-price authority (review [HIGH], accepted — corrects Addendum 24):** the taker leaf prices buy_no on the NO ladder only (`src/strategy/live_inference/executable_cost.py` `_levels_for_direction` → `no_asks`; `assert_not_no_complement_cost` forbids 1−YES); `complementary_top_bid` in the adapter is the MAKER-QUOTE lane's non-crossing cap (1 − YES bid − tick) used only when the NO ask is empty, never a taker price. The exposure forks priced NO at the native ask, which is the engine's actual taker route, so they were exact, not conservative; the Addendum 24 note "must be fixed before any buy_no cell is promoted" is withdrawn. The 4,174-burst agreement (NO ask vs 1−YES bid to 0.0001) makes the question moot at the mid-band regardless.
- **Fee coefficient (review, partly accepted):** `fee_rate_fraction` on each snapshot is the venue's per-token CLOB fee rate at capture time (`source: clob_fee_rate`, `base_fee` in bps; sampled by month: 06 mostly 0.05, 07 mixed, 08–09 mostly 0.10) — the book study used each token's own captured rate, not an assumed category rate, so the "documented 0.05 weather rate" is not what the venue returns for these tokens now. No fill-side fee evidence exists (`venue_trade_facts.fee_paid_micro` null on 5,095/5,135 rows, 0 on the rest); capture-time per-token rate is the only provenance and is recorded as such.
- **Collector (review [BLOCKER] + 2×[HIGH] + [MEDIUM], every one verified against the market-channel docs — `price_changes[]` entries carry `price, size, side`; text `PING` every 10 s; `operation: subscribe/unsubscribe` — and fixed):** research/family-book-capture @ **7180ad448** (pushed): per-token ladder loaded from `book` and updated by every `price_change` entry (size 0 deletes), tops derived never carried, 60 s depth checkpoints on their own clock plus on every snapshot, text PING, subscribe/unsubscribe for universe refresh (no reconnect), `connection_epoch` + per-epoch initialisation (uninitialised `price_change` dropped and counted in `feed_health`), coverage validity from epochs + connection state with denominators from `token_meta`, yesterday's still-open markets retained, measured db+WAL bytes, retention off the receive path. 15/15 tests (re-run on the main-tree venv). Still unlaunched; the launch decision is the operator's.
- **Reporting corrections:** the obs-arrival `ask>0.95` row is empty by construction (q ≤ 1 and q − ask > 0.05) — a structural exclusion, not a negative test; "$2.4/day" was turnover capacity at median $9 depth, not profit; debias "62" = HIGH rows 08-28..09-05 with any book capture within the hour, "55" = rows contributing (bin, side) decisions at margin 0.05, and 174/161 are the `off`/`eb` picks from the same 788 decisions — two populations of one extraction; the paired contrast (n=193 decision rows) is the shared-row quantity.
- **Review's remaining audits, mapped:** Q0 done (above). Q1 collapses into the price-authority correction (the native NO ask is the route; there is no cheaper executable complement). Q3 is this amendment. Q4 (actual passive-fill economics) is already the 09-04 markout study plus Addendum 24b's filled-order ledger: actual maker fills mark negative in every lead × direction × band cell (15 m −4.0¢ … EOD −5.1¢), filled lo-band [−4.6¢,+4.1¢] UNRESOLVED. Q2 (absolute vs timing payoff, frozen YES/margin-0.10 nowcast-blend rule): G_policy +0.059 at +3 min and +0.057 at +15 min, treatment − placebo −0.006 [−0.027,+0.007], n=58–65, cell selected from a belief × margin × side × split search with no multiplicity adjustment, ~$2.4/day turnover — UNRESOLVED and not capitalisable at $220; not zero. Q5 needs the feed (archive pairs 0.37%).
- **Replacement stopping statement (adopted from the review):** none of the reviewed policies currently qualifies for capital promotion. Some fixed tested cohorts have negative executable economics; others remain unresolved because of limited coverage, few independent dates, uncertain execution costs or inadequate controls. This evidence does not establish venue-wide absence of profitable policies. Closed weather transforms remain retired. Further work is limited to the identified price/fee and population audits (done above), followed — only where needed — by corrected book-state acquisition and frozen policy tests. Change condition unchanged: a frozen, independently evaluated, executable policy ledger clearing the simultaneous return and dollar-capacity bounds, with the mechanism control passing where a mechanism is claimed. Neither a better belief score nor a new subdivision of this same sample is that result.
- **Runtime, same window:** riskguard DATA_DEGRADED 01:21–01:38Z (storage_capacity `ENTRY_RESERVE_BREACHED`, free 9.6% after the 11:42-local TimeMachine snapshot); thinned → 13.0% free, GREEN at 01:39Z; fifth episode, same cause, durable fix still trades-DB retention (operator). 09-06 UTC: 22 BUY / 13 SELL commands, 62 fills, $149.75 notional. Open-Meteo 09-06 final day_count 4,980 (includes the pre-r7 morning); the clean verdict is 09-07, one-shot re-armed. Error classes: `TLSV1_ALERT_DECODE_ERROR` ConnectError 1 today / 4 in the previous rotation on the orderbook-snapshot POST, captures continued every minute (transient); omega `ValueError` 6 today at the DATA_DEGRADED onset / 154 previous rotation (existing class); `PartialExitEconomicDebtError` 8 today on 32d6009c-007 / 318 previous rotation (existing class, same position as the pinned-posterior warnings).

### Item 37 amendment 3 (2026-09-07 ~02:20Z): full-family book feed LIVE — the precondition for every book-only test is now being collected

- **Decision (standing "do not wait" law):** research/family-book-capture launched from the repaired build after two further fixes found on first contact with the venue — (1) only the first `{"type":"market"}` subscribe message takes effect (a second one added nothing: probed A kept streaming, B received 0 events), so 11 batches of 5,324 tokens yielded exactly 500 streaming tokens; fixed by sending batch 1 as `type:market` and the rest as `operation:subscribe` (be0f2250d); (2) `connection_epoch` now continues from the DB across process restarts so validity grouping cannot collide (bab1b3ae9). Verified on epoch 2: 5,324/5,324 tokens with top-of-book rows and depth checkpoints, 242/242 families streaming within ~60 s of connect, 0 reconnects. Epoch 1 rows are the one-batch defect and are excluded from any census.
- **Where:** `state/research/family_books.db` (WAL, 21-day retention, public data only, no credentials, no trading-DB writes), pid in `state/research/family_book_capture.pid`, log `logs/family_book_capture.log`, worktree scratchpad `wt-fbc` (outside the reaper's roots). Crash/reconnect/retention monitor armed for the session.
- **What it unlocks (unchanged from the review):** C1/C2 rerun only on valid paired states (Q5), with the review's power arithmetic: first look after 14 fully covered days, predetermined; zero events over 14 days still bounds capacity only at ≈$7.5/week, so a null after 14 days retires the candidates as "not worth further measurement", never as absent.

## Item 38 (2026-09-07 ~03:00Z): re-alignment — where resources fail to become market advantage (five read-only audits, all verified)

The operator's main line restated: disk, API quota and compute must convert into a probability lead at every horizon and into correct execution choices; the disk fix is locating the non-converting writes, not deleting. Five audits (A storage+quota, B execution choice, C sharpness deficit, D chain latency, E served reliability) ran read-only; every load-bearing claim below was reproduced by the main thread before being written.

- **Where the belief loses (C, reproduced n=18,993 / 632 clusters):** the market's ask is ahead of the served day0 belief by the same Brier margin BEFORE an observation as after it (−0.038 [−0.046,−0.029] vs −0.036 [−0.044,−0.028]); its ask 15 min before the METAR beats our post-METAR belief (−0.027). Murphy decomposition: 93% resolution, 7% reliability. Flat across k, |innovation|, hours-left; the nowcast reproduces the served score to 0.001. Verdict: the deficit is formed between observations from information the posterior does not condition on; possession speed, innovation shrink and remainder models act on a channel where the gap is zero. Memory `sharpness-deficit-is-baseline-not-observation-channel`.
- **Where the selector loses (E, reproduced 595 claims):** on certified claims the served q over-states P(win) in every decile (q .8–.9 wins 0.43, .9–1 wins 0.58; REL 0.119) while the price on the same rows is calibrated (REL 0.002). The same belief on the unselected event population has REL 0.004 on contested bins; conditioning on q − ask > 0.05 raises it to 0.047 and > 0.15 to 0.107. The certified population is the belief's own upward errors, selected. day0 trades on the raw nowcast q (market-anchored correction applied to 1.4% of day0 certs); demanded margin rises 0.06→0.46 with q. Memory `selected-certificates-carry-winners-curse-reliability`.
- **Where the law is dead (B, verified file:line):** `day0_nowcast_entry` placed all 22 BUYs on 09-06 with no active gate; its own probation law (`_revision_probation_gate_reason`, riskguard.py:4659; latch at ≥3 realized closes and net ≤ 0; riskguard's 7-day day0 curve: realized 24, net −$9.55) is short-circuited at riskguard.py:4703 by `selection_revision_bound is not True` (6e507819b, 08-30), a flag no producer in `src/` ever sets — the only binder lives in `scripts/evaluate_current_regime_capital_advantage.py`, writes a JSON riskguard never reads, and has itself failed every 5 min since 09-02 (77 s deadline; profile: proof registry re-derived 5,715× per run, 62 s). The qkernel lane is closed only by the other law (e-value 0.0095 vs 10); day0's e-value cohort has 2 clusters ⇒ inconclusive ⇒ ungated. Markout 7-day: day0 buy_no +15 m [−0.042,−0.017], buy_yes [−0.091,−0.018]; net −$35.76 on 31 closes. **Ships (class 1, wiring gap in the existing law, no new gate):** riskguard binds each curve position to its entry receipt's global_selection_revision and evaluates probation on the bound cohort (executor in flight, branch fix/riskguard-probation-selection-binding); capital-evidence job made to finish inside its deadline without changing any value (branch fix/capital-evidence-runtime). Latent: adapter 6431-6459 exempts MAKER_REST NO from an alpha-only automated gate (never fired; on record, not changed).
- **Where the chain waits (D, verified against provider Last-Modified):** hops release→available→fetch-start are structurally zero; the whole wait is provider publication. ECMWF publishes 06/18Z ENS at +424 min (13:04Z/01:04Z), 00/12Z at +460 (19:40Z); Zeus has first steps 8 min later and the complete 373-step run 30–35 min later (steps trickle). Lane D's "177 unexplained minutes" was the calendar's 285-min short-horizon lag being earlier than the provider, harmlessly (index probes every 2 min return not_released). Belief → first market capture 22 s median; belief → certificate 124 s median. Latency is not a lever at any lead. Audit-trail defect on record: `ensemble_snapshots.fetch_time` / `source_run.fetch_started_at` drift up to 39 min later than `recorded_at`/`computed_at` since ~08-26 — measure chain speed on the latter.
- **Where disk does not convert (A, verified; nothing deleted):** DEAD (no reader in src/): `executable_market_snapshot_compact` (61.9k rows/day, writer's own docstring "discovery-only"), `book_hash_transitions`, `market_price_history`, world `hourly_observations`, world `execution_feasibility_evidence`. AUDIT-ONLY: world `forecasts` (59k rows/day; live path serves from the attached forecasts DB), trades `execution_feasibility_evidence`. Largest write-only mass: `opportunity_event_processing` terminal rows (reactor reads only pending/processing; prune script exists, unscheduled; 44 GB), `replacement_forecast_live/{processed,seed_processed}` 11.4 GB with zero read-back sites, `ensemble_snapshots.provenance_json` (low-metric rows 21 KB of which `low_local_day_min_interval_evidence` 7.3 KB/row avg + `boundary_normalization` 2.4 KB have zero readers; the materializer's SELECT reads `members_json` ≈ 0.95 KB), `risk_state.details_json` ~34 KB/row every 36 s with only the latest row read. Decision-feeding but unbounded reads: `settlement_outcomes` full-table read per call (chain_mirror_reconciler.py:796), `calibration_pairs` 81M rows filtered by bucket only (corpus frozen 05-31), `position_events`/`decision_log` exact-id only. Duplicate-storage hypothesis on `ensemble_snapshots`: not supported (0 duplicate (source, run, city, metric, date) keys in the last 50k rows).
- **Where quota does not convert (A):** `bayes_precision_fusion_single_runs_locations_batched` is requested by two processes (live daemon 617 for day0 at 72 h/1 h, ingest 93409 for BPF at 120 h/0 h) with a shared payload cache keyed on the hour window, so identical (model, run, city) fetches never hit — 394 of ~800 daily calls; `previous_runs` (54/day) and `archive_hourly` (63/day) write tables no live builder reads; a third live process (`forecast_live_daemon`, pid 93674) was not in the operator's attribution. Consumed by the served posterior: `day0_source_clock_ensemble` and the three `bayes_precision_fusion_*` jobs only.
- **Book feed (precondition, live):** full-family capture verified on epoch 2 (5,324/5,324 tokens, 242/242 families); four venue-contact fixes landed on research/family-book-capture (additive subscribe batches, epoch continuation, change-gated depth, venue text PING as heartbeat); storage after fixes ≈ 6.5 GB/day still dominated by top-of-book churn — retention 21 d holds ~135 GB, to be revisited after the first steady day; connection still drops every 4–9 min at 35–50k msgs/min (reconnect 2–4 s, epochs tracked; coverage census must treat epoch boundaries as gaps).
- **Decision rule for what ships from this item:** only (1) wiring gaps in existing law (B, in flight), (2) the capital-evidence runtime (B, in flight), (3) storage/quota removals the operator authorises from the A ledger — never a new gate, cap, transform, or calibrator refit. C and E are diagnosis: the lever they name is an information source the posterior lacks, plus a selector that must be judged on post-selection reliability, which is what the e-value/probation law does once it is wired.
