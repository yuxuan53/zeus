state/zeus.db
state/risk_state.db
state/baseline_results.json
state/ensemble-log/
state/positions.json
state/*-paper.json
state/*-paper.db
state/*.db-shm
state/*.db-wal
state/legacy_state_archive/
docs/archives/
logs/*.log
logs/*.err
.omx/
.omc/
.code-review-graph/
equity_curve.png
.DS_Store
__pycache__/
*.pyc
.venv/

state/*.db
state/*.bak
state/*.db.aborted-*
state/*.db.pre-rebuild-*

# Backfill / rebuild scratch logs (transient nohup + tee output).
# Topology doctor walks state/ directly; active run logs are still
# classified per-file in architecture/topology.yaml, but new run logs
# should default to ignored so they don't pile up as unclassified.
state/rebuild-*.log
state/rebuild-*.out
state/fillback-*.log
state/fillback-*.out
state/hko-catchup-*.log
state/ogimet-*.log
state/wu-*.log
state/city_settlement_audit.json

# Orphan SQLite sidecars at repo root (should never exist; canonical DB is state/zeus-world.db).
/zeus-world.db-shm
/zeus-world.db-wal

# Local review / patch scratch artifacts should never become root topology surfaces.
/k_bugs.json
/patch_schema.py

.git-commit-msg.tmp

raw/oracle_shadow_snapshots/
docs/**/*.xlsx
