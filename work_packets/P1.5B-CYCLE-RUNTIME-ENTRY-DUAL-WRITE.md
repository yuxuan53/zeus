# P1.5B-CYCLE-RUNTIME-ENTRY-DUAL-WRITE

```yaml
work_packet_id: P1.5B-CYCLE-RUNTIME-ENTRY-DUAL-WRITE
packet_type: feature_packet
objective: Retry the `src/engine/cycle_runtime.py` entry-path caller migration so it emits canonical entry events/projection when canonical schema is present, while preserving existing legacy writes on legacy-schema runtimes and keeping all other callers untouched.
why_this_now: `P1.5A-LEGACY-ENTRY-TELEMETRY-COMPAT` is pushed, so the specific legacy-helper crash path that blocked the first caller migration is now narrowed. The cycle-runtime entry caller can be retried honestly as the first bounded runtime caller migration.
why_not_other_approach:
  - Reuse the reopened `P1.5` packet without a new freeze | the earlier packet recorded a real contradiction and should not be silently reused
  - Jump to another caller first | cycle-runtime entry remains the narrowest caller with the clearest builder inputs and rollback path
truth_layer: Canonical authority becomes more real only when an actual runtime caller emits canonical entry events/projection; this packet retries that process for one caller with the updated helper-compat behavior now in place.
control_layer: This packet changes only the cycle-runtime entry write path; it does not widen to exit/harvester/reconciliation callers, DB-first reads, or cutover semantics.
evidence_layer: Targeted tests, caller-level dual-write proof note, rollback note, and explicit no-cutover/no-team-default note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - work_packets/P1.5A-LEGACY-ENTRY-TELEMETRY-COMPAT.md
  - src/engine/AGENTS.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/engine/cycle_runtime.py
  - src/engine/lifecycle_events.py
  - src/state/db.py
  - src/state/ledger.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.5B-CYCLE-RUNTIME-ENTRY-DUAL-WRITE.md
  - src/engine/cycle_runtime.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/state/db.py
  - src/state/ledger.py
  - src/state/projection.py
  - src/engine/lifecycle_events.py
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the cycle-runtime caller migration, targeted tests, and paired Architects control-surface updates together; current helper-compat behavior remains, but this caller returns to legacy-only writes.
acceptance:
  - The cycle-runtime entry path emits canonical entry events/projection when canonical schema is present.
  - Existing legacy entry writes remain in place on legacy-schema runtimes.
  - The caller does not crash on canonically bootstrapped DBs.
  - No exit-path, harvester, or reconciliation caller is migrated in this packet.
  - No DB-first read or cutover behavior is introduced.
evidence_required:
  - targeted pytest output
  - caller-level dual-write proof note
  - rollback note
  - no-cutover / no-team-default note
```

## Notes

- This is a retry packet created only after `P1.5A` removed the specific helper-compat blocker.
- Team is still not the default; this remains cleaner as a single-owner one-caller slice.
- If implementation pressure reveals a need to touch a second caller or any state module, stop and freeze a successor packet instead of widening this one.
