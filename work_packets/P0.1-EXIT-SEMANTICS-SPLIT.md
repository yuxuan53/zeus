# P0.1-EXIT-SEMANTICS-SPLIT

```yaml
work_packet_id: P0.1-EXIT-SEMANTICS-SPLIT
packet_type: refactor_packet
objective: Introduce explicit exit-intent semantics and exit event vocabulary scaffolding before any broader lifecycle or ledger work, without batching full cutover behavior.
why_this_now: Foundation ordering requires P0.1 immediately after P0.2 because local-close semantics are the deepest pre-ledger corruption risk. Exit truth must be split before richer lifecycle authority work lands.
why_not_other_approach:
  - Jump to P0.3 or P1 first | canonical storage would preserve false closure semantics
  - Perform the full behavioral cutover now | would batch later P2 work into the first P0.1 packet
truth_layer: The touched execution path must represent exit intent and pending exit as first-class semantics rather than silently collapsing them into local closure.
control_layer: This packet introduces scaffolding and event vocabulary only; full runtime behavioral cutover stays for later bounded packets.
evidence_layer: Event-model legality tests, no-scope-widening note, rollback note, and explicit proof that the packet remains scaffolding-first.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-01
  - INV-02
required_reads:
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - architecture/self_check/authority_index.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/invariants.yaml
  - architecture/negative_constraints.yaml
  - src/engine/AGENTS.md
  - src/execution/exit_lifecycle.py
  - src/execution/executor.py
  - src/engine/cycle_runtime.py
files_may_change:
  - src/execution/executor.py
  - src/execution/exit_lifecycle.py
  - src/engine/cycle_runtime.py
  - tests/test_runtime_guards.py
  - architects_progress.md
  - architects_task.md
  - work_packets/P0.1-EXIT-SEMANTICS-SPLIT.md
files_may_not_change:
  - migrations/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - src/control/**
  - src/supervisor_api/**
  - .github/workflows/**
  - .claude/CLAUDE.md
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
tests_required:
  - targeted exit-semantics runtime-guard tests
  - architecture-contract verification for exit/settlement law
parity_required: false
replay_required: false
rollback: Revert touched execution/runtime files, targeted tests, and paired Architects ledger updates together.
acceptance:
  - ExitIntent concept exists on the touched execution boundary.
  - Explicit exit event vocabulary scaffolding exists on the touched path.
  - Packet remains scaffolding-first and does not perform the full runtime cutover yet.
  - No P0.3/P0.4/P0.5/P1/P2 work is batched into this packet.
  - Tests cover event-model legality on the touched path.
evidence_required:
  - targeted test output
  - no-scope-widening note
  - rollback note
  - invariant reference note
```

## Notes

- This packet is the first `P0.1` slice, not the final P2 runtime cutover.
- If execution reveals that the work must alter schema, transaction boundaries, or full monitor-close behavior immediately, stop and freeze a narrower successor packet rather than widening this one.
