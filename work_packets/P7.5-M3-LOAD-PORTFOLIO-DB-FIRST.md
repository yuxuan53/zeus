# P7.5-M3-LOAD-PORTFOLIO-DB-FIRST

```yaml
work_packet_id: P7.5-M3-LOAD-PORTFOLIO-DB-FIRST
packet_type: schema_packet
objective: Make `load_portfolio()` read canonical `position_current` first while keeping JSON fallback explicit as emergency compatibility only when canonical projection is unavailable, without deleting legacy surfaces or widening into broader cutover.
why_this_now: P7R4 closed the concrete open-position parity blocker for the current paper cohort. With status-summary already DB-first from P6 and canonical open-position parity now proven on the touched seam, the next honest M3 move is the portfolio-loader read cutover rather than more backfill or schema work.
why_not_other_approach:
  - Freeze a broad DB-first cutover packet now | still over-bundles riskguard and other reader shifts beyond the next concrete seam
  - Keep `load_portfolio()` JSON-primary longer | leaves the central runtime loader on legacy truth after parity is now available
  - Delete JSON surfaces now | violates dual-write -> parity -> cutover -> delete ordering
truth_layer: `position_current` should become the primary read truth for portfolio-loading semantics, with JSON retained only as an explicit emergency compatibility fallback when canonical projection is unavailable or incomplete.
control_layer: This packet is limited to `load_portfolio()` and the minimum DB/query helpers plus packet-bounded tests needed to prove DB-first reads. It must not delete JSON surfaces, must not widen into riskguard DB-first work, and must not silently suppress fallback activation.
evidence_layer: targeted DB-first-loader pytest output, work-packet grammar output, kernel-manifest check output, explicit fallback-path proof, rollback note, and p7.6-readiness note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/state/db.py
  - src/state/portfolio.py
  - tests/test_runtime_guards.py
  - tests/test_pnl_flow_and_audit.py
  - work_packets/P7R4-OPEN-POSITION-CANONICAL-BACKFILL.md
files_may_change:
  - work_packets/P7.5-M3-LOAD-PORTFOLIO-DB-FIRST.md
  - src/state/db.py
  - src/state/portfolio.py
  - tests/test_runtime_guards.py
  - tests/test_pnl_flow_and_audit.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/control/**
  - src/observability/**
  - src/riskguard/**
  - src/engine/**
  - src/execution/**
  - src/supervisor_api/**
  - src/state/ledger.py
  - src/state/projection.py
  - tests/test_architecture_contracts.py
  - tests/test_riskguard.py
  - tests/test_healthcheck.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted DB-first loader tests
parity_required: true
replay_required: true
rollback: Revert the P7.5 loader-cutover edits and paired slim control-surface updates together; repo returns to the accepted P7R4 boundary with `load_portfolio()` still JSON-primary.
acceptance:
  - `load_portfolio()` becomes DB-first on the touched seam.
  - JSON fallback remains explicit and only activates as emergency compatibility when canonical projection is unavailable.
  - No legacy-surface deletion or broader DB-first cutover is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - kernel-manifest check output
  - explicit fallback-path proof
  - rollback note
  - p7.6-readiness note
```

## Notes

- Team remains closed by default for this packet.
- This packet is a bounded M3 loader seam only, not a full M3 cutover.
- If implementation evidence shows `load_portfolio()` still depends on a deeper parity blocker, reopen or supersede honestly instead of pretending the loader is ready.

## Closeout readiness notes

- P7.6-readiness note: if this packet lands cleanly and its post-close gate passes, the next lawful move is reassessing whether a separate bounded riskguard DB-first packet is now supported.
