# P4.1-OPPORTUNITY-FACTS

```yaml
work_packet_id: P4.1-OPPORTUNITY-FACTS
packet_type: feature_packet
objective: Install the first durable `opportunity_fact` writer path so each evaluated candidate-direction attempt records point-in-time learning truth for both no-trade and trade-eligible decisions, without yet widening into `availability_fact`, `execution_fact`, or `outcome_fact` writers.
why_this_now: P4 begins as a writer/flow installation phase because the canonical schema already contains the learning fact tables. The narrowest next step is to persist opportunity-level decision truth before layering separate availability, execution, outcome, and analytics packets on top.
why_not_other_approach:
  - Add new learning schema now | the architecture kernel already contains the fact tables, so widening back into migrations would be false phase work
  - Start with `availability_fact` first | would skip the base per-decision opportunity layer the P4 sequence names first
  - Write durable learning truth directly from `evaluator.py` | would blur the engine/state boundary and violate the evaluator-as-decision-not-authority posture
  - Mix `availability_fact`, `execution_fact`, or `outcome_fact` writes into this packet | would erase rollback boundaries and hide which fact layer was actually installed
truth_layer: One row in `opportunity_fact` must represent each evaluated candidate-direction attempt using decision-time fields and explicit availability/rejection truth, not hindsight reconstruction.
control_layer: This packet is limited to a narrow runtime-to-state seam: `src/engine/cycle_runtime.py` may call a new `src/state/db.py` helper to write `opportunity_fact` rows after evaluator decisions are materialized, and targeted tests may prove the present-path write and explicit absent-path behavior. No availability/execution/outcome writer, analytics query, schema, or governance-law work is allowed in this slice.
evidence_layer: Targeted runtime/db tests, work-packet grammar check, kernel-manifest check, rollback note, explicit no-other-fact-layer-yet note, and explicit capability-present/capability-absent proof note.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-04
  - INV-06
  - INV-09
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/engine/AGENTS.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - next_round_handoff.md
  - src/engine/cycle_runtime.py
  - src/state/db.py
  - src/engine/evaluator.py
  - src/state/decision_chain.py
  - tests/test_runtime_guards.py
  - tests/test_db.py
files_may_change:
  - work_packets/P4.1-OPPORTUNITY-FACTS.md
  - src/engine/cycle_runtime.py
  - src/state/db.py
  - tests/test_runtime_guards.py
  - tests/test_db.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/control/**
  - src/execution/**
  - src/observability/**
  - src/riskguard/**
  - src/supervisor_api/**
  - tests/test_architecture_contracts.py
  - tests/test_pnl_flow_and_audit.py
  - tests/test_replay_time_provenance.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted opportunity-fact runtime/db tests
parity_required: false
replay_required: false
rollback: Revert the P4.1 runtime/state/test edits and paired slim control-surface updates together; repo returns to the pre-P4 boundary where learning fact tables exist in schema but no runtime writer claims are made.
acceptance:
  - `opportunity_fact` receives one row for a no-trade evaluated candidate-direction attempt with point-in-time snapshot/rejection/availability truth preserved.
  - `opportunity_fact` receives one row for a trade-eligible evaluated candidate-direction attempt before execution/outcome layers are introduced.
  - The writer uses decision-time values and does not silently substitute a latest snapshot when decision snapshot linkage is absent.
  - When the `opportunity_fact` table is unavailable, behavior is explicit and test-proven rather than silently pretending durable write success.
  - No `availability_fact`, `execution_fact`, `outcome_fact`, analytics-query, or schema change is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - kernel-manifest check output
  - rollback note
  - no-other-fact-layer-yet note
  - capability-present-and-absent note
```

## Notes

- Team remains closed by default for this first P4 runtime/state slice.
- This packet installs only the first durable opportunity-fact writer seam.
- The next P4 packet after this one is `P4.2-AVAILABILITY-FACTS`.

## Closeout readiness notes

- No-other-fact-layer-yet note: This packet writes only `opportunity_fact`; it does not add `availability_fact`, `execution_fact`, `outcome_fact`, analytics-query, or schema work.
- Capability-present-and-absent note: targeted tests prove the write path when `opportunity_fact` exists and the explicit `skipped_missing_table` advisory path when it does not.
