# P3.2-POLICY-RESOLVER

```yaml
work_packet_id: P3.2-POLICY-RESOLVER
packet_type: feature_packet
objective: Install the first packet-bounded strategy policy resolver by adding `StrategyPolicy` and `resolve_strategy_policy(conn, strategy_key, now)` under `src/riskguard`, with layered precedence for hard safety controls, durable manual overrides, durable risk actions, and default policy, without yet wiring evaluator consumption or riskguard emission.
why_this_now: P3.1 completed the durable table contract, and the architecture sequence names policy resolution as the next P3 slice before evaluator consumption. Current repo truth still resolves protective behavior through direct control-plane helpers and advisory-only risk output, so the next safe move is to create the standalone resolver without changing runtime consumers yet.
why_not_other_approach:
  - Jump straight to evaluator consumption | would wire behavior before the resolver contract is explicit and independently tested
  - Expand riskguard emission first | would generate durable actions before the resolution semantics are frozen
  - Fold resolver logic directly into evaluator or control-plane code | would blur governance intent and widen packet scope across boundaries
truth_layer: Strategy-aware protection must resolve from one explicit policy object keyed by `strategy_key`, with durable manual/risk rows layered underneath hard safety controls and above default policy.
control_layer: This packet is limited to the resolver contract plus targeted tests; it may add a new `src/riskguard` module and tests, but it must not yet change evaluator sequencing, cycle-runner gating, riskguard emission, or control-plane write behavior.
evidence_layer: Targeted resolver tests, work-packet grammar check, rollback note, and explicit no-actuation-yet note.
zones_touched:
  - K1_governance
invariants_touched:
  - INV-04
  - INV-05
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/riskguard/AGENTS.md
  - tests/AGENTS.md
  - src/control/control_plane.py
  - src/riskguard/riskguard.py
  - tests/test_riskguard.py
  - work_packets/P3.1-STRATEGY-POLICY-TABLES.md
files_may_change:
  - work_packets/P3.2-POLICY-RESOLVER.md
  - src/riskguard/policy.py
  - tests/test_riskguard.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/control/**
  - src/engine/**
  - src/execution/**
  - src/state/**
  - src/supervisor_api/**
  - src/riskguard/riskguard.py
  - tests/test_architecture_contracts.py
  - tests/test_db.py
  - tests/test_runtime_guards.py
  - tests/test_live_safety_invariants.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted riskguard resolver tests
parity_required: false
replay_required: false
rollback: Revert the P3.2 resolver/test edits and paired slim control-surface updates together; repo returns to the post-P3.1 table-only boundary with no resolver behavior claimed.
acceptance:
  - `src/riskguard/policy.py` defines `StrategyPolicy` and `resolve_strategy_policy(conn, strategy_key, now)`.
  - Resolution order is explicit: hard safety controls, manual overrides, risk actions, then default policy.
  - Targeted tests prove one-strategy gating, one-strategy allocation shrink, manual override precedence over automatic action, and expired override restoration of automatic policy.
  - No evaluator-consumption, cycle-runner, riskguard-emission, or control-plane-write behavior is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - rollback note
  - no-actuation-yet note
```

## Notes

- Team remains closed by default for this resolver-only governance slice.
- The resolver may read current hard-safety control state, but this packet must not mutate control-plane behavior or claim durable control-plane migration.
- The next P3 packet after this one is `P3.3-EVALUATOR-POLICY-CONSUMPTION`.
