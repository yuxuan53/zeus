# P6.2-CONTROL-PLANE-DURABLE-OVERRIDE-WRITES

```yaml
work_packet_id: P6.2-CONTROL-PLANE-DURABLE-OVERRIDE-WRITES
packet_type: feature_packet
objective: Move the active control-plane command path off memory-only `_control_state` for the durable override subset by writing operator commands into `control_overrides` with explicit provenance and expiry semantics, while preserving ingress-only `control_plane.json` and leaving lifecycle-command durability for a later packet.
why_this_now: P6.1 finished the status-summary consumer cutover and passed its post-close gate. The next spec-ordered move is control-plane durable override writes.
why_not_other_approach:
  - Keep `_control_state` as the live durable source longer | would leave operator commands without durable teeth after the status summary is already DB-derived
  - Introduce `lifecycle_commands` now | widens into a larger command-model packet before the existing override-capable commands are durably bridged
  - Delete `control_plane.json` ingress now | violates the spec requirement that `control_plane.json` remains ingress-only
truth_layer: `control_plane.json` remains ingress-only, but commands in the durable override subset must land in `control_overrides` with explicit provenance, precedence, and expiry rather than relying on in-memory replay as durable truth.
control_layer: This packet is limited to the control-plane ingress/processing seam, the minimum DB helper(s) needed to write/read `control_overrides` for current commands, and packet-bounded restart-survival tests. It must not delete `control_plane.json`, must not widen into `lifecycle_commands`, and must not delete `strategy_tracker`.
evidence_layer: targeted control-plane durability pytest output, work-packet grammar output, kernel-manifest check output, rollback note, and explicit p6.3-readiness note.
zones_touched:
  - K1_governance
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-05
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/control/control_plane.py
  - src/state/db.py
  - src/riskguard/policy.py
  - tests/test_pnl_flow_and_audit.py
  - work_packets/P6.1-STATUS-SUMMARY-DB-DERIVED.md
files_may_change:
  - work_packets/P6.2-CONTROL-PLANE-DURABLE-OVERRIDE-WRITES.md
  - src/control/control_plane.py
  - src/state/db.py
  - tests/test_pnl_flow_and_audit.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/observability/**
  - src/riskguard/riskguard.py
  - src/engine/**
  - src/execution/**
  - src/supervisor_api/**
  - src/state/portfolio.py
  - src/state/ledger.py
  - src/state/projection.py
  - tests/test_architecture_contracts.py
  - tests/test_runtime_guards.py
  - tests/test_riskguard.py
  - tests/test_healthcheck.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted control-plane durability and restart-survival tests
parity_required: false
replay_required: false
rollback: Revert the P6.2 control-plane ingress/override edits and paired slim control-surface updates together; repo returns to the accepted P6.1 status-summary boundary without any claimed durable command bridge.
acceptance:
  - Commands in scope write durable `control_overrides` rows with provenance/expiry/precedence semantics.
  - Restart-survival proof shows the durable override subset still governs behavior after refresh/reload.
  - `control_plane.json` remains ingress-only and no `lifecycle_commands` or deletion work is mixed into this packet.
  - No strategy-tracker deletion or unrelated operator-surface widening is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - kernel-manifest check output
  - rollback note
  - p6.3-readiness note
```

## Notes

- Team remains closed by default for this packet.
- The first durable bridge should prefer commands already representable in `control_overrides`; commands that need a future `lifecycle_commands` substrate stay out of scope.
- If current command semantics cannot be bridged honestly inside `control_overrides`, narrow or reopen the packet instead of widening silently.

## Closeout readiness notes

- P6.3-readiness note: if this packet lands cleanly and its post-close gate passes, the next legal move is the `strategy_tracker` deletion/demotion path packet.

## Evidence log

- targeted pytest output: `.venv/bin/pytest -q tests/test_pnl_flow_and_audit.py -k 'control or recommended_commands'` -> `10 passed, 39 deselected in 1.18s`
- work-packet grammar output: `python3 scripts/check_work_packets.py` -> `work packet grammar ok`
- kernel-manifest check output: `python3 scripts/check_kernel_manifests.py` -> `kernel manifests ok`
- rollback note: revert the P6.2 control-plane ingress/override edits and paired slim control-surface updates together; repo returns to the accepted P6.1 status-summary boundary without any claimed durable command bridge
- p6.3-readiness note: the next legal move after a clean P6.2 closeout is the `strategy_tracker` deletion/demotion path packet
