# P3.1-STRATEGY-POLICY-TABLES

```yaml
work_packet_id: P3.1-STRATEGY-POLICY-TABLES
packet_type: schema_packet
objective: Complete the first durable P3 strategy-policy table contract by adding the missing `strategy_health` table and locking the `strategy_health` / `risk_actions` / `control_overrides` schema set with targeted architecture-contract evidence before any resolver or actuation work begins.
why_this_now: GOV-01 is now honestly closed in slim control state, the architecture spec names strategy policy tables as the first P3 slice, and current repo truth still relies on memory-backed control state in `src/control/control_plane.py` while the architecture-kernel schema still lacks the full durable P3 table set.
why_not_other_approach:
  - Jump straight to policy resolver or evaluator wiring | would bypass the durable table contract the P3 sequence requires first
  - Expand riskguard or control-plane behavior first | would keep protection actuation coupled to advisory or memory-only surfaces
  - Treat existing `risk_actions` / `control_overrides` migration stubs as sufficient by themselves | P3 still lacks the full table set and explicit contract locks for the durable strategy-policy substrate
truth_layer: Strategy-aware protection must resolve from durable table-backed policy truth keyed by `strategy_key`, not from memory-only control state or advisory sidecars.
control_layer: This packet is limited to the durable table contract for `strategy_health`, `risk_actions`, and `control_overrides`; it may change schema and targeted architecture-contract tests, but it must not yet change policy resolution, evaluator sequencing, riskguard emission logic, or operator-precedence semantics.
evidence_layer: Schema diff, targeted architecture-contract tests, work-packet grammar check, rollback note, and explicit no-actuation-yet note.
zones_touched:
  - K0_frozen_kernel
  - K1_governance
invariants_touched:
  - INV-04
  - INV-05
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - work_packets/FOUNDATION-TEAM-GATE.md
  - migrations/AGENTS.md
  - src/riskguard/AGENTS.md
  - tests/AGENTS.md
  - migrations/2026_04_02_architecture_kernel.sql
  - src/control/control_plane.py
  - src/riskguard/riskguard.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P3.1-STRATEGY-POLICY-TABLES.md
  - migrations/2026_04_02_architecture_kernel.sql
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - src/**
  - tests/test_db.py
  - tests/test_riskguard.py
  - tests/test_runtime_guards.py
  - tests/test_live_safety_invariants.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: true
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the P3.1 table-contract edits and paired slim control-surface updates together; repo returns to the pre-P3 durable-policy gap with no resolver/emitter behavior claimed.
acceptance:
  - The architecture-kernel schema defines the full first-pass P3 table set: `strategy_health`, `risk_actions`, and `control_overrides`.
  - Targeted architecture-contract tests lock the presence of the durable strategy-policy table contract on canonical bootstrap surfaces.
  - The touched table contract does not introduce any governance key that competes with `strategy_key`.
  - No resolver, evaluator-consumption, riskguard-emission, or manual-override-precedence claim is mixed into this packet.
evidence_required:
  - schema diff
  - targeted pytest output
  - no-actuation-yet note
  - rollback note
```

## Notes

- `risk_actions` and `control_overrides` already exist in `migrations/2026_04_02_architecture_kernel.sql`; this packet completes and locks the P3 table contract rather than pretending those existing surfaces do not exist.
- Team remains closed by default; this is a high-sensitivity schema/governance boundary packet.
- Read `src/riskguard/AGENTS.md` and `tests/AGENTS.md` before implementation, but keep this slice confined to the durable table-contract layer.
- The next P3 packets after this freeze remain `P3.2 policy resolver`, `P3.3 evaluator consumption`, `P3.4 riskguard emission`, and `P3.5 manual override precedence`.
