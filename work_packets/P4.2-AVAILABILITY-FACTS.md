# P4.2-AVAILABILITY-FACTS

```yaml
work_packet_id: P4.2-AVAILABILITY-FACTS
packet_type: feature_packet
objective: Install the first durable `availability_fact` writer path for discovery/evaluation-time data and infrastructure failures on the current opportunity-entry path, without widening into execution/order-chain availability logging, `execution_fact`, `outcome_fact`, analytics work, or schema changes.
why_this_now: P4.1 installed the base `opportunity_fact` writer, so the next safe step is to persist explicit availability failures that explain why opportunities were skipped or degraded. The architecture spec names availability facts as the second P4 sequence layer.
why_not_other_approach:
  - Jump straight to `execution_fact` or `outcome_fact` | would skip the explicit missing-data truth layer required to separate opportunity loss from later execution/outcome effects
  - Fold availability rows into `opportunity_fact` only | would hide first-class availability truth behind overloaded opportunity records
  - Start logging order/chain availability in the same slice | would widen beyond the current discovery/evaluation seam and blur rollback boundaries
truth_layer: Data and infrastructure failures that block or degrade an opportunity must become first-class durable facts rather than remaining only inside rejection strings or transient logs.
control_layer: This packet is limited to the current discovery/evaluation seam in `src/engine/cycle_runtime.py` and a new `src/state/db.py` helper for `availability_fact` rows. It may cover city-target and candidate-scope failures on the opportunity-entry path, but must not yet change execution/order-chain availability handling, analytics queries, or other P4 fact writers.
evidence_layer: Targeted runtime/db tests, work-packet grammar check, kernel-manifest check, rollback note, explicit no-other-fact-layer-yet note, and explicit capability-present/capability-absent proof note.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-06
  - INV-09
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/engine/AGENTS.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - work_packets/P4.1-OPPORTUNITY-FACTS.md
  - src/engine/cycle_runtime.py
  - src/state/db.py
  - tests/test_runtime_guards.py
  - tests/test_db.py
files_may_change:
  - work_packets/P4.2-AVAILABILITY-FACTS.md
  - src/engine/cycle_runtime.py
  - src/state/db.py
  - tests/test_runtime_guards.py
  - tests/test_db.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/control/**
  - src/execution/**
  - src/observability/**
  - src/riskguard/**
  - src/supervisor_api/**
  - tests/test_architecture_contracts.py
  - tests/test_pnl_flow_and_audit.py
  - tests/test_replay_time_provenance.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted availability-fact runtime/db tests
parity_required: false
replay_required: false
rollback: Revert the P4.2 runtime/state/test edits and paired slim control-surface updates together; repo returns to the accepted post-P4.1 boundary where opportunity facts exist but availability facts are not yet durably written.
acceptance:
  - Discovery/evaluation-time data or infrastructure failures on the current opportunity-entry path write durable `availability_fact` rows with explicit scope and impact.
  - Non-availability no-trade outcomes such as edge insufficiency do not create `availability_fact` rows.
  - `availability_fact` writes stay limited to the current city-target/candidate discovery-evaluation seam and do not widen into order/chain execution availability or analytics work.
  - When the `availability_fact` table is unavailable, behavior is explicit and test-proven rather than silently pretending durable write success.
  - No `execution_fact`, `outcome_fact`, analytics-query, or schema change is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - kernel-manifest check output
  - rollback note
  - no-other-fact-layer-yet note
  - capability-present-and-absent note
```

## Notes

- Team remains closed by default for this second P4 runtime/state slice.
- This packet installs only the first durable availability-fact writer seam on the discovery/evaluation path.
- The next P4 packet after this one is `P4.3-EXECUTION-FACTS`.

## Closeout readiness notes

- No-other-fact-layer-yet note: This packet writes only `availability_fact`; it does not add `execution_fact`, `outcome_fact`, analytics-query, or schema work.
- Capability-present-and-absent note: targeted tests must prove the write path when `availability_fact` exists and the explicit advisory skip path when it does not.
