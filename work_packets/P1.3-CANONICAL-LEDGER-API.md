# P1.3-CANONICAL-LEDGER-API

```yaml
work_packet_id: P1.3-CANONICAL-LEDGER-API
packet_type: refactor_packet
objective: Extract the canonical append/project API from `src/state/db.py` into dedicated `src/state/ledger.py` and `src/state/projection.py` modules while preserving the current helper behavior and keeping runtime wiring/cutover explicitly out of scope.
why_this_now: `P1.2-CANONICAL-SCHEMA-BOOTSTRAP` is pushed, so the next Stage-2 slice is the append/project API itself. Repo truth still keeps the canonical API buried inside `src/state/db.py`, while the architecture spec requires dedicated ledger/projection modules.
why_not_other_approach:
  - Jump straight to dual-write | would widen from API extraction into runtime/cutover work before the dedicated canonical module boundary exists
  - Leave the API in `db.py` until later | keeps canonical authority logic hidden inside transitional runtime code and weakens the P1 module boundary
truth_layer: Canonical lifecycle append/project logic should live in dedicated authority modules rather than remain buried inside the transitional DB utility file.
control_layer: This packet only extracts and preserves the helper-level API surface; it does not wire runtime callers into canonical writes, change `init_schema()`, or introduce dual-write/DB-first behavior.
evidence_layer: Targeted architecture-contract tests, module-surface proof, rollback note, and explicit no-cutover note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - work_packets/P1.2-CANONICAL-SCHEMA-BOOTSTRAP.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.3-CANONICAL-LEDGER-API.md
  - src/state/db.py
  - src/state/ledger.py
  - src/state/projection.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/engine/**
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the dedicated ledger/projection modules, any compatibility wrappers in `src/state/db.py`, targeted tests, and paired Architects control-surface updates together.
acceptance:
  - `src/state/ledger.py` exposes the canonical append API.
  - `src/state/projection.py` exposes the canonical projection helper surface used by the append API.
  - `src/state/db.py` preserves compatibility wrappers without changing current runtime wiring.
  - Targeted tests prove the extracted API still enforces the same event/projection boundary checks.
  - No dual-write, DB-first read, or runtime cutover behavior is introduced.
evidence_required:
  - targeted pytest output
  - module-surface proof
  - rollback note
  - no-cutover note
```

## Notes

- This packet advances the Stage-2 append/project API boundary after the P1.2 schema-bootstrap guard slice.
- It is still not a runtime cutover packet.
- Team remains disallowed by default because this is a K0/K2 authority-bearing extraction slice.
