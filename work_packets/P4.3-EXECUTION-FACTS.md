# P4.3-EXECUTION-FACTS

```yaml
work_packet_id: P4.3-EXECUTION-FACTS
packet_type: feature_packet
objective: Install the first durable `execution_fact` writer path for current entry and exit order lifecycle events on the runtime execution seam, without widening into `outcome_fact`, analytics work, schema changes, or broader lifecycle redesign.
why_this_now: P4.1 and P4.2 now durably record opportunity and availability truth, so the next safe step in the architecture sequence is to persist execution-order lifecycle truth separately from opportunity loss and final outcomes.
why_not_other_approach:
  - Jump to `outcome_fact` first | would skip the order-lifecycle layer needed to separate execution quality from final economic outcomes
  - Derive execution facts only later from mixed event logs | would keep execution truth indirect instead of first-class durable fact rows
  - Widen into full lifecycle/canonical-event redesign now | would erase rollback boundaries and mix architecture work into a fact-layer packet
truth_layer: One row in `execution_fact` must represent an order lifecycle on the current runtime seam, preserving order role, posted/filled/voided timing, pricing, shares, quality, and terminal execution status without pretending final economic outcome is already known.
control_layer: This packet is limited to `src/state/db.py` plus the current entry/exit execution call sites (`src/engine/cycle_runtime.py`, `src/execution/exit_lifecycle.py`) and targeted tests. It may install durable `execution_fact` writes for the current entry and exit order lifecycle paths, but must not add `outcome_fact`, analytics-query, or schema work.
evidence_layer: Targeted runtime/db tests, work-packet grammar check, kernel-manifest check, rollback note, explicit no-outcome-fact-yet note, and explicit capability-present/capability-absent proof note.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-06
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/engine/AGENTS.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - work_packets/P4.2-AVAILABILITY-FACTS.md
  - src/state/db.py
  - src/engine/cycle_runtime.py
  - src/execution/exit_lifecycle.py
  - tests/test_db.py
  - tests/test_runtime_guards.py
files_may_change:
  - work_packets/P4.3-EXECUTION-FACTS.md
  - src/state/db.py
  - src/engine/cycle_runtime.py
  - src/execution/exit_lifecycle.py
  - tests/test_db.py
  - tests/test_runtime_guards.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - migrations/**
  - src/control/**
  - src/observability/**
  - src/riskguard/**
  - src/supervisor_api/**
  - tests/test_architecture_contracts.py
  - tests/test_pnl_flow_and_audit.py
  - tests/test_replay_time_provenance.py
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted execution-fact runtime/db tests
parity_required: false
replay_required: false
rollback: Revert the P4.3 runtime/state/test edits and paired slim control-surface updates together; repo returns to the accepted post-P4.2 boundary where opportunity and availability facts exist but execution facts are not yet durably written.
acceptance:
  - Current entry order lifecycle writes durable `execution_fact` rows with explicit order role and terminal execution status.
  - Current exit order lifecycle writes durable `execution_fact` rows with explicit order role and terminal execution status.
  - `execution_fact` writes remain separate from final `outcome_fact` truth and do not overclaim economic completion.
  - When the `execution_fact` table is unavailable, behavior is explicit and test-proven rather than silently pretending durable write success.
  - No `outcome_fact`, analytics-query, or schema change is mixed into this packet.
evidence_required:
  - targeted pytest output
  - work-packet grammar output
  - kernel-manifest check output
  - rollback note
  - no-outcome-fact-yet note
  - capability-present-and-absent note
```

## Notes

- Team remains closed by default for this third P4 runtime/state slice.
- This packet installs only the first durable execution-fact writer seam on the current entry/exit order lifecycle path.
- The next P4 packet after this one is `P4.4-OUTCOME-FACTS`.

## Closeout readiness notes

- No-outcome-fact-yet note: This packet writes only `execution_fact`; it does not add `outcome_fact`, analytics-query, or schema work.
- Capability-present-and-absent note: targeted tests must prove the write path when `execution_fact` exists and the explicit advisory skip path when it does not.
- Discrete-settlement-authority note: this packet records runtime execution telemetry only; it does not derive pricing, variance, or settlement interpretation from contract-support geometry.
