# P0.4-DATA-AVAILABILITY-TRUTH

```yaml
work_packet_id: P0.4-DATA-AVAILABILITY-TRUTH
packet_type: feature_packet
objective: Make upstream data availability explicit truth on the touched path so missing, stale, rate-limited, or chain-unavailable conditions are recorded as first-class outcomes rather than disappearing into logs.
why_this_now: Foundation ordering requires P0.4 after P0.3 because learning and diagnostics become dishonest if missing data remains implicit while the system advances toward canonical authority.
why_not_other_approach:
  - Jump to P0.5 or P1 first | later learning/protection work would inherit hidden availability bias
  - Wait for the full learning layer | opportunity loss and reliability drift would stay undercounted
truth_layer: Availability failures on the touched path must become explicit decision-time facts rather than incidental log noise.
control_layer: This packet defines touched runtime/diagnostic fact recording only; broader analytics and later learning work remain outside scope.
evidence_layer: Targeted tests, no-scope-widening note, rollback note, and explicit proof that unavailable/stale/rate-limited conditions are surfaced as first-class outcomes on the touched path.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-06
  - INV-09
required_reads:
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - architecture/self_check/authority_index.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/invariants.yaml
  - architecture/negative_constraints.yaml
  - src/engine/evaluator.py
  - src/engine/cycle_runtime.py
  - src/state/decision_chain.py
files_may_change:
  - src/engine/evaluator.py
  - src/engine/cycle_runtime.py
  - src/state/decision_chain.py
  - tests/test_runtime_guards.py
  - architects_progress.md
  - architects_task.md
  - work_packets/P0.4-DATA-AVAILABILITY-TRUTH.md
files_may_not_change:
  - migrations/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - src/control/**
  - src/supervisor_api/**
  - .github/workflows/**
  - .claude/CLAUDE.md
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
tests_required:
  - targeted runtime-guard tests
  - architecture-contract verification for missing-data truth anchors
parity_required: false
replay_required: false
rollback: Revert touched runtime/diagnostic files, targeted tests, and paired Architects ledger updates together.
acceptance:
  - The touched path records explicit data-availability outcomes rather than hiding them as generic failures or logs.
  - Diagnostics can separate “no edge” from “no reliable data” on the touched path.
  - No P0.5/P1/P2/P4 learning-layer batching occurs in this packet.
  - No schema or migration work is introduced.
evidence_required:
  - targeted test output
  - invariant reference note
  - rollback note
  - no-scope-widening note
```

## Notes

- This packet is about explicit data-availability truth on the touched path, not the full later learning layer.
- If execution reveals that broader fact-table/schema work is required immediately, stop and freeze a narrower successor packet rather than widening this one.
