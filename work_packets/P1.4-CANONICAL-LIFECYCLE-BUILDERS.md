# P1.4-CANONICAL-LIFECYCLE-BUILDERS

```yaml
work_packet_id: P1.4-CANONICAL-LIFECYCLE-BUILDERS
packet_type: feature_packet
objective: Add pure lifecycle-event/projection builder helpers that translate runtime position/decision context into canonical `position_events` and `position_current` payloads without wiring any runtime caller to the canonical ledger yet.
why_this_now: `P1.3-CANONICAL-LEDGER-API` is pushed, so the next narrow prerequisite before runtime caller migration is the builder layer named by the architecture spec. Runtime callers cannot dual-write safely until the canonical payload shapes are constructed by a dedicated, testable translation surface.
why_not_other_approach:
  - Start dual-writing directly from runtime callers now | would force caller-specific payload construction without a dedicated builder layer and widen the first migration packet
  - Hide payload translation inside `db.py` or caller code | would weaken the module boundary that P1.3 just established
truth_layer: Canonical append/project writes need a dedicated translation layer from runtime objects into canonical event/projection payloads.
control_layer: This packet adds pure builder helpers only; it does not change runtime caller behavior, dual-write, or cutover semantics.
evidence_layer: Targeted architecture-contract tests, builder-surface proof, rollback note, and explicit no-dual-write note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - work_packets/P1.3-CANONICAL-LEDGER-API.md
  - src/engine/AGENTS.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/engine/cycle_runtime.py
  - src/state/ledger.py
  - src/state/projection.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.4-CANONICAL-LIFECYCLE-BUILDERS.md
  - src/engine/lifecycle_events.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/state/db.py
  - src/engine/cycle_runtime.py
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the lifecycle builder module, targeted tests, and paired Architects control-surface updates together.
acceptance:
  - `src/engine/lifecycle_events.py` exists as a pure builder surface.
  - Builders produce canonical event/projection payloads that match the Stage-2 schema/API contract.
  - No runtime caller is switched to dual-write in this packet.
  - No cutover, DB-first read, or reconciliation wiring is introduced.
evidence_required:
  - targeted pytest output
  - builder-surface proof
  - rollback note
  - no-dual-write note
```

## Notes

- This packet prepares the first runtime-caller migration without starting it.
- It stays deliberately below the line of dual-write.
- Team remains disallowed by default because this is still a K0/K2 authority-bearing bridge packet.
