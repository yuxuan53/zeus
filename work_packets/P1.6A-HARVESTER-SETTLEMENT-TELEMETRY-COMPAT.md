# P1.6A-HARVESTER-SETTLEMENT-TELEMETRY-COMPAT

```yaml
work_packet_id: P1.6A-HARVESTER-SETTLEMENT-TELEMETRY-COMPAT
packet_type: refactor_packet
objective: Make the settlement telemetry helper in `src/state/db.py` degrade cleanly on canonically bootstrapped databases so the remaining harvester compatibility inventory can proceed without this raw legacy-helper crash.
why_this_now: `P1.5B-CYCLE-RUNTIME-ENTRY-DUAL-WRITE` is pushed. The next remaining P1 dual-write family in spec order is harvester, and repo truth shows `log_settlement_event()` still routes through the generic legacy event helper that fails on canonical-only DBs.
why_not_other_approach:
  - Jump straight to harvester dual-write now | would repeat the same helper-compat contradiction already encountered on the first caller migration
  - Broaden into a general state-helper overhaul | exceeds the narrow blocker needed to unblock the next harvester packet
truth_layer: The repo must distinguish generic fail-loud legacy-helper misuse from the narrower settlement-telemetry compatibility required before harvester can dual-write against canonical schema.
control_layer: This packet changes only the settlement telemetry helper compatibility behavior in `src/state/db.py`; it does not migrate harvester itself, remove generic fail-loud guards broadly, or alter canonical ledger/schema modules.
evidence_layer: Targeted tests, compatibility proof note, remaining-blocker note, rollback note, and explicit no-harvester-migration note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/state/db.py
  - src/execution/harvester.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.6A-HARVESTER-SETTLEMENT-TELEMETRY-COMPAT.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/engine/**
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - src/state/ledger.py
  - src/state/projection.py
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the settlement telemetry helper compatibility change, targeted tests, and paired Architects control-surface updates together; settlement telemetry returns to the previous fail-loud canonical-schema behavior.
acceptance:
  - `log_settlement_event` degrades cleanly on canonically bootstrapped DBs instead of crashing through legacy `position_events` assumptions.
  - Generic legacy-helper misuse remains explicitly guarded where this packet does not need to relax it.
  - Legacy-schema behavior remains unchanged for the touched helper.
  - No harvester caller is migrated in this packet.
  - Remaining harvester blockers are recorded explicitly instead of implied away.
evidence_required:
  - targeted pytest output
  - compatibility proof note
  - remaining-blocker note
  - rollback note
  - no-harvester-migration note
```

## Notes

- This is a blocker-removal packet for the harvester family, analogous in shape to `P1.5A` but restricted to settlement telemetry.
- It is not a cutover packet and does not itself dual-write harvester state.
- It does **not** claim harvester is now fully canonical-safe; it removes one known crash while leaving remaining blockers explicit.
- Team remains closed by default; this is a narrow K0/K2 unblocker.
