# P1.7A-RECONCILIATION-LIFECYCLE-EVENT-COMPAT

```yaml
work_packet_id: P1.7A-RECONCILIATION-LIFECYCLE-EVENT-COMPAT
packet_type: refactor_packet
objective: Make the reconciliation lifecycle-event helper in `src/state/db.py` degrade cleanly on canonically bootstrapped databases so the remaining reconciliation blocker inventory can proceed without the current raw legacy-event-helper crash.
why_this_now: `P1.6D-HARVESTER-SETTLEMENT-DUAL-WRITE` is pushed. The next remaining P1 dual-write family is reconciliation, and repo truth shows `log_reconciled_entry_event()` still routes through the generic legacy event helper that fails on canonical-only DBs.
why_not_other_approach:
  - Jump straight to reconciliation dual-write now | would repeat the same helper-compat contradiction pattern already resolved for entry and harvester
  - Broaden into a general reconciliation/state-helper overhaul | exceeds the narrow blocker needed to unblock the next reconciliation packet
truth_layer: The repo must distinguish generic fail-loud legacy-helper misuse from the narrower reconciliation-event compatibility needed before reconciliation can dual-write against canonical schema.
control_layer: This packet changes only the reconciliation lifecycle-event helper compatibility behavior in `src/state/db.py`; it does not migrate reconciliation itself, remove generic fail-loud guards broadly, or alter canonical ledger/schema modules.
evidence_layer: Targeted tests, compatibility proof note, remaining-blocker note, rollback note, and explicit no-reconciliation-migration note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/state/db.py
  - src/state/chain_reconciliation.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.7A-RECONCILIATION-LIFECYCLE-EVENT-COMPAT.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
  - architects_state_index.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/engine/**
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - src/state/chronicler.py
  - src/state/ledger.py
  - src/state/projection.py
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the reconciliation-event compatibility change, targeted tests, and paired Architects control-surface updates together; reconciliation lifecycle-event logging returns to the previous fail-loud canonical-schema behavior.
acceptance:
  - `log_reconciled_entry_event` degrades cleanly on canonically bootstrapped DBs instead of crashing through legacy `position_events` assumptions.
  - Generic legacy-helper misuse remains explicitly guarded where this packet does not need to relax it.
  - Legacy-schema behavior remains unchanged for the touched helper.
  - No reconciliation caller is migrated in this packet.
evidence_required:
  - targeted pytest output
  - compatibility proof note
  - remaining-blocker note
  - rollback note
  - no-reconciliation-migration note
```

## Notes

- This is the next blocker-removal packet after the harvester settlement slice.
- It is not a cutover packet and does not itself dual-write reconciliation state.
- It does **not** claim reconciliation is now canonical-safe; it removes one known lifecycle-event crash while leaving the query-path blocker explicit.
- Team remains closed by default; this is a narrow K0/K2 unblocker.
