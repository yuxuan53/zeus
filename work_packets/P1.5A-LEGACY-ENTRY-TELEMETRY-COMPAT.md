# P1.5A-LEGACY-ENTRY-TELEMETRY-COMPAT

```yaml
work_packet_id: P1.5A-LEGACY-ENTRY-TELEMETRY-COMPAT
packet_type: refactor_packet
objective: Make the legacy entry telemetry helpers in `src/state/db.py` degrade cleanly on canonically bootstrapped databases so the first runtime-caller migration can be retried without raw legacy-helper crashes.
why_this_now: Adversarial review reopened `P1.5-CYCLE-RUNTIME-ENTRY-DUAL-WRITE` because the current entry path still routes through legacy helpers that depend on legacy `position_events` / `trade_decisions` surfaces and can crash on canonically bootstrapped databases.
why_not_other_approach:
  - Ignore the contradiction and accept P1.5 as-is | would create a false-complete Stage-2 claim and hide schema-present runtime failure
  - Widen directly into a broader state/cutover packet | would exceed the narrow blocker needed to unblock the first caller migration
truth_layer: The repo must distinguish between fail-loud generic legacy-helper misuse and the narrower entry-telemetry compatibility needed to let a future caller migration coexist with canonically bootstrapped DBs.
control_layer: This packet changes only legacy entry telemetry compatibility behavior in `src/state/db.py`; it does not migrate any runtime caller, remove legacy writes globally, or alter canonical ledger/schema modules.
evidence_layer: Targeted tests, compatibility proof note, rollback note, and explicit no-caller-migration note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - architecture/self_check/zero_context_entry.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - work_packets/P1.5-CYCLE-RUNTIME-ENTRY-DUAL-WRITE.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.5A-LEGACY-ENTRY-TELEMETRY-COMPAT.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/engine/**
  - src/state/ledger.py
  - src/state/projection.py
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
  - python3 scripts/check_kernel_manifests.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the `src/state/db.py` compatibility change, targeted tests, and paired Architects control-surface updates together; legacy entry telemetry returns to the previous fail-loud canonical-schema behavior.
acceptance:
  - `log_trade_entry` and `log_execution_report` degrade cleanly on canonically bootstrapped DBs instead of crashing through legacy `position_events` assumptions.
  - Generic legacy-helper misuse remains explicitly guarded where this packet does not need to relax it.
  - Legacy-schema behavior remains unchanged for the touched helpers.
  - No runtime caller is migrated in this packet.
evidence_required:
  - targeted pytest output
  - compatibility proof note
  - rollback note
  - no-caller-migration note
```

## Notes

- This is a blocker-removal packet created because `P1.5` could not be accepted honestly.
- It is not a cutover packet and does not replace the later runtime caller migration packet.
- Team remains closed by default; this is a narrow K0/K2 unblocker.
