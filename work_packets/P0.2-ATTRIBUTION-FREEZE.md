# P0.2-ATTRIBUTION-FREEZE

```yaml
work_packet_id: P0.2-ATTRIBUTION-FREEZE
packet_type: refactor_packet
objective: Freeze attribution in the current runtime entry path by having the evaluator emit canonical `strategy_key` directly, preserve it through touched runtime records, and reject missing or malformed attribution instead of downstream invention.
why_this_now: The foundation sequence starts with P0.2 because bearing-capacity work must precede more exciting P1/P2/P3 work. Current runtime still lets `strategy`, `edge_source`, `discovery_mode`, and downstream classification act like competing attribution centers.
why_not_other_approach:
  - Jump to P1/P2/P3 first | later lifecycle/protection work would inherit polluted governance identity
  - Batch all of P0 together | would violate the one-packet-one-objective rule and hide acceptance drift
truth_layer: Existing K0 law already says `strategy_key` is the sole governance key; this packet makes the current runtime entry path conform to that law without changing schema or manifest authority.
control_layer: The evaluator becomes the canonical emitter for the touched runtime path, and downstream touched components preserve or reject attribution instead of re-inventing it.
evidence_layer: Targeted runtime tests, no-scope-widening note, rollback note, and explicit proof that downstream fallback/re-inference is removed on the touched path.
zones_touched:
  - K2_runtime
invariants_touched:
  - INV-04
required_reads:
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - architecture/self_check/authority_index.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/invariants.yaml
  - architecture/negative_constraints.yaml
  - src/state/AGENTS.md
  - src/engine/evaluator.py
  - src/engine/cycle_runtime.py
  - src/state/portfolio.py
  - src/state/decision_chain.py
files_may_change:
  - src/engine/evaluator.py
  - src/engine/cycle_runtime.py
  - src/state/portfolio.py
  - src/state/decision_chain.py
  - tests/test_runtime_guards.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
  - work_packets/P0.2-ATTRIBUTION-FREEZE.md
files_may_not_change:
  - migrations/**
  - architecture/**
  - docs/governance/**
  - docs/architecture/**
  - src/control/**
  - src/supervisor_api/**
  - .github/workflows/**
  - .claude/CLAUDE.md
schema_changes: false
ci_gates_required:
  - python3 scripts/check_work_packets.py
tests_required:
  - targeted attribution regression tests
  - architecture-contract verification for strategy_key law
parity_required: false
replay_required: false
rollback: Revert the touched runtime attribution files, targeted tests, and paired Architects ledger updates together.
acceptance:
  - Evaluator emits canonical `strategy_key` directly on the touched entry path.
  - Touched downstream components stop inventing strategy when the evaluator already assigned it.
  - Missing or malformed attribution is rejected or quarantined on the touched path instead of defaulting to `opening_inertia`.
  - No P0.1, P0.3, P0.4, or P0.5 work is batched into this packet.
  - No schema or migration work is introduced.
evidence_required:
  - targeted test output
  - no-scope-widening note
  - rollback note
  - invariant reference note
```

## Notes

- This is the **first real P0 packet** and must stay bearing-capacity work, not feature work.
- This packet does **not** authorize team execution.
- If execution reveals that schema changes are required immediately, stop and freeze a narrower successor packet rather than widening this one.
