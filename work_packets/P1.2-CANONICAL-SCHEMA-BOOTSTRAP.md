# P1.2-CANONICAL-SCHEMA-BOOTSTRAP

```yaml
work_packet_id: P1.2-CANONICAL-SCHEMA-BOOTSTRAP
packet_type: schema_packet
objective: Add an explicit canonical-schema bootstrap entrypoint that can install the architecture-kernel tables on a fresh database, reject legacy name collision up front, and make post-bootstrap non-runtime-readiness explicit instead of leaving raw legacy-helper failures.
why_this_now: `FOUNDATION-TEAM-GATE` is closed, the mainline has advanced into Stage 2 canonical-authority rollout, and repo truth already contains migration SQL plus helper-level canonical append/project scaffolding but still lacks an explicit collision-aware bootstrap path and a loud transitional guard around legacy helper misuse.
why_not_other_approach:
  - Mutate `init_schema()` into a pretend-canonical installer now | would blur descriptive-vs-normative truth and risk silent live-schema drift
  - Rename or repurpose the live legacy `position_events` table in this slice | widens into migration/cutover work that still requires a later dedicated packet and human-gated decisions if live write paths are affected
truth_layer: Canonical schema add must be explicit and collision-aware; the repo must not silently claim that the canonical table contract is installed when legacy runtime schema still occupies the same authority name, and it must not let a fresh canonical bootstrap masquerade as runtime-ready.
control_layer: This packet adds a narrow schema-bootstrap path, explicit transitional misuse guards, and tests only; it does not switch runtime writers/readers, alter `init_schema()` semantics, or claim DB-first/cutover behavior.
evidence_layer: Packet freeze note, targeted architecture-contract tests, caller-audit evidence, concrete canonical row example, staged parity waiver, rollback note, and explicit no-cutover / not-runtime-ready note.
zones_touched:
  - K0_frozen_kernel
  - K2_runtime
invariants_touched:
  - INV-03
  - INV-08
  - INV-10
required_reads:
  - architecture/self_check/authority_index.md
  - docs/governance/zeus_autonomous_delivery_constitution.md
  - docs/architecture/zeus_durable_architecture_spec.md
  - docs/governance/zeus_change_control_constitution.md
  - architecture/kernel_manifest.yaml
  - architecture/invariants.yaml
  - architecture/zones.yaml
  - architecture/negative_constraints.yaml
  - architecture/self_check/zero_context_entry.md
  - work_packets/FOUNDATION-MAINLINE-PLAN.md
  - work_packets/FOUNDATION-TEAM-GATE.md
  - src/state/AGENTS.md
  - tests/AGENTS.md
  - migrations/AGENTS.md
  - migrations/2026_04_02_architecture_kernel.sql
  - src/state/db.py
  - tests/test_architecture_contracts.py
files_may_change:
  - work_packets/P1.2-CANONICAL-SCHEMA-BOOTSTRAP.md
  - src/state/db.py
  - tests/test_architecture_contracts.py
  - architects_progress.md
  - architects_task.md
files_may_not_change:
  - AGENTS.md
  - migrations/**
  - src/engine/**
  - src/execution/**
  - src/riskguard/**
  - src/control/**
  - src/supervisor_api/**
  - docs/governance/**
  - docs/architecture/**
  - architecture/**
  - .github/workflows/**
  - .claude/CLAUDE.md
  - zeus_final_tribunal_overlay/**
schema_changes: true
ci_gates_required:
  - python3 scripts/check_work_packets.py
tests_required:
  - targeted architecture-contract tests
parity_required: false
replay_required: false
rollback: Revert the canonical-schema bootstrap helper, the targeted tests, and the paired Architects control-surface updates together; canonical schema bootstrap returns to SQL-only/read-only staging.
acceptance:
  - A dedicated schema-bootstrap entrypoint exists for the architecture-kernel SQL.
  - The bootstrap entrypoint succeeds on a fresh database and verifies the canonical `position_events` + `position_current` contract.
  - The bootstrap entrypoint rejects legacy runtime schema collision before claiming canonical schema readiness.
  - Canonically bootstrapped databases fail loudly when legacy runtime `position_events` helpers are used.
  - `init_schema()` remains explicitly legacy/transitional in this slice.
  - No runtime writer/reader cutover, dual-write, or DB-first read behavior is introduced.
  - Replay/parity remains staged-advisory and is not misrepresented as complete.
evidence_required:
  - targeted pytest output
  - caller-audit note
  - concrete canonical row example
  - work-packet grammar output
  - staged parity/replay waiver
  - rollback note
  - no-cutover / not-runtime-ready note
```

## Notes

- This is the first post-`FOUNDATION-TEAM-GATE` mainline packet, but it is a transitional Stage-2 bootstrap/guard slice rather than a claim that Stage-2 schema convergence is complete.
- It advances Stage 2 by making schema installation explicit without pretending runtime convergence.
- The legacy `position_events` name collision remains descriptive runtime truth; this packet only makes that collision impossible to ignore silently.
- A fresh canonically bootstrapped database is intentionally **not** claimed to be runtime-ready in this packet.
