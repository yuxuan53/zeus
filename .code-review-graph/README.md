# Code Review Graph Cache

The graph database (`graph.db`) is a derived local review cache. It may guide
code discovery and blast-radius analysis, but it is not authority and is
intentionally not tracked.

See `architecture/code_review_graph_protocol.yaml` for the integration protocol.
